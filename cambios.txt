diff --git a/Frontend/package-lock.json b/Frontend/package-lock.json
index 3a850c3..c1c8609 100644
--- a/Frontend/package-lock.json
+++ b/Frontend/package-lock.json
@@ -14,7 +14,6 @@
         "@mui/material": "^7.3.1",
         "@tanstack/react-query": "^5.90.2",
         "@toolpad/core": "^0.16.0",
-        "bootstrap": "^5.3.8",
         "boxicons": "^2.1.4",
         "chart.js": "^4.5.0",
         "jwt-decode": "^4.0.0",
@@ -22,7 +21,6 @@
         "lucide-react": "^0.544.0",
         "motion": "^12.23.22",
         "react": "^19.1.0",
-        "react-bootstrap": "^2.10.10",
         "react-chartjs-2": "^5.3.0",
         "react-dom": "^19.1.0",
         "react-icons": "^5.7.0",
@@ -1764,75 +1762,6 @@
         "url": "https://opencollective.com/popperjs"
       }
     },
-    "node_modules/@react-aria/ssr": {
-      "version": "3.9.10",
-      "resolved": "https://registry.npmjs.org/@react-aria/ssr/-/ssr-3.9.10.tgz",
-      "integrity": "sha512-hvTm77Pf+pMBhuBm760Li0BVIO38jv1IBws1xFm1NoL26PU+fe+FMW5+VZWyANR6nYL65joaJKZqOdTQMkO9IQ==",
-      "license": "Apache-2.0",
-      "dependencies": {
-        "@swc/helpers": "^0.5.0"
-      },
-      "engines": {
-        "node": ">= 12"
-      },
-      "peerDependencies": {
-        "react": "^16.8.0 || ^17.0.0-rc.1 || ^18.0.0 || ^19.0.0-rc.1"
-      }
-    },
-    "node_modules/@restart/hooks": {
-      "version": "0.4.16",
-      "resolved": "https://registry.npmjs.org/@restart/hooks/-/hooks-0.4.16.tgz",
-      "integrity": "sha512-f7aCv7c+nU/3mF7NWLtVVr0Ra80RqsO89hO72r+Y/nvQr5+q0UFGkocElTH6MJApvReVh6JHUFYn2cw1WdHF3w==",
-      "license": "MIT",
-      "dependencies": {
-        "dequal": "^2.0.3"
-      },
-      "peerDependencies": {
-        "react": ">=16.8.0"
-      }
-    },
-    "node_modules/@restart/ui": {
-      "version": "1.9.4",
-      "resolved": "https://registry.npmjs.org/@restart/ui/-/ui-1.9.4.tgz",
-      "integrity": "sha512-N4C7haUc3vn4LTwVUPlkJN8Ach/+yIMvRuTVIhjilNHqegY60SGLrzud6errOMNJwSnmYFnt1J0H/k8FE3A4KA==",
-      "license": "MIT",
-      "dependencies": {
-        "@babel/runtime": "^7.26.0",
-        "@popperjs/core": "^2.11.8",
-        "@react-aria/ssr": "^3.5.0",
-        "@restart/hooks": "^0.5.0",
-        "@types/warning": "^3.0.3",
-        "dequal": "^2.0.3",
-        "dom-helpers": "^5.2.0",
-        "uncontrollable": "^8.0.4",
-        "warning": "^4.0.3"
-      },
-      "peerDependencies": {
-        "react": ">=16.14.0",
-        "react-dom": ">=16.14.0"
-      }
-    },
-    "node_modules/@restart/ui/node_modules/@restart/hooks": {
-      "version": "0.5.1",
-      "resolved": "https://registry.npmjs.org/@restart/hooks/-/hooks-0.5.1.tgz",
-      "integrity": "sha512-EMoH04NHS1pbn07iLTjIjgttuqb7qu4+/EyhAx27MHpoENcB2ZdSsLTNxmKD+WEPnZigo62Qc8zjGnNxoSE/5Q==",
-      "license": "MIT",
-      "dependencies": {
-        "dequal": "^2.0.3"
-      },
-      "peerDependencies": {
-        "react": ">=16.8.0"
-      }
-    },
-    "node_modules/@restart/ui/node_modules/uncontrollable": {
-      "version": "8.0.4",
-      "resolved": "https://registry.npmjs.org/uncontrollable/-/uncontrollable-8.0.4.tgz",
-      "integrity": "sha512-ulRWYWHvscPFc0QQXvyJjY6LIXU56f0h8pQFvhxiKk5V1fcI8gp9Ht9leVAhrVjzqMw0BgjspBINx9r6oyJUvQ==",
-      "license": "MIT",
-      "peerDependencies": {
-        "react": ">=16.14.0"
-      }
-    },
     "node_modules/@rolldown/pluginutils": {
       "version": "1.0.0-beta.27",
       "resolved": "https://registry.npmjs.org/@rolldown/pluginutils/-/pluginutils-1.0.0-beta.27.tgz",
@@ -2336,15 +2265,6 @@
       "dev": true,
       "license": "Apache-2.0"
     },
-    "node_modules/@swc/helpers": {
-      "version": "0.5.17",
-      "resolved": "https://registry.npmjs.org/@swc/helpers/-/helpers-0.5.17.tgz",
-      "integrity": "sha512-5IKx/Y13RsYd+sauPb2x+U/xZikHjolzfuDgTAl/Tdf3Q8rslRvC19NKDLgAJQ6wsqADk10ntlv08nPFw/gO/A==",
-      "license": "Apache-2.0",
-      "dependencies": {
-        "tslib": "^2.8.0"
-      }
-    },
     "node_modules/@swc/types": {
       "version": "0.1.23",
       "resolved": "https://registry.npmjs.org/@swc/types/-/types-0.1.23.tgz",
@@ -2513,12 +2433,6 @@
         "@types/react": "*"
       }
     },
-    "node_modules/@types/warning": {
-      "version": "3.0.3",
-      "resolved": "https://registry.npmjs.org/@types/warning/-/warning-3.0.3.tgz",
-      "integrity": "sha512-D1XC7WK8K+zZEveUPY+cf4+kgauk8N4eHr/XIHXGlGYkHLud6hK9lYfZk1ry1TNh798cZUCgb6MqGEG8DkJt6Q==",
-      "license": "MIT"
-    },
     "node_modules/@vitejs/plugin-react-swc": {
       "version": "3.11.0",
       "resolved": "https://registry.npmjs.org/@vitejs/plugin-react-swc/-/plugin-react-swc-3.11.0.tgz",
@@ -2629,25 +2543,6 @@
       "dev": true,
       "license": "MIT"
     },
-    "node_modules/bootstrap": {
-      "version": "5.3.8",
-      "resolved": "https://registry.npmjs.org/bootstrap/-/bootstrap-5.3.8.tgz",
-      "integrity": "sha512-HP1SZDqaLDPwsNiqRqi5NcP0SSXciX2s9E+RyqJIIqGo+vJeN5AJVM98CXmW/Wux0nQ5L7jeWUdplCEf0Ee+tg==",
-      "funding": [
-        {
-          "type": "github",
-          "url": "https://github.com/sponsors/twbs"
-        },
-        {
-          "type": "opencollective",
-          "url": "https://opencollective.com/bootstrap"
-        }
-      ],
-      "license": "MIT",
-      "peerDependencies": {
-        "@popperjs/core": "^2.11.8"
-      }
-    },
     "node_modules/boxicons": {
       "version": "2.1.4",
       "resolved": "https://registry.npmjs.org/boxicons/-/boxicons-2.1.4.tgz",
@@ -2828,12 +2723,6 @@
         "url": "https://paulmillr.com/funding/"
       }
     },
-    "node_modules/classnames": {
-      "version": "2.5.1",
-      "resolved": "https://registry.npmjs.org/classnames/-/classnames-2.5.1.tgz",
-      "integrity": "sha512-saHYOzhIQs6wy2sVxTM6bUDsQO4F50V9RQ22qBpEdCW+I+/Wmke2HOl6lS6dTpdxVhb88/I6+Hs+438c3lfUow==",
-      "license": "MIT"
-    },
     "node_modules/client-only": {
       "version": "0.0.1",
       "resolved": "https://registry.npmjs.org/client-only/-/client-only-0.0.1.tgz",
@@ -2988,15 +2877,6 @@
       "dev": true,
       "license": "MIT"
     },
-    "node_modules/dequal": {
-      "version": "2.0.3",
-      "resolved": "https://registry.npmjs.org/dequal/-/dequal-2.0.3.tgz",
-      "integrity": "sha512-0je+qPKHEMohvfRTCEo3CrPG6cAzAYgmzKyxRiYSSDkS6eGJdyVJm7WaYA5ECaAD9wLB2T4EEeymA5aFVcYXCA==",
-      "license": "MIT",
-      "engines": {
-        "node": ">=6"
-      }
-    },
     "node_modules/detect-hover": {
       "version": "1.0.3",
       "resolved": "https://registry.npmjs.org/detect-hover/-/detect-hover-1.0.3.tgz",
@@ -4340,25 +4220,6 @@
         "react-is": "^16.13.1"
       }
     },
-    "node_modules/prop-types-extra": {
-      "version": "1.1.1",
-      "resolved": "https://registry.npmjs.org/prop-types-extra/-/prop-types-extra-1.1.1.tgz",
-      "integrity": "sha512-59+AHNnHYCdiC+vMwY52WmvP5dM3QLeoumYuEyceQDi9aEhtwN9zIQ2ZNo25sMyXnbh32h+P1ezDsUpUH3JAew==",
-      "license": "MIT",
-      "dependencies": {
-        "react-is": "^16.3.2",
-        "warning": "^4.0.0"
-      },
-      "peerDependencies": {
-        "react": ">=0.14.0"
-      }
-    },
-    "node_modules/prop-types-extra/node_modules/react-is": {
-      "version": "16.13.1",
-      "resolved": "https://registry.npmjs.org/react-is/-/react-is-16.13.1.tgz",
-      "integrity": "sha512-24e6ynE2H+OKt4kqsOvNd8kBpV65zoxbA4BVsEOB3ARVWQki/DHzaUoC5KuON/BiccDaCCTZBuOcfZs70kR8bQ==",
-      "license": "MIT"
-    },
     "node_modules/prop-types/node_modules/react-is": {
       "version": "16.13.1",
       "resolved": "https://registry.npmjs.org/react-is/-/react-is-16.13.1.tgz",
@@ -4384,37 +4245,6 @@
         "node": ">=0.10.0"
       }
     },
-    "node_modules/react-bootstrap": {
-      "version": "2.10.10",
-      "resolved": "https://registry.npmjs.org/react-bootstrap/-/react-bootstrap-2.10.10.tgz",
-      "integrity": "sha512-gMckKUqn8aK/vCnfwoBpBVFUGT9SVQxwsYrp9yDHt0arXMamxALerliKBxr1TPbntirK/HGrUAHYbAeQTa9GHQ==",
-      "license": "MIT",
-      "dependencies": {
-        "@babel/runtime": "^7.24.7",
-        "@restart/hooks": "^0.4.9",
-        "@restart/ui": "^1.9.4",
-        "@types/prop-types": "^15.7.12",
-        "@types/react-transition-group": "^4.4.6",
-        "classnames": "^2.3.2",
-        "dom-helpers": "^5.2.1",
-        "invariant": "^2.2.4",
-        "prop-types": "^15.8.1",
-        "prop-types-extra": "^1.1.0",
-        "react-transition-group": "^4.4.5",
-        "uncontrollable": "^7.2.1",
-        "warning": "^4.0.3"
-      },
-      "peerDependencies": {
-        "@types/react": ">=16.14.8",
-        "react": ">=16.14.0",
-        "react-dom": ">=16.14.0"
-      },
-      "peerDependenciesMeta": {
-        "@types/react": {
-          "optional": true
-        }
-      }
-    },
     "node_modules/react-chartjs-2": {
       "version": "5.3.0",
       "resolved": "https://registry.npmjs.org/react-chartjs-2/-/react-chartjs-2-5.3.0.tgz",
@@ -4466,12 +4296,6 @@
       "integrity": "sha512-tr41fA15Vn8p4X9ntI+yCyeGSf1TlYaY5vlTZfQmeLBrFo3psOPX6HhTDnFNL9uj3EhP0KAQ80cugCl4b4BERA==",
       "license": "MIT"
     },
-    "node_modules/react-lifecycles-compat": {
-      "version": "3.0.4",
-      "resolved": "https://registry.npmjs.org/react-lifecycles-compat/-/react-lifecycles-compat-3.0.4.tgz",
-      "integrity": "sha512-fBASbA6LnOU9dOU2eW7aQ8xmYBSXUIWr+UmF9b1efZBazGNO+rcXT/icdKnYm2pTwcRylVUYwW7H1PHfLekVzA==",
-      "license": "MIT"
-    },
     "node_modules/react-router": {
       "version": "7.8.2",
       "resolved": "https://registry.npmjs.org/react-router/-/react-router-7.8.2.tgz",
@@ -5292,21 +5116,6 @@
         "node": ">= 0.8.0"
       }
     },
-    "node_modules/uncontrollable": {
-      "version": "7.2.1",
-      "resolved": "https://registry.npmjs.org/uncontrollable/-/uncontrollable-7.2.1.tgz",
-      "integrity": "sha512-svtcfoTADIB0nT9nltgjujTi7BzVmwjZClOmskKu/E8FW9BXzg9os8OLr4f8Dlnk0rYWJIWr4wv9eKUXiQvQwQ==",
-      "license": "MIT",
-      "dependencies": {
-        "@babel/runtime": "^7.6.3",
-        "@types/react": ">=16.9.11",
-        "invariant": "^2.2.4",
-        "react-lifecycles-compat": "^3.0.4"
-      },
-      "peerDependencies": {
-        "react": ">=15.0.0"
-      }
-    },
     "node_modules/uri-js": {
       "version": "4.4.1",
       "resolved": "https://registry.npmjs.org/uri-js/-/uri-js-4.4.1.tgz",
diff --git a/Frontend/package.json b/Frontend/package.json
index 6f79a9d..5397f8e 100644
--- a/Frontend/package.json
+++ b/Frontend/package.json
@@ -16,7 +16,6 @@
     "@mui/material": "^7.3.1",
     "@tanstack/react-query": "^5.90.2",
     "@toolpad/core": "^0.16.0",
-    "bootstrap": "^5.3.8",
     "boxicons": "^2.1.4",
     "chart.js": "^4.5.0",
     "jwt-decode": "^4.0.0",
@@ -24,7 +23,6 @@
     "lucide-react": "^0.544.0",
     "motion": "^12.23.22",
     "react": "^19.1.0",
-    "react-bootstrap": "^2.10.10",
     "react-chartjs-2": "^5.3.0",
     "react-dom": "^19.1.0",
     "react-icons": "^5.7.0",
diff --git a/Frontend/src/Components/Admin/DashboardAdmin.jsx b/Frontend/src/Components/Admin/DashboardAdmin.jsx
new file mode 100644
index 0000000..fdfbc3f
--- /dev/null
+++ b/Frontend/src/Components/Admin/DashboardAdmin.jsx
@@ -0,0 +1,182 @@
+import { useMemo, useState } from "react";
+import {
+    ShieldCheck,
+    Users,
+    UserCheck,
+    UserX,
+    Search,
+    Mail,
+    AtSign,
+} from "lucide-react";
+import { useUsuarios } from "../../Hooks/Vendedor/useUsuarios";
+import styles from "../../assets/Css/Admin/DashboardAdmin.module.scss";
+import ImagePreview from "../../Utils/Components/ImagePreview";
+
+export default function DashboardAdmin() {
+    const { data: usuarios = [], isLoading, error } = useUsuarios();
+    const [busqueda, setBusqueda] = useState("");
+
+    const usuariosFiltrados = useMemo(() => {
+        return usuarios.filter((usuario) => {
+            const texto = busqueda.toLowerCase();
+
+            return (
+                usuario.nombre.toLowerCase().includes(texto) ||
+                usuario.usuario.toLowerCase().includes(texto) ||
+                usuario.correo.toLowerCase().includes(texto)
+            );
+        });
+    }, [usuarios, busqueda]);
+
+    const totalUsuarios = usuarios.length;
+
+    const activos = usuarios.filter(
+        (usuario) => usuario.activo
+    ).length;
+
+    const inactivos = totalUsuarios - activos;
+
+    const administradores = usuarios.filter(
+        (usuario) => usuario.rol === 1
+    ).length;
+
+    const empleados = usuarios.filter(
+        (usuario) => usuario.rol === 2
+    ).length;
+
+    const obtenerRol = (rol) => {
+        switch (rol) {
+            case 1:
+                return "Administrador";
+            case 2:
+                return "Empleado";
+            default:
+                return "Usuario";
+        }
+    };
+
+    if (isLoading) {
+        return (
+            <section className={styles.dashboard}>
+                <div className="cargando">
+                    <i className="bx bx-loader-alt bx-spin"></i>
+                    <p>Cargando usuarios...</p>
+                </div>
+            </section>
+        );
+    }
+
+    if (error) {
+        return (
+            <section className={styles.dashboard}>
+                <div className={styles.error}>
+                    Error al cargar los usuarios.
+                </div>
+            </section>
+        );
+    }
+
+    return (
+        <main className={styles.dashboard}>
+            <header className={styles.header}>
+                <div>
+                    <h1>Administración</h1>
+                    <p>Gestiona los usuarios registrados en el sistema.</p>
+                </div>
+            </header>
+
+            <section className={styles.stats}>
+                <article className={styles.statCard}>
+                    <Users size={28} />
+                    <span>{totalUsuarios}</span>
+                    <small>Total usuarios</small>
+                </article>
+
+                <article className={styles.statCard}>
+                    <UserCheck size={28} />
+                    <span>{activos}</span>
+                    <small>Activos</small>
+                </article>
+
+                <article className={styles.statCard}>
+                    <UserX size={28} />
+                    <span>{empleados}</span>
+                    <small>Empleados</small>
+                </article>
+
+                <article className={styles.statCard}>
+                    <ShieldCheck size={28} />
+                    <span>{administradores}</span>
+                    <small>Administradores</small>
+                </article>
+            </section>
+
+            <div className={styles.searchContainer}>
+                <Search size={18} />
+                <input
+                    type="text"
+                    placeholder="Buscar usuario..."
+                    value={busqueda}
+                    onChange={(e) => setBusqueda(e.target.value)}
+                />
+            </div>
+
+            <section className={styles.usersGrid}>
+                {usuariosFiltrados.map((usuario) => (
+                    <article
+                        key={usuario.id_usuario}
+                        className={styles.userCard}
+                    >
+                        <div className={styles.avatar}>
+                            <ImagePreview
+                                src={usuario.imagen}
+                                alt={usuario.nombre}
+                                fallback={<span>
+                                    {usuario.nombre.charAt(0).toUpperCase()}
+                                </span>}
+                            />
+                        </div>
+
+                        <div className={styles.userInfo}>
+                            <div className={styles.top}>
+                                <h3>{usuario.nombre}</h3>
+
+                                <span
+                                    className={
+                                        usuario.activo
+                                            ? styles.activo
+                                            : styles.inactivo
+                                    }
+                                >
+                                    {usuario.activo
+                                        ? "Activo"
+                                        : "Inactivo"}
+                                </span>
+                            </div>
+
+                            <p>
+                                <AtSign size={15} />
+                                {usuario.usuario}
+                            </p>
+
+                            <p>
+                                <Mail size={15} />
+                                {usuario.correo}
+                            </p>
+
+                            <span className={styles.rol}>
+                                {obtenerRol(usuario.rol)}
+                            </span>
+                        </div>
+                    </article>
+                ))}
+
+                {usuariosFiltrados.length === 0 && (
+                    <div className={styles.empty}>
+                        No se encontraron usuarios.
+                    </div>
+                )}
+            </section>
+        </main>
+    );
+}
\ No newline at end of file
diff --git a/Frontend/src/Components/Client/DatosPersonales/Login.jsx b/Frontend/src/Components/Client/DatosPersonales/Login.jsx
index 01b6662..9c64ecc 100644
--- a/Frontend/src/Components/Client/DatosPersonales/Login.jsx
+++ b/Frontend/src/Components/Client/DatosPersonales/Login.jsx
@@ -1,6 +1,6 @@
 import { useState } from "react";
 import { useAuth } from "../../../Hooks/Client/useAuth";
-import styles from "../../../assets/Css/index.module.scss";
+import styles from "../../../assets/Css/Principales/Auth.module.scss";
 
 const Login = () => {
     const { login, loading, error, loginForm, handleLoginChange } = useAuth();
@@ -14,55 +14,80 @@ const Login = () => {
     };
 
     return (
-        <main className={styles.Container + " justify-content-center"}>
-            <div className="card w-75 p-3">
-                <h3 className="card-title text-center">Login</h3>
-                <form onSubmit={handleSubmit}>
-                    <div className="mb-3">
-                        <label htmlFor="identifier" className="form-label">
-                            <i className="bx bx-user"></i> Usuario o Correo
-                        </label>
-                        <input
-                            type="text"
-                            className="form-control"
-                            id="identifier"
-                            value={loginForm.identifier}
-                            onChange={handleLoginChange}
-                            required
-                        />
-                    </div>
-                    <div className="mb-3">
-                        <label htmlFor="password" className="form-label">
-                            <i className="bx bx-lock-alt"></i> Contraseña
-                        </label>
-                        <div className="input-group">
+        <main className={styles.authMain}>
+            <div className={styles.authContainer}>
+                <div className={styles.authCard}>
+                    <h1 className={styles.authTitle}>Login</h1>
+
+                    {error && (
+                        <div className={styles.errorAlert}>
+                            <i className="bx bx-exclamation-circle"></i>
+                            <span>{error}</span>
+                        </div>
+                    )}
+
+                    <form onSubmit={handleSubmit} className={styles.authForm}>
+                        <div className={styles.formGroup}>
+                            <label htmlFor="identifier" className={styles.formLabel}>
+                                <i className="bx bx-user"></i>
+                                Usuario o Correo
+                            </label>
                             <input
-                                type={showPassword ? "text" : "password"}
-                                className="form-control"
-                                id="password"
-                                value={loginForm.password}
+                                type="text"
+                                id="identifier"
+                                className={styles.formInput}
+                                placeholder="Tu usuario o correo electrónico"
+                                value={loginForm.identifier}
                                 onChange={handleLoginChange}
                                 required
                             />
-                            <button
-                                className="btn btn-outline-secondary"
-                                type="button"
-                                onClick={() => setShowPassword(!showPassword)}
-                            >
-                                <i
-                                    className={`bx ${
-                                        showPassword ? "bx-hide" : "bx-show"
-                                    }`}
-                                ></i>
-                            </button>
                         </div>
-                    </div>
-                    <div className="d-grid">
-                        <button type="submit" className="btn btn-primary" disabled={loading}>
-                            {loading ? "Cargando..." : "Login"}
+
+                        <div className={styles.formGroup}>
+                            <label htmlFor="password" className={styles.formLabel}>
+                                <i className="bx bx-lock-alt"></i>
+                                Contraseña
+                            </label>
+                            <div className={styles.inputGroup}>
+                                <input
+                                    type={showPassword ? "text" : "password"}
+                                    id="password"
+                                    className={styles.formInputPassword}
+                                    placeholder="Tu contraseña"
+                                    value={loginForm.password}
+                                    onChange={handleLoginChange}
+                                    required
+                                />
+                                <button
+                                    type="button"
+                                    className={styles.togglePasswordBtn}
+                                    onClick={() => setShowPassword(!showPassword)}
+                                    title={showPassword ? "Ocultar contraseña" : "Mostrar contraseña"}
+                                >
+                                    <i className={`bx ${showPassword ? "bx-hide" : "bx-show"}`}></i>
+                                </button>
+                            </div>
+                        </div>
+
+                        <button
+                            type="submit"
+                            className={styles.submitBtn}
+                            disabled={loading}
+                        >
+                            {loading ? (
+                                <>
+                                    <i className="bx bx-loader-alt bx-spin"></i>
+                                    Cargando...
+                                </>
+                            ) : (
+                                <>
+                                    <i className="bx bx-log-in"></i>
+                                    Iniciar Sesión
+                                </>
+                            )}
                         </button>
-                    </div>
-                </form>
+                    </form>
+                </div>
             </div>
         </main>
     );
diff --git a/Frontend/src/Components/Client/DatosPersonales/Register.jsx b/Frontend/src/Components/Client/DatosPersonales/Register.jsx
index c4b74d9..ed8b042 100644
--- a/Frontend/src/Components/Client/DatosPersonales/Register.jsx
+++ b/Frontend/src/Components/Client/DatosPersonales/Register.jsx
@@ -1,6 +1,6 @@
 import { useState } from "react";
 import { useAuth } from "../../../Hooks/Client/useAuth";
-import styles from "../../../assets/Css/index.module.scss";
+import styles from "../../../assets/Css/Principales/Auth.module.scss";
 
 const Register = () => {
     const { 
@@ -26,127 +26,163 @@ const Register = () => {
     };
 
     return (
-        <main className={styles.Container + " justify-content-center"}>
-            <div className="card w-75 p-3">
-                <h3 className="card-title text-center">Register</h3>
-                <form onSubmit={handleSubmit}>
-                    <div className="mb-3 mt-3">
-                        <label htmlFor="imgProfileInput" className="form-label d-flex justify-content-center w-100" style={{cursor: 'pointer'}}>
-                            {registerForm.fotoPreview ? (
-                                <img
-                                id="imagen"
-                                src={registerForm.fotoPreview}
-                                alt="Profile Picture"
-                                className="rounded-circle border"
-                                style={{width: '150px', height: '150px', objectFit: 'cover'}}
+        <main className={styles.authMain}>
+            <div className={styles.authContainer}>
+                <div className={styles.authCard}>
+                    <h1 className={styles.authTitle}>Registro</h1>
+
+                    {error && (
+                        <div className={styles.errorAlert}>
+                            <i className="bx bx-exclamation-circle"></i>
+                            <span>{error}</span>
+                        </div>
+                    )}
+
+                    <form onSubmit={handleSubmit} className={styles.authForm}>
+                        <div className={styles.profileUploadGroup}>
+                            <label htmlFor="imgProfileInput" className={styles.profileUploadLabel}>
+                                {registerForm.fotoPreview ? (
+                                    <img
+                                        src={registerForm.fotoPreview}
+                                        alt="Foto de perfil"
+                                        className={styles.profileImage}
+                                    />
+                                ) : (
+                                    <div className={styles.profilePlaceholder}>
+                                        <i className="bx bx-image-add"></i>
+                                        <p>Subir Foto</p>
+                                    </div>
+                                )}
+                            </label>
+                            <input
+                                type="file"
+                                id="imgProfileInput"
+                                className={styles.profileFileInput}
+                                name="imagen"
+                                accept="image/*"
+                                onChange={handleFileChange}
+                            />
+                        </div>
+
+                        <div className={styles.formGroup}>
+                            <label htmlFor="nombre" className={styles.formLabel}>
+                                <i className="bx bx-user"></i>
+                                Nombre y Apellido
+                            </label>
+                            <input
+                                type="text"
+                                id="nombre"
+                                className={styles.formInput}
+                                placeholder="Tu nombre completo"
+                                value={registerForm.nombre}
+                                onChange={handleChange}
+                                required
+                            />
+                        </div>
+
+                        <div className={styles.formGroup}>
+                            <label htmlFor="usuario" className={styles.formLabel}>
+                                <i className="bx bx-id-card"></i>
+                                Usuario Visible
+                            </label>
+                            <input
+                                type="text"
+                                id="usuario"
+                                className={styles.formInput}
+                                placeholder="Tu nombre de usuario"
+                                value={registerForm.usuario}
+                                onChange={handleChange}
+                                required
+                            />
+                        </div>
+
+                        <div className={styles.formGroup}>
+                            <label htmlFor="correo" className={styles.formLabel}>
+                                <i className="bx bx-envelope"></i>
+                                Correo Electrónico
+                            </label>
+                            <input
+                                type="email"
+                                id="correo"
+                                className={styles.formInput}
+                                placeholder="Tu correo electrónico"
+                                value={registerForm.correo}
+                                onChange={handleChange}
+                                required
+                            />
+                        </div>
+
+                        <div className={styles.formGroup}>
+                            <label htmlFor="contrasena" className={styles.formLabel}>
+                                <i className="bx bx-lock-alt"></i>
+                                Contraseña
+                            </label>
+                            <div className={styles.inputGroup}>
+                                <input
+                                    type={showPassword ? "text" : "password"}
+                                    id="contrasena"
+                                    className={styles.formInputPassword}
+                                    placeholder="Crea una contraseña segura"
+                                    value={registerForm.contrasena}
+                                    onChange={handleChange}
+                                    required
+                                />
+                                <button
+                                    type="button"
+                                    className={styles.togglePasswordBtn}
+                                    onClick={() => setShowPassword(!showPassword)}
+                                    title={showPassword ? "Ocultar contraseña" : "Mostrar contraseña"}
+                                >
+                                    <i className={`bx ${showPassword ? "bxs-hide" : "bxs-show"}`}></i>
+                                </button>
+                            </div>
+                        </div>
+
+                        <div className={styles.formGroup}>
+                            <label htmlFor="confirmPassword" className={styles.formLabel}>
+                                <i className="bx bx-lock-alt"></i>
+                                Confirmar Contraseña
+                            </label>
+                            <div className={styles.inputGroup}>
+                                <input
+                                    type={showConfirmPassword ? "text" : "password"}
+                                    id="confirmPassword"
+                                    className={styles.formInputPassword}
+                                    placeholder="Repite tu contraseña"
+                                    value={registerForm.confirmPassword}
+                                    onChange={handleChange}
+                                    required
                                 />
+                                <button
+                                    type="button"
+                                    className={styles.togglePasswordBtn}
+                                    onClick={() => setShowConfirmPassword(!showConfirmPassword)}
+                                    title={showConfirmPassword ? "Ocultar contraseña" : "Mostrar contraseña"}
+                                >
+                                    <i className={`bx ${showConfirmPassword ? "bxs-hide" : "bxs-show"}`}></i>
+                                </button>
+                            </div>
+                        </div>
+
+                        <button
+                            type="submit"
+                            className={styles.submitBtn}
+                            disabled={loading}
+                        >
+                            {loading ? (
+                                <>
+                                    <i className="bx bx-loader-alt bx-spin"></i>
+                                    Cargando...
+                                </>
                             ) : (
-                                <div className="d-flex flex-column text-center">
-                                    <i className="bx bx-image-add fs-1"></i> 
-                                    <p>Subir Foto</p>
-                                </div>
+                                <>
+                                    <i className="bx bx-user-plus"></i>
+                                    Crear Cuenta
+                                </>
                             )}
-                        </label>
-                        <input
-                            type="file"
-                            className="d-none"
-                            id="imgProfileInput"
-                            name="imagen"
-                            accept="image/*"
-                            onChange={handleFileChange}
-                        />
-                    </div>
-                    <div className="mb-3">
-                        <label htmlFor="nombre" className="form-label">
-                            <i className="bx bx-user"></i> Nombre y Apellido
-                        </label>
-                        <input
-                            type="text"
-                            className="form-control"
-                            id="nombre"
-                            value={registerForm.nombre}
-                            onChange={handleChange}
-                            required
-                        />
-                    </div>
-                    <div className="mb-3">
-                        <label htmlFor="usuario" className="form-label">
-                            <i className="bx bx-id-card"></i> Usuario Visible
-                        </label>
-                        <input
-                            type="text"
-                            className="form-control"
-                            id="usuario"
-                            value={registerForm.usuario}
-                            onChange={handleChange}
-                            required
-                        />
-                    </div>
-                    <div className="mb-3">
-                        <label htmlFor="correo" className="form-label">
-                            <i className="bx bx-envelope"></i> Correo
-                        </label>
-                        <input
-                            type="email"
-                            className="form-control"
-                            id="correo"
-                            value={registerForm.correo}
-                            onChange={handleChange}
-                            required
-                        />
-                    </div>
-                    <div className="mb-3 position-relative">
-                        <label htmlFor="contrasena" className="form-label">
-                            <i className="bx bx-lock-alt"></i> Contraseña
-                        </label>
-                        <input
-                            type={showPassword ? "text" : "password"}
-                            className="form-control"
-                            id="contrasena"
-                            value={registerForm.contrasena}
-                            onChange={handleChange}
-                            required
-                        />
-                        <i 
-                            className={`bx ${showPassword ? 'bxs-show' : 'bxs-hide'}`}
-                            style={{
-                                position: 'absolute',
-                                right: '10px',
-                                top: '38px',
-                                cursor: 'pointer'
-                            }}
-                            onClick={() => setShowPassword(!showPassword)}
-                        ></i>
-                    </div>
-                    <div className="mb-3 position-relative">
-                        <label htmlFor="confirmPassword" className="form-label">
-                            <i className="bx bx-lock-alt"></i> Confirmar Contraseña
-                        </label>
-                        <input
-                            type={showConfirmPassword ? "text" : "password"}
-                            className="form-control"
-                            id="confirmPassword"
-                            value={registerForm.confirmPassword}
-                            onChange={handleChange}
-                            required
-                        />
-                        <i 
-                            className={`bx ${showConfirmPassword ? 'bxs-show' : 'bxs-hide'}`}
-                            style={{
-                                position: 'absolute',
-                                right: '10px',
-                                top: '38px',
-                                cursor: 'pointer'
-                            }}
-                            onClick={() => setShowConfirmPassword(!showConfirmPassword)}
-                        ></i>
-                    </div>
-                    <div className="d-grid">
-                        <button type="submit" className="btn btn-primary" disabled={loading}>
-                            {loading ? "Cargando..." : "Register"}
                         </button>
-                    </div>
-                </form>
+                    </form>
+                </div>
             </div>
         </main>
     );
diff --git a/Frontend/src/Components/Client/Fijos/Footer.jsx b/Frontend/src/Components/Client/Fijos/Footer.jsx
index aadee47..46ecf7b 100644
--- a/Frontend/src/Components/Client/Fijos/Footer.jsx
+++ b/Frontend/src/Components/Client/Fijos/Footer.jsx
@@ -2,20 +2,21 @@ import { useAuthContext } from "../../../Pages/Context/AuthContext";
 import { Hourglass } from 'ldrs/react'
 import { CalendarClock } from 'lucide-react';
 import useContador from "../../../Hooks/Client/Contador";
-import styles from "../../../assets/Css/index.module.scss";
+import styles from "../../../assets/Css/Principales/index.module.scss";
+import Color from "../../../Utils/Components/Color";
 
 function Footer() {
     const { isAuthenticated , user } = useAuthContext();
     const contador = useContador(user?.exp);
     
     return (
-        <footer className={styles.Footer}>
-            <div className={styles["Footer-container"]}>
-                <p>Cigarrería JJ ©</p>
+        <footer className={styles.footer}>
+            <div className={styles["footer-container"]}>
+                <p className={styles["footer-brand"]}>Cigarrería JJ ©</p>
 
                 {isAuthenticated ? (
-                    <div className={styles.FooterContador}>
-                        <div className={styles.FooterTiempo}>
+                    <div className={styles["footer-contador"]}>
+                        <div className={styles["footer-tiempo"]}>
                             <Hourglass size={32} speed={4} color="var(--rojo-400)" />
                             <span>
                                 Tiempo restante <br />
@@ -23,7 +24,7 @@ function Footer() {
                             </span>
                             <Hourglass size={32} speed={4} color="var(--rojo-400)" />
                         </div>
-                        <div className={styles.FooterTiempo}>
+                        <div className={styles["footer-tiempo"]}>
                             <CalendarClock size={32} color="var(--rojo-400)" />
                             <span>
                                 Cierre de sesión <br />
@@ -32,9 +33,9 @@ function Footer() {
                             <CalendarClock size={32} color="var(--rojo-400)" />
                         </div>
                     </div>
-                ): null}
+                ) : null}
 
-                <p>Desarrollado por: Fabian Amaya</p>
+                <Color />
             </div>
         </footer>
     );
diff --git a/Frontend/src/Components/Client/Fijos/Header.jsx b/Frontend/src/Components/Client/Fijos/Header.jsx
index daa2bf7..718582f 100644
--- a/Frontend/src/Components/Client/Fijos/Header.jsx
+++ b/Frontend/src/Components/Client/Fijos/Header.jsx
@@ -1,5 +1,5 @@
 import navConfig from "../../../Utils/RutasNav.json";
-import styles from "../../../assets/Css/index.module.scss";
+import styles from "../../../assets/Css/Principales/index.module.scss";
 import { Hourglass } from 'ldrs/react'
 import { useAuthContext } from "../../../Pages/Context/AuthContext";
 import { NavLink } from "react-router-dom";
@@ -28,44 +28,45 @@ function Header() {
     });
 
     return (
-        <header className={styles.Header}>
-            <div className={styles["Header-container"]}>
+        <header className={styles.header}>
+            <div className={styles["header-container"]}>
                 {/* Logo SOLO en escritorio */}
-                <NavLink to="/" className={`${styles["Header-logo"]} ${styles["only-desktop"]}`}>
+                <NavLink to="/" className={`${styles["header-logo"]} ${styles["only-desktop"]}`}>
                     <img src={logo} alt="Logo" />
                 </NavLink>
 
                 {/* Título siempre visible */}
-                <h1 className={styles["Header-title"]}>Cigarrería JJ</h1>
+                <h1 className={styles["header-title"]}>Cigarrería JJ</h1>
 
                 {/* Nav SOLO en escritorio */}
-                <nav className={`${styles["Header-nav"]} ${styles["only-desktop"]}`}>
-                    <ul className={styles["Header-list"]}>
+                <nav className={`${styles["header-nav"]} ${styles["only-desktop"]}`}>
+                    <ul className={styles["header-list"]}>
                         {navBar.map((item, index) => (
-                            <li key={index} className={styles["Header-item"]}>
-                                <NavLink to={item.ruta} className={styles["Header-link"]}>
+                            <li key={index} className={styles["header-item"]}>
+                                <NavLink to={item.ruta} className={styles["header-link"]}>
                                     <i className={item.icon}></i>
-                                    {item.texto}
+                                    <span>{item.texto}</span>
                                 </NavLink>
                             </li>
                         ))}
                     </ul>
-                    {isAuthenticated ?
-                    (<NavLink to="/Personal" className={`${styles["HeaderUser"]} ${styles["only-desktop"]}`}>
-                        {loading ? (<Hourglass size={80} color="var(--texto)" />) : 
-                        imagen ? (
-                            <img src={imagen} alt="Imagen de Perfil" />
-                        ) : (
-                            <i className="bx bx-user "></i>
-                        )
-                        }
-                    </NavLink>
-                    ): null}
+
+                    {isAuthenticated ? (
+                        <NavLink to="/Personal" className={`${styles["header-user"]} ${styles["only-desktop"]}`}>
+                            {loading ? (
+                                <Hourglass size={80} color="var(--texto)" />
+                            ) : imagen ? (
+                                <img src={imagen} alt="Imagen de Perfil" />
+                            ) : (
+                                <i className="bx bx-user"></i>
+                            )}
+                        </NavLink>
+                    ) : null}
                 </nav>
 
                 {/* Botón Hamburguesa SOLO en móvil */}
                 <button
-                    className={`${styles["Header-burger"]} ${styles["only-mobile"]}`}
+                    className={`${styles["header-burger"]} ${styles["only-mobile"]}`}
                     onClick={() => setMenuOpen(!menuOpen)}
                 >
                     <i className={menuOpen ? "bx bx-x" : "bx bx-menu"}></i>
@@ -73,44 +74,42 @@ function Header() {
             </div>
 
             {/* Menú lateral con logo + opciones */}
-            <nav
-                className={`${styles["Header-overlay"]} ${
-                    menuOpen ? styles["is-open"] : ""
-                }`}
-            >
-                <div className={styles["Overlay-content"]}>
-                    <NavLink to="/" className={styles["Overlay-logo"]}>
-                        <img src= {logo} alt="Logo" />
+            <nav className={`${styles["header-overlay"]} ${menuOpen ? styles["is-open"] : ""}`}>
+                <div className={styles["overlay-content"]}>
+                    <NavLink to="/" className={styles["overlay-logo"]}>
+                        <img src={logo} alt="Logo" />
                     </NavLink>
-                    <ul className={styles["Header-list"]}>
+
+                    <ul className={styles["header-list"]}>
                         {navBar.map((item, index) => (
-                            <li key={index} className={styles["Header-item"]}>
+                            <li key={index} className={styles["header-item"]}>
                                 <NavLink
                                     to={item.ruta}
-                                    className={styles["Header-link"]}
+                                    className={styles["header-link"]}
                                     onClick={() => setMenuOpen(false)}
                                 >
                                     <i className={item.icon}></i>
-                                    {item.texto}
+                                    <span>{item.texto}</span>
                                 </NavLink>
                             </li>
                         ))}
                     </ul>
+
                     {isAuthenticated ? (
-                        <NavLink 
-                            to="/Personal" 
-                            className={`${styles["HeaderUser"]} ${styles["Overlay-logo"]}`}
+                        <NavLink
+                            to="/Personal"
+                            className={`${styles["header-user"]} ${styles["overlay-logo"]}`}
                             onClick={() => setMenuOpen(false)}
                         >
-                            {loading ? (<Hourglass size={80} color="var(--texto)" />) : 
-                            imagen ? (
+                            {loading ? (
+                                <Hourglass size={80} color="var(--texto)" />
+                            ) : imagen ? (
                                 <img src={imagen} alt="Imagen de Perfil" />
                             ) : (
-                                <i className="bx bx-user "></i>
-                            )
-                            }
+                                <i className="bx bx-user"></i>
+                            )}
                         </NavLink>
-                    ): null}
+                    ) : null}
                 </div>
             </nav>
         </header>
diff --git a/Frontend/src/Components/Client/Inicio.jsx b/Frontend/src/Components/Client/Inicio.jsx
index 86f8c6c..cd974bd 100644
--- a/Frontend/src/Components/Client/Inicio.jsx
+++ b/Frontend/src/Components/Client/Inicio.jsx
@@ -2,8 +2,7 @@ import { useAuthContext } from "../../Pages/Context/AuthContext";
 import { RectangleEllipsis, KeySquare } from "lucide-react";
 import { NavLink } from "react-router-dom";
 import navConfig from "../../Utils/RutasNav.json";
-import styles from "../../assets/Css/index.module.scss";
-import stylesInicio from "../../assets/Css/Inicio.module.scss";
+import stylesInicio from "../../assets/Css/Principales/Inicio.module.scss";
 import { TareaContainer } from "../Client/Tareas/ComponentsTareas";
 
 export default function Inicio() {
@@ -23,12 +22,12 @@ export default function Inicio() {
     });
 
     return (
-        <main className= {`${styles.Container} ${stylesInicio.ItemContainer}`}>
+        <main className={`${stylesInicio.inicioMain}`}>
             {isAuthenticated ? (
-                <div className={stylesInicio.ItemContainer}>
-                    <h2>¿Qué deseas hacer?</h2>
+                <section className={stylesInicio.authenticatedSection}>
+                    <h1 className={stylesInicio.pageTitle}>¿Qué deseas hacer?</h1>
                     <TareaContainer items={navBar} />
-                </div>
+                </section>
             ) : (
                 <InformacionSinLogin />
             )}
diff --git a/Frontend/src/Components/Client/Personal/Personal.jsx b/Frontend/src/Components/Client/Personal/Personal.jsx
index f850d1d..9b501e4 100644
--- a/Frontend/src/Components/Client/Personal/Personal.jsx
+++ b/Frontend/src/Components/Client/Personal/Personal.jsx
@@ -2,9 +2,8 @@ import { useState, useEffect } from "react";
 import { motion } from "framer-motion";
 import { useAuthContext } from "../../../Pages/Context/AuthContext";
 import useInfoPersonal from "../../../Hooks/Client/useInfoPersonal";
-import styles from "../../../assets/Css/index.module.scss";
 import stylesPersonal from "../../../assets/Css/PerfilPersonal.module.scss";
-import { Loading, Error } from "../../../Utils/Cargando";
+import { Loading, Error } from "../../../Utils/Components/Cargando";
 
 export default function Personal() {
     const { user } = useAuthContext();
@@ -83,34 +82,34 @@ export default function Personal() {
 
     return (
         <motion.main
-            className={stylesPersonal.PerfilContainer}
+            className={stylesPersonal["perfil-container"]}
             initial={{ opacity: 0, y: 20 }}
             animate={{ opacity: 1, y: 0 }}
             transition={{ duration: 0.6 }}
         >
             <motion.div
-                className={`${styles.Container}`}
+                className={stylesPersonal.container}
                 initial={{ scale: 0.9, opacity: 0 }}
                 animate={{ scale: 1, opacity: 1 }}
                 transition={{ delay: 0.3 }}
             >
-                <h2 className={stylesPersonal.Title}>Mi Perfil</h2>
+                <h2 className={stylesPersonal.title}>Mi Perfil</h2>
 
                 {/* Imagen de perfil */}
-                <section className={stylesPersonal.ImagenSection + " card"}>
+                <section className={`${stylesPersonal["imagen-section"]} card`}>
                     {perfil?.imagen === null ? (
                         <i className="bx bx-user"></i>
                     ) : (
                         <motion.img
                             src={perfil?.imagen || "/default-user.png"}
                             alt="perfil"
-                            className={stylesPersonal.ImagenPerfil}
+                            className={stylesPersonal["imagen-perfil"]}
                             whileHover={{ scale: 1.05 }}
                         />
                     )}
                     <label
                         htmlFor="imagen"
-                        className={stylesPersonal.BtnSubir + " btn btn-primary"}
+                        className={`${stylesPersonal["btn-subir"]} btn btn-primary`}
                     >
                         Cambiar imagen
                         <input
@@ -124,9 +123,10 @@ export default function Personal() {
                 </section>
 
                 {/* Datos personales */}
-                <section className={stylesPersonal.Datos + " card"}>
+                <section className={`${stylesPersonal.datos} card`}>
                     <h3>Información Personal</h3>
-                    <div className={stylesPersonal.Grupo}>
+
+                    <div className={stylesPersonal.grupo}>
                         <label>Nombre</label>
                         <input
                             name="nombre"
@@ -135,7 +135,8 @@ export default function Personal() {
                             placeholder="Tu nombre"
                         />
                     </div>
-                    <div className={stylesPersonal.Grupo}>
+
+                    <div className={stylesPersonal.grupo}>
                         <label>Correo</label>
                         <input
                             name="correo"
@@ -144,7 +145,8 @@ export default function Personal() {
                             placeholder="correo@ejemplo.com"
                         />
                     </div>
-                    <div className={stylesPersonal.Grupo}>
+
+                    <div className={stylesPersonal.grupo}>
                         <label>Usuario</label>
                         <input
                             name="usuario"
@@ -153,6 +155,7 @@ export default function Personal() {
                             placeholder="Usuario"
                         />
                     </div>
+
                     <button
                         className="btn btn-primary"
                         onClick={handleGuardar}
@@ -164,7 +167,7 @@ export default function Personal() {
 
                 {/* Cambio de contraseña */}
                 <motion.section
-                    className={stylesPersonal.PasswordSection + " card"}
+                    className={`${stylesPersonal["password-section"]} card`}
                     initial={{ opacity: 0 }}
                     whileInView={{ opacity: 1 }}
                     transition={{ delay: 0.5 }}
@@ -172,9 +175,9 @@ export default function Personal() {
                     <h3>Cambiar Contraseña</h3>
 
                     {/* Contraseña actual */}
-                    <div className={stylesPersonal.Grupo}>
+                    <div className={stylesPersonal.grupo}>
                         <label>Contraseña actual</label>
-                        <div className={stylesPersonal.InputPassword}>
+                        <div className={stylesPersonal["input-password"]}>
                             <input
                                 type={showPassword.actual ? "text" : "password"}
                                 value={passwords.actual}
@@ -187,18 +190,16 @@ export default function Personal() {
                                 placeholder="Contraseña actual"
                             />
                             <i
-                                className={`bx ${
-                                    showPassword.actual ? "bx-show" : "bx-hide"
-                                }`}
+                                className={`bx ${showPassword.actual ? "bx-show" : "bx-hide"}`}
                                 onClick={() => toggleVisibility("actual")}
                             ></i>
                         </div>
                     </div>
 
                     {/* Nueva contraseña */}
-                    <div className={stylesPersonal.Grupo}>
+                    <div className={stylesPersonal.grupo}>
                         <label>Nueva contraseña</label>
-                        <div className={stylesPersonal.InputPassword}>
+                        <div className={stylesPersonal["input-password"]}>
                             <input
                                 type={showPassword.nueva ? "text" : "password"}
                                 value={passwords.nueva}
@@ -211,18 +212,16 @@ export default function Personal() {
                                 placeholder="Nueva contraseña"
                             />
                             <i
-                                className={`bx ${
-                                    showPassword.nueva ? "bx-show" : "bx-hide"
-                                }`}
+                                className={`bx ${showPassword.nueva ? "bx-show" : "bx-hide"}`}
                                 onClick={() => toggleVisibility("nueva")}
                             ></i>
                         </div>
                     </div>
 
                     {/* Confirmar contraseña */}
-                    <div className={stylesPersonal.Grupo}>
+                    <div className={stylesPersonal.grupo}>
                         <label>Confirmar contraseña</label>
-                        <div className={stylesPersonal.InputPassword}>
+                        <div className={stylesPersonal["input-password"]}>
                             <input
                                 type={showPassword.confirmar ? "text" : "password"}
                                 value={passwords.confirmar}
@@ -235,9 +234,7 @@ export default function Personal() {
                                 placeholder="Confirmar nueva contraseña"
                             />
                             <i
-                                className={`bx ${
-                                    showPassword.confirmar ? "bx-show" : "bx-hide"
-                                }`}
+                                className={`bx ${showPassword.confirmar ? "bx-show" : "bx-hide"}`}
                                 onClick={() => toggleVisibility("confirmar")}
                             ></i>
                         </div>
diff --git a/Frontend/src/Components/Client/Tareas/Catalogo.jsx b/Frontend/src/Components/Client/Tareas/Catalogo.jsx
index 1c8021f..a74e16f 100644
--- a/Frontend/src/Components/Client/Tareas/Catalogo.jsx
+++ b/Frontend/src/Components/Client/Tareas/Catalogo.jsx
@@ -1,13 +1,11 @@
-import styles from "../../../assets/Css/index.module.scss";
-import stylesFiltro from "../../../assets/Css/deuda.module.scss";
 import stylesCatalogo from "../../../assets/Css/Catalogo.module.scss";
-import stylesBoton from "../../../assets/Css/crud.module.scss";
-import { Loading, Error } from "../../../Utils/Cargando";
+import { Loading, Error } from "../../../Utils/Components/Cargando";
 import { useCatalogo, useProductosSinFiltro } from "../../../Hooks/Client/useCatalogo";
 import { useEffect, useMemo, useState } from "react";
 import Filtro from "../../Vendedor/GestionInventario/Filtro";
 import Paginacion from "../../Vendedor/GestionInventario/Paginacion";
 import { useAuthContext } from "../../../Pages/Context/AuthContext";
+import ImagePreview from "../../../Utils/Components/ImagePreview";
 
 export default function Catalogo() {
     const { user } = useAuthContext();
@@ -106,7 +104,7 @@ export default function Catalogo() {
     }, [ProductosNomales, ProductosSinFiltro]);
 
     return (
-        <main className={`${styles.Container} ${stylesFiltro.Container}`}>
+        <main className={stylesCatalogo.container}>
 
             <h2>Catálogo</h2>
 
@@ -167,10 +165,14 @@ export function CartProducto({ Producto }) {
     return (
         <div key={id_producto} className={stylesCatalogo.cartProductoItem}>
             <div className={stylesCatalogo.ImagenProducto}>
-                {imagen ? <img src={imagen} alt={nombre} loading="lazy"/> : <i className="bx bx-image"></i>}
+                <ImagePreview
+                    src={imagen}
+                    alt={nombre}
+                    fallback={<i className="bx bx-image"></i>}
+                />
             </div>
 
-            {!activo && 
+            {!activo &&
                 <p className={stylesCatalogo.ProductoInactivo}>
                     Producto inactivo
                 </p>
@@ -208,14 +210,14 @@ export function CartProducto({ Producto }) {
     );
 }
 
-export function FiltroActivo({estado, setEstado}){
+export function FiltroActivo({ estado, setEstado }) {
     const { user } = useAuthContext();
 
-    return(
+    return (
         <>
             {(user?.rol === 1 || user?.rol === 2) && (
-                <label className={`${stylesBoton.SwitchLabel} ${stylesCatalogo.FiltroActivo}`}>
-                    <span className={stylesBoton.SwitchText}>
+                <label className={`${stylesCatalogo.switchLabel} ${stylesCatalogo.FiltroActivo}`}>
+                    <span className={stylesCatalogo.switchText}>
                         {estado ? "Todos los Productos" : "Productos Acual"}
                     </span>
                     <input
@@ -224,7 +226,7 @@ export function FiltroActivo({estado, setEstado}){
                         checked={estado}
                         onChange={(e) => setEstado(e.target.checked)}
                     />
-                    <span className={stylesBoton.Switch}></span>
+                    <span className={stylesCatalogo.switch}></span>
                 </label>
             )}
         </>
@@ -233,4 +235,4 @@ export function FiltroActivo({estado, setEstado}){
 
 function formatPrice(price) {
     return `$ ${new Intl.NumberFormat("es-ES", { useGrouping: true }).format(price)}`;
-}
+}
\ No newline at end of file
diff --git a/Frontend/src/Components/Client/Tareas/ComponentsTareas.jsx b/Frontend/src/Components/Client/Tareas/ComponentsTareas.jsx
index 9e1ad16..87d721d 100644
--- a/Frontend/src/Components/Client/Tareas/ComponentsTareas.jsx
+++ b/Frontend/src/Components/Client/Tareas/ComponentsTareas.jsx
@@ -1,31 +1,35 @@
 import { NavLink } from "react-router-dom";
-import stylesInicio from "../../../assets/Css/Inicio.module.scss";
+import stylesInicio from "../../../assets/Css/Principales/Inicio.module.scss";
 
 export function TareaItem({ icon, nombre, descripcion, ruta }) {
     return (
-        <div className={stylesInicio.Tarea}>
-            <NavLink to={ruta}>
-                <i className={icon}></i>
-                {nombre}
+        <article className={stylesInicio.tareaCard}>
+            <NavLink to={ruta} className={stylesInicio.tareaLink}>
+                <span className={stylesInicio.tareaIconWrapper}>
+                    <i className={icon}></i>
+                </span>
+                <h3 className={stylesInicio.tareaTitle}>{nombre}</h3>
             </NavLink>
-            <p>{descripcion}</p>
-        </div>
+            <p className={stylesInicio.tareaDescription}>{descripcion}</p>
+        </article>
     );
 }
 
 export function TareaContainer({ titulo, items }) {
     return (
-        <div className={stylesInicio.TareaContainer}>
-            {titulo && <h3>{titulo}</h3>}
-            {items.map((item, index) => (
-                <TareaItem
-                    key={index}
-                    icon={item.icono || item.icon}
-                    nombre={item.nombre || item.texto}
-                    descripcion={item.descripcion}
-                    ruta={item.ruta}
-                />
-            ))}
-        </div>
+        <section className={stylesInicio.tareasSection}>
+            {titulo && <h2 className={stylesInicio.sectionTitle}>{titulo}</h2>}
+            <div className={stylesInicio.tareasGrid}>
+                {items.map((item, index) => (
+                    <TareaItem
+                        key={index}
+                        icon={item.icono || item.icon}
+                        nombre={item.nombre || item.texto}
+                        descripcion={item.descripcion}
+                        ruta={item.ruta}
+                    />
+                ))}
+            </div>
+        </section>
     );
-}
+}
\ No newline at end of file
diff --git a/Frontend/src/Components/Client/Tareas/Deudas.jsx b/Frontend/src/Components/Client/Tareas/Deudas.jsx
index 9c070cb..1948fa5 100644
--- a/Frontend/src/Components/Client/Tareas/Deudas.jsx
+++ b/Frontend/src/Components/Client/Tareas/Deudas.jsx
@@ -1,10 +1,9 @@
-import styles from "../../../assets/Css/index.module.scss";
 import stylesDeuda from "../../../assets/Css/deuda.module.scss";
 import { HandCoins, Coins  } from 'lucide-react';
 import { useAuthContext } from "../../../Pages/Context/AuthContext";
 import { NavLink } from "react-router-dom";
 import useDeudasUsuario from "../../../Hooks/Vendedor/Deudas/useDeudasUsuario";
-import { Loading, Error  } from "../../../Utils/Cargando";
+import { Loading, Error  } from "../../../Utils/Components/Cargando";
 
 export default function Deudas() {
     const { user } = useAuthContext();
@@ -17,7 +16,7 @@ export default function Deudas() {
     ]
 
     return (
-        <main className={`${styles.Container} ${stylesDeuda.Container}`} >
+        <main className={`${stylesDeuda.Container}`} >
             
             <h2>Historial de deudas</h2>
             {user?.rol === 1 || user?.rol === 2 ?
diff --git a/Frontend/src/Components/Vendedor/CierreDia/Bolsillos.jsx b/Frontend/src/Components/Vendedor/CierreDia/Bolsillos.jsx
index 4451396..59803c0 100644
--- a/Frontend/src/Components/Vendedor/CierreDia/Bolsillos.jsx
+++ b/Frontend/src/Components/Vendedor/CierreDia/Bolsillos.jsx
@@ -5,7 +5,7 @@ import HeroCierre from "./Cierre/HeroCierre";
 import ResumenGeneral from "./Cierre/ResumenGeneral";
 import ProgressResumen from "./Cierre/ProgressResumen";
 import ListaBolsillos from "./Bolsillos/ListaBolsillos";
-import { Loading, Error } from "../../../Utils/Cargando";
+import { Loading, Error } from "../../../Utils/Components/Cargando";
 import { detactarEstadoCierre } from "./cierreDia.utils";
 
 export default function Bolsillos() {
diff --git a/Frontend/src/Components/Vendedor/CierreDia/CierreDia.jsx b/Frontend/src/Components/Vendedor/CierreDia/CierreDia.jsx
index dfa7703..cfe6ea4 100644
--- a/Frontend/src/Components/Vendedor/CierreDia/CierreDia.jsx
+++ b/Frontend/src/Components/Vendedor/CierreDia/CierreDia.jsx
@@ -8,7 +8,7 @@ import RangoFechas from "./Cierre/RangoFechas";
 import ResumenCajas from "./Cierre/ResumenCajas";
 import ResumenVentas from "./Cierre/ResumenVentas";
 import ListaBolsillos from "./Bolsillos/ListaBolsillos";
-import { Loading, Error } from "../../../Utils/Cargando";
+import { Loading, Error } from "../../../Utils/Components/Cargando";
 import { detactarEstadoCierre } from "./cierreDia.utils";
 
 export default function CierreDia() {
diff --git a/Frontend/src/Components/Vendedor/Deudas/CrearDeuda.jsx b/Frontend/src/Components/Vendedor/Deudas/CrearDeuda.jsx
index 5fdd7a6..166e547 100644
--- a/Frontend/src/Components/Vendedor/Deudas/CrearDeuda.jsx
+++ b/Frontend/src/Components/Vendedor/Deudas/CrearDeuda.jsx
@@ -1,4 +1,3 @@
-import styles from "../../../assets/Css/index.module.scss";
 import stylesDeuda from "../../../assets/Css/deuda.module.scss";
 import { useNavigate } from "react-router-dom";
 import { useState } from "react";
@@ -53,7 +52,7 @@ export default function CrearDeuda() {
     };
 
     return (
-        <main className={`${styles.Container} ${stylesDeuda.Container}`}>
+        <main className={`${stylesDeuda.Container}`}>
             
 
             <h2>Crear Deuda</h2>
diff --git a/Frontend/src/Components/Vendedor/Deudas/DetallesDeuda.jsx b/Frontend/src/Components/Vendedor/Deudas/DetallesDeuda.jsx
index ad3f68c..0fd308c 100644
--- a/Frontend/src/Components/Vendedor/Deudas/DetallesDeuda.jsx
+++ b/Frontend/src/Components/Vendedor/Deudas/DetallesDeuda.jsx
@@ -1,7 +1,6 @@
-import styles from "../../../assets/Css/index.module.scss";
 import stylesDeuda from "../../../assets/Css/deuda.module.scss";
 import useDetallesDeudas from "../../../Hooks/Vendedor/Deudas/useDetallesDeudas";
-import { Loading, Error } from "../../../Utils/Cargando";
+import { Loading, Error } from "../../../Utils/Components/Cargando";
 import { useParams } from "react-router-dom";
 
 export default function DetallesDeuda() {
@@ -13,7 +12,7 @@ export default function DetallesDeuda() {
                         : "Ocurrió un error al cargar los detalles"
 
     return (
-        <main className={`${styles.Container} ${stylesDeuda.Container}`}>
+        <main className={`${stylesDeuda.Container}`}>
 
             <h2>Detalles de deuda</h2>
 
diff --git a/Frontend/src/Components/Vendedor/Deudas/ListaDeudas.jsx b/Frontend/src/Components/Vendedor/Deudas/ListaDeudas.jsx
index 4b183ec..e66565b 100644
--- a/Frontend/src/Components/Vendedor/Deudas/ListaDeudas.jsx
+++ b/Frontend/src/Components/Vendedor/Deudas/ListaDeudas.jsx
@@ -1,4 +1,3 @@
-import styles from "../../../assets/Css/index.module.scss";
 import stylesDeuda from "../../../assets/Css/deuda.module.scss";
 import { Hourglass } from "ldrs/react";
 import { useNavigate, NavLink } from "react-router-dom";
@@ -31,7 +30,7 @@ export default function ListaDeudas() {
         });
 
     return (
-        <main className={`${styles.Container} ${stylesDeuda.Container}`}>
+        <main className={`${stylesDeuda.Container}`}>
 
             <h2>Lista de deudas</h2>
 
diff --git a/Frontend/src/Components/Vendedor/Estadisticas.jsx b/Frontend/src/Components/Vendedor/Estadisticas.jsx
index 5e9671a..51ebd0b 100644
--- a/Frontend/src/Components/Vendedor/Estadisticas.jsx
+++ b/Frontend/src/Components/Vendedor/Estadisticas.jsx
@@ -1,5 +1,4 @@
 import { useEstadisticas } from "../../Hooks/Vendedor/useEstadisticas";
-import styles from "../../assets/Css/index.module.scss";
 import stylesEstadisticas from "../../assets/Css/Estadisticas.module.scss";
 import { Hourglass } from "ldrs/react";
 import { Bar, PolarArea } from "react-chartjs-2";
@@ -72,7 +71,7 @@ const Estadisticas = ({ limit = 15 }) => {
     };
 
     return (
-        <main className={`${styles.Container} ${stylesEstadisticas.estadisticas}`}>
+        <main className={`${stylesEstadisticas.estadisticas}`}>
             <h2>📊 Estadísticas</h2>
 
             {/* Gráfico más vendidos */}
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/CRUDsProductos/CrudProductos.jsx b/Frontend/src/Components/Vendedor/GestionInventario/CRUDsProductos/CrudProductos.jsx
index 787f10f..779dd5c 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/CRUDsProductos/CrudProductos.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/CRUDsProductos/CrudProductos.jsx
@@ -113,77 +113,94 @@ export default function ProductoForm({ open, onClose, onSubmit, modoEdicion, pro
                 <h3>{modoEdicion ? "Editar Producto" : "Nuevo Producto"}</h3>
 
                 <div className={`${styles.ModalContent} ${styles.ScrollableContent}`}>
-                    <label>Código de Barras
-                        <input name="codigo_barras" value={form.codigo_barras} onChange={handleChange} />
+                    <label>
+                        Código de Barras
+                        <input
+                            type="text"
+                            name="codigo_barras"
+                            value={form.codigo_barras}
+                            onChange={handleChange}
+                            placeholder="Ej: 770200100001"
+                        />
                     </label>
 
-                    <label>Nombre
-                        <input name="nombre" value={form.nombre} onChange={handleChange} />
+                    <label>
+                        Nombre
+                        <input
+                            type="text"
+                            name="nombre"
+                            value={form.nombre}
+                            onChange={handleChange}
+                            placeholder="Nombre del producto"
+                            required
+                        />
                     </label>
 
-                    <label>Marca</label>
-                    {isLoadingMarcas ? (
-                        <p>Cargando marcas...</p>
-                    ) : (
-                        <Select
-                            options={optionsMarcas}
-                            value={marcaSeleccionada}
-                            onChange={handleMarcaChange}
-                            placeholder="Buscar o seleccionar marca..."
-                            isClearable
-                            className={styles.SelectMarca}
-                            styles={{
-                                control: (base, state) => ({
-                                    ...base,
-                                    backgroundColor: "var(--input-bg)",
-                                    borderColor: state.isFocused
-                                        ? "var(--input-focus-border)"
-                                        : "var(--input-border)",
-                                    color: "var(--input-text)",
-                                    borderRadius: "var(--radius-sm)",
-                                    padding: "0.2rem 0.3rem",
-                                    boxShadow: "none",
-                                    "&:hover": {
-                                        borderColor: "var(--input-focus-border)",
-                                    },
-                                }),
-                                menu: (base) => ({
-                                    ...base,
-                                    backgroundColor: "var(--input-bg)",
-                                    color: "var(--input-text)",
-                                    borderRadius: "var(--radius-sm)",
-                                    border: "1px solid var(--input-border)",
-                                    zIndex: 100,
-                                }),
-                                option: (base, state) => ({
-                                    ...base,
-                                    backgroundColor: state.isFocused
-                                        ? "var(--input-focus-border)"
-                                        : "var(--input-bg)",
-                                    color: "var(--input-text)",
-                                    cursor: "pointer",
-                                }),
-                                singleValue: (base) => ({
-                                    ...base,
-                                    color: "var(--input-text)",
-                                }),
-                                placeholder: (base) => ({
-                                    ...base,
-                                    color: "var(--input-placeholder)",
-                                }),
-                                input: (base) => ({
-                                    ...base,
-                                    color: "var(--input-text)",
-                                }),
-                            }}
-                        />
-                    )}
+                    <label>
+                        Marca
+                        {isLoadingMarcas ? (
+                            <div style={{ padding: "10px", color: "var(--texto-sec)" }}>
+                                Cargando marcas...
+                            </div>
+                        ) : (
+                            <Select
+                                options={optionsMarcas}
+                                value={marcaSeleccionada}
+                                onChange={handleMarcaChange}
+                                placeholder="Buscar o seleccionar marca..."
+                                isClearable
+                                className={styles.SelectMarca}
+                                styles={{
+                                    control: (base, state) => ({
+                                        ...base,
+                                        backgroundColor: "var(--input-bg)",
+                                        borderColor: state.isFocused ? "var(--rojo-500)" : "var(--input-border)",
+                                        color: "var(--input-text)",
+                                        borderRadius: "var(--radius-md)",
+                                        padding: "0.2rem",
+                                        boxShadow: state.isFocused ? "var(--focus-ring)" : "none",
+                                        "&:hover": {
+                                            borderColor: "var(--rojo-700)",
+                                        },
+                                    }),
+                                    menu: (base) => ({
+                                        ...base,
+                                        backgroundColor: "var(--input-bg)",
+                                        color: "var(--input-text)",
+                                        borderRadius: "var(--radius-md)",
+                                        border: "1px solid var(--input-border)",
+                                        zIndex: 100,
+                                    }),
+                                    option: (base, state) => ({
+                                        ...base,
+                                        backgroundColor: state.isFocused ? "rgba(198, 40, 40, 0.15)" : "var(--input-bg)",
+                                        color: "var(--input-text)",
+                                        cursor: "pointer",
+                                    }),
+                                    singleValue: (base) => ({
+                                        ...base,
+                                        color: "var(--input-text)",
+                                    }),
+                                    placeholder: (base) => ({
+                                        ...base,
+                                        color: "var(--placeholder)",
+                                    }),
+                                    input: (base) => ({
+                                        ...base,
+                                        color: "var(--input-text)",
+                                    }),
+                                }}
+                            />
+                        )}
+                    </label>
 
-                    <label>Descripción
+                    <label>
+                        Descripción
                         <textarea
                             name="descripcion"
                             value={form.descripcion}
                             onChange={handleChange}
+                            placeholder="Describe el producto..."
                             rows="3"
                         />
                     </label>
@@ -191,65 +208,73 @@ export default function ProductoForm({ open, onClose, onSubmit, modoEdicion, pro
                     <div className={styles.ImagenContainer}>
                         <label htmlFor="imgProduc">
                             Imagen del producto
-                            {preview ? (
-                                <div className={styles.ImagenPreview}>
-                                    <img src={preview} alt="Vista previa" loading="lazy"/>
-                                </div>
-                            ) : (
-                                <div className={styles.ImagenPreview}>
+                            <div className={styles.ImagenPreview} onClick={() => document.getElementById("imgProduc").click()}>
+                                {preview ? (
+                                    <img src={preview} alt="Vista previa" loading="lazy" />
+                                ) : (
                                     <i className="bx bx-image-add"></i>
-                                </div>
-                            )}
+                                )}
+                            </div>
                         </label>
                         <input
                             id="imgProduc"
                             type="file"
                             accept="image/*"
                             onChange={handleFileChange}
-                            className="d-none"
+                            style={{ display: "none" }}
                         />
                     </div>
 
                     <div className={styles.FilaInputs}>
-                        <label>Precio Compra
+                        <label>
+                            Precio Compra
                             <input
                                 type="number"
                                 name="precio_compra"
                                 value={form.precio_compra}
                                 onChange={handleChange}
+                                placeholder="0.00"
+                                step="0.01"
                             />
                         </label>
 
-                        <label>Precio Venta
+                        <label>
+                            Precio Venta
                             <input
                                 type="number"
                                 name="precio_venta"
                                 value={form.precio_venta}
                                 onChange={handleChange}
+                                placeholder="0.00"
+                                step="0.01"
                             />
                         </label>
                     </div>
 
                     <div className={styles.FilaInputs}>
-                        <label>Stock Mínimo
+                        <label>
+                            Stock Mínimo
                             <input
                                 type="number"
                                 name="stock_minimo"
                                 value={form.stock_minimo}
                                 onChange={handleChange}
+                                placeholder="0"
                             />
                         </label>
-                        
-                        <label>Unidad de Medida
+
+                        <label>
+                            Unidad de Medida
                             <input
+                                type="text"
                                 name="unidad_medida"
                                 value={form.unidad_medida}
                                 onChange={handleChange}
+                                placeholder="Ej: kg, L, ud"
                             />
                         </label>
                     </div>
 
-
                     <label className={styles.SwitchLabel}>
                         <input
                             type="checkbox"
@@ -259,14 +284,18 @@ export default function ProductoForm({ open, onClose, onSubmit, modoEdicion, pro
                         />
                         <span className={styles.Switch}></span>
                         <span className={styles.SwitchText}>
-                            {form.activo ? "Producto Activo" : "Producto Desactivado"}
+                            Producto {form.activo ? "Activo" : "Desactivado"}
                         </span>
                     </label>
                 </div>
 
                 <div className={styles.ModalActions}>
-                    <button className="btn-secondary" onClick={onClose}>Cancelar</button>
-                    <button className="btn-primary" onClick={handleSubmit}>
+                    <button className={styles.btn_secondary} onClick={onClose}>
+                        <i className="bx bx-x"></i>
+                        Cancelar
+                    </button>
+                    <button className={styles.btn_primary} onClick={handleSubmit}>
+                        <i className={`bx ${modoEdicion ? "bx-check" : "bx-plus"}`}></i>
                         {modoEdicion ? "Actualizar" : "Guardar"}
                     </button>
                 </div>
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/CRUDsProductos/Productos.jsx b/Frontend/src/Components/Vendedor/GestionInventario/CRUDsProductos/Productos.jsx
index cd6fcd3..1ea9250 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/CRUDsProductos/Productos.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/CRUDsProductos/Productos.jsx
@@ -7,8 +7,8 @@ import ProductoForm from "./CrudProductos";
 import Filtro from "../Filtro";
 import Paginacion from "../Paginacion";
 import styles from "../../../../assets/Css/crud.module.scss";
-import stylesFiltro from "../../../../assets/Css/deuda.module.scss";
-import { Error, Loading } from "../../../../Utils/Cargando";
+import { Error, Loading } from "../../../../Utils/Components/Cargando";
+import ImagePreview from "../../../../Utils/Components/ImagePreview";
 import ModalCrearStock from "../CrudAlmacenes/ModalCrearStock";
 
 export default function ProductosView() {
@@ -17,8 +17,6 @@ export default function ProductosView() {
     const { crearInventario, isCreando } = useInventario();
 
     // 🧠 Estados
-    const [imgGrande, setImgGrande] = useState(false);
-    const [imgGrandeSrc, setImgGrandeSrc] = useState("");
     const [open, setOpen] = useState(false);
     const [modoEdicion, setModoEdicion] = useState(false);
     const [productoSeleccionado, setProductoSeleccionado] = useState(null);
@@ -95,13 +93,6 @@ export default function ProductosView() {
     };
     const handleClose = () => setOpen(false);
 
-    // 🖼️ Imagen ampliada
-    const handleImgGrande = (producto = null) => {
-        setImgGrande(true);
-        setImgGrandeSrc(producto);
-    };
-    const handleCloseImgGrande = () => setImgGrande(false);
-
     // 💾 Guardar producto
     const handleGuardar = async (form) => {
         try {
@@ -126,10 +117,9 @@ export default function ProductosView() {
     if (errorProductos) return <Error msg={errorProductos.message} />;
 
     return (
-        <main className={`${styles.Contenedor} ${stylesFiltro.Container}`}>
+        <main className={styles.Contenedor}>
             <h2>Gestión de Productos</h2>
 
-            {/* 🔸 Filtros */}
             <Filtro
                 busqueda={busqueda}
                 setBusqueda={setBusqueda}
@@ -155,75 +145,91 @@ export default function ProductosView() {
                 />
             </Filtro>
 
-            {/* 🔸 Tabla de productos */}
             <section className={styles.TablaSection}>
                 <div className={styles.Header}>
-                    <button className="btn-primary" onClick={() => handleOpen()}>
-                        + Nuevo Producto
+                    <button onClick={() => handleOpen()}>
+                        <i className="bx bx-plus"></i>
+                        Nuevo Producto
                     </button>
                 </div>
 
-                <table>
-                    <thead>
-                        <tr>
-                            <th>Imagen</th>
-                            <th>Nombre</th>
-                            <th>Marca</th>
-                            <th>Categoría</th>
-                            <th>Precio Venta</th>
-                            <th>Stock Actual</th>
-                            <th>Stock Mínimo</th>
-                            <th>Acciones</th>
-                        </tr>
-                    </thead>
-                    <tbody>
-                        {productosPaginados.map((prod) => (
-                            <tr key={prod.id_producto}>
-                                <td>
-                                    <img
-                                        src={prod.imagen}
-                                        alt={prod.nombre}
-                                        className={styles.ImagenProducto}
-                                        onClick={() => handleImgGrande(prod)}
-                                        loading="lazy"
-                                    />
-                                </td>
-                                <td>{prod.nombre}</td>
-                                <td>{prod.marca?.nombre}</td>
-                                <td>{prod.marca?.categoria?.nombre}</td>
-                                <td>{formatPrice(prod.precio_venta)}</td>
-                                <td>
-                                    {prod.stock_actual === 0 ? (
-                                        <button
-                                            className="btn-outline-primary"
-                                            onClick={() => {
-                                                setProductoSeleccionado(prod);
-                                                setOpenCrear(true);
-                                            }}
-                                        >
-                                            <SquarePlus />
-                                            Stock
-                                        </button>
-                                    ) : (
-                                        prod.stock_actual
-                                    )}
-                                </td>
-                                <td>{prod.stock_minimo}</td>
-                                <td>
-                                    <button
-                                        className="btn-outline-primary"
-                                        onClick={() => handleOpen(prod)}
-                                    >
-                                        Editar
-                                    </button>
-                                </td>
+                <div className={styles.tablaWrapper}>
+                    <table>
+                        <thead>
+                            <tr>
+                                <th>Imagen</th>
+                                <th>Nombre</th>
+                                <th>Marca</th>
+                                <th>Categoría</th>
+                                <th>Precio Venta</th>
+                                <th>Stock Actual</th>
+                                <th>Stock Mín.</th>
+                                <th>Visibilidad</th>
+                                <th>Acciones</th>
                             </tr>
-                        ))}
-                    </tbody>
-                </table>
+                        </thead>
+                        <tbody>
+                            {productosPaginados.map((prod) => (
+                                <tr key={prod.id_producto}>
+                                    <td data-label="Imagen">
+                                        <ImagePreview
+                                            src={prod.imagen}
+                                            alt={prod.nombre}
+                                            className={styles.ImagenProducto}
+                                        />
+                                    </td>
+                                    <td data-label="Nombre">{prod.nombre}</td>
+                                    <td data-label="Marca">{prod.marca?.nombre || "—"}</td>
+                                    <td data-label="Categoría">{prod.marca?.categoria?.nombre || "—"}</td>
+                                    <td data-label="Precio Venta">{formatPrice(prod.precio_venta)}</td>
+                                    <td data-label="Stock">
+                                        {prod.stock_actual === 0 ? (
+                                            <button
+                                                className={styles.tablaAccionesBtn}
+                                                onClick={() => {
+                                                    setProductoSeleccionado(prod);
+                                                    setOpenCrear(true);
+                                                }}
+                                            >
+                                                <i className="bx bx-plus-circle"></i>
+                                            </button>
+                                        ) : (
+                                            <span style={{ fontWeight: "600", color: "var(--texto)" }}>
+                                                {prod.stock_actual}
+                                            </span>
+                                        )}
+                                    </td>
+                                    <td data-label="Stock Mín.">{prod.stock_minimo}</td>
+                                    <td data-label="Visibilidad">
+                                        <div className={styles.visibilityToggle}>
+                                            <button
+                                                className={`${styles.toggleSwitch} ${prod.activo ? styles.active : ""}`}
+                                                onClick={() => handleOpen(prod)}
+                                                title={prod.activo ? "Desactivar" : "Activar"}
+                                            />
+                                            <span className={styles.toggleLabel}>
+                                                {prod.activo ? "Activo" : "Inactivo"}
+                                            </span>
+                                        </div>
+                                    </td>
+                                    <td data-label="Acciones">
+                                        <div className={styles.acciones}>
+                                            <button
+                                                className={styles.tablaAccionesBtn}
+                                                onClick={() => handleOpen(prod)}
+                                            >
+                                                <i className="bx bx-edit"></i>
+                                                Editar
+                                            </button>
+                                        </div>
+                                    </td>
+                                </tr>
+                            ))}
+                        </tbody>
+                    </table>
+                </div>
             </section>
 
-            {/* 🔸 Modal producto */}
             {open && (
                 <ProductoForm
                     open={open}
@@ -234,7 +240,6 @@ export default function ProductosView() {
                 />
             )}
 
-            {/* 🔸 Modal crear stock */}
             {openCrear && (
                 <ModalCrearStock
                     open={openCrear}
@@ -259,20 +264,6 @@ export default function ProductosView() {
                     ]}
                 />
             )}
-
-            {/* 🔸 Imagen grande */}
-            {imgGrande && (
-                <div className={styles.ImgGrande} onClick={handleCloseImgGrande}>
-                    {imgGrandeSrc.imagen ? (
-                        <img src={imgGrandeSrc.imagen} alt={imgGrandeSrc.nombre} loading="lazy"/>
-                    ) : (
-                        <div className="d-flex flex-column gap-3 justify-content-center align-items-center">
-                            <i className="bx bxs-image-alt"></i>
-                            <h5>{imgGrandeSrc.nombre}</h5>
-                        </div>
-                    )}
-                </div>
-            )}
         </main>
     );
 }
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/AlmacenesView.jsx b/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/AlmacenesView.jsx
index dbd504b..60bc001 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/AlmacenesView.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/AlmacenesView.jsx
@@ -5,12 +5,11 @@ import ModalActualizarStock from "./ModalActualizarStock";
 import ModalTransferirStock from "./ModalTransferirStock";
 import Paginacion from "../Paginacion";
 import styles from "../../../../assets/Css/crud.module.scss";
-import stylesInicio from "../../../../assets/Css/Catalogo.module.scss";
-import stylesFiltro from "../../../../assets/Css/deuda.module.scss";
 import { Warehouse, ShoppingBasket } from "lucide-react";
-import { Error, Loading } from "../../../../Utils/Cargando";
+import { Error, Loading } from "../../../../Utils/Components/Cargando";
 import { useAuthContext } from "../../../../Pages/Context/AuthContext";
 import Swal from "sweetalert2";
+import ImagePreview from "../../../../Utils/Components/ImagePreview";
 
 export default function AlmacenesView() {
     const {user} = useAuthContext();
@@ -148,7 +147,7 @@ export default function AlmacenesView() {
     if (errorInventario) return <Error msg={errorInventario.message} />;
 
     return (
-        <main className={`${styles.Contenedor} ${stylesFiltro.Container}`}>
+        <main className={`${styles.Contenedor}`}>
             <h2>Gestión de Inventario</h2>
 
             <Filtro
@@ -169,17 +168,17 @@ export default function AlmacenesView() {
                 />
             </Filtro>
 
-            <section className={stylesInicio.cartProducto}>
+            <section className={styles.inventarioGrid}>
                 {inventarioAgrupado
                     .slice(page * rowsPerPage, page * rowsPerPage + rowsPerPage)
                     .map((item) => {
-                        return <div className={stylesInicio.cartProductoItem} key={item.id_producto}>
-                            <div className={stylesInicio.ImagenProducto}>
-                                {item.imagen ? (
-                                    <img src={item.imagen} alt={item.nombre_producto} loading="lazy"/>
-                                ) : (
-                                    <i className="bx bx-image"></i>
-                                )}
+                        return <div className={styles.inventarioCard} key={item.id_producto}>
+                            <div className={styles.inventarioImagen}>
+                                <ImagePreview
+                                    src={item.imagen}
+                                    alt={item.nombre_producto}
+                                    loading="lazy"
+                                />
                             </div>
 
                             <h3>{item.nombre_producto}</h3>
@@ -187,7 +186,7 @@ export default function AlmacenesView() {
                             <p><b>Codigo</b> {item.codigo_barras}</p>
 
                             {item.almacenes.map((a) => (
-                                <div key={a.id_almacen} className={stylesInicio.InfoProducto}>
+                                <div key={a.id_almacen} className={styles.inventarioAlmacenRow}>
                                     {a.id_almacen === 1 ? (
                                         <Warehouse size={40} color="var(--azul-500)" />
                                     ) : (
@@ -203,17 +202,17 @@ export default function AlmacenesView() {
                                 <b>Stock Total</b> {item.stock_total}
                             </p>
 
-                            <div className={stylesInicio.BotonesProducto + " btn-group"}>
+                            <div className={styles.inventarioAcciones}>
                                 {user.rol === 1 ? 
                                     <button
-                                        className="btn btn-outline-primary"
+                                        className={styles.btnOutlinePrimary}
                                         onClick={() => handleActualizarStock(item)}
                                     >
                                         Actualizar
                                     </button>
                                 :null}
                                 <button
-                                    className="btn btn-outline-secondary"
+                                    className={styles.btnOutlineSecondary}
                                     onClick={() => handleTransferirStock(item)}
                                 >
                                     Transferir
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalActualizarStock.jsx b/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalActualizarStock.jsx
index 035c8e5..08f5fc4 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalActualizarStock.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalActualizarStock.jsx
@@ -84,10 +84,10 @@ export default function ModalActualizarStock({ open, onClose, onSubmit, item })
                 </div>
 
                 <div className={styles.ModalActions}>
-                    <button className="btn-secondary" onClick={onClose}>
+                    <button className={styles.btn_secondary} onClick={onClose}>
                         Cancelar
                     </button>
-                    <button className="btn-primary" onClick={handleSubmit}>
+                    <button className={styles.btn_primary} onClick={handleSubmit}>
                         Guardar
                     </button>
                 </div>
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalCrearStock.jsx b/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalCrearStock.jsx
index 75ffd2a..2b4474b 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalCrearStock.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalCrearStock.jsx
@@ -95,10 +95,10 @@ export default function ModalCrearStock({ open, onClose, onSubmit, productos, al
                 </div>
 
                 <div className={styles.ModalActions}>
-                    <button className="btn-secondary" onClick={onClose}>
+                    <button className={styles.btn_secondary} onClick={onClose}>
                         Cancelar
                     </button>
-                    <button className="btn-primary" onClick={handleSubmit}>
+                    <button className={styles.btn_primary} onClick={handleSubmit}>
                         Crear
                     </button>
                 </div>
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalTransferirStock.jsx b/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalTransferirStock.jsx
index 09cef9c..0e0cc18 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalTransferirStock.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/CrudAlmacenes/ModalTransferirStock.jsx
@@ -158,10 +158,10 @@ export default function ModalTransferirStock({ open, onClose, onSubmit, item })
                 </div>
 
                 <div className={styles.ModalActions}>
-                    <button className="btn-secondary" onClick={onClose}>
+                    <button className={styles.btn_secondary} onClick={onClose}>
                         Cancelar
                     </button>
-                    <button className="btn-primary" onClick={handleSubmit}>
+                    <button className={styles.btn_primary} onClick={handleSubmit}>
                         Transferir
                     </button>
                 </div>
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/CrudCategorias/CategoriasView.jsx b/Frontend/src/Components/Vendedor/GestionInventario/CrudCategorias/CategoriasView.jsx
index 6a8c10d..78046cc 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/CrudCategorias/CategoriasView.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/CrudCategorias/CategoriasView.jsx
@@ -1,12 +1,11 @@
 import { useState, useMemo } from "react";
 import Swal from "sweetalert2";
-import { Error, Loading } from "../../../../Utils/Cargando";
+import { Error, Loading } from "../../../../Utils/Components/Cargando";
 import useCrudCategorias from "../../../../Hooks/Vendedor/GestionInventario/useCrudCategorias";
 import CategoriaForm from "./CrudCategorias";
 import Filtro from "../Filtro";
 import Paginacion from "../Paginacion";
 import styles from "../../../../assets/Css/crud.module.scss";
-import stylesFiltro from "../../../../assets/Css/deuda.module.scss";
 
 export default function CategoriasView() {
     const { categorias, isLoadingCategorias, errorCategorias, crearCategoria, actualizarCategoria } =
@@ -70,10 +69,9 @@ export default function CategoriasView() {
     if (errorCategorias) return <Error msg={errorCategorias.message} />;
 
     return (
-        <main className={`${styles.Contenedor} ${stylesFiltro.Container}`}>
+        <main className={styles.Contenedor}>
             <h2>Gestión de Categorías</h2>
 
-            {/* 🔸 Filtro */}
             <Filtro
                 busqueda={busqueda}
                 setBusqueda={setBusqueda}
@@ -87,44 +85,46 @@ export default function CategoriasView() {
                 />
             </Filtro>
 
-            {/* 🔸 Tabla */}
             <section className={styles.TablaSection}>
                 <div className={styles.Header}>
-                    <button className="btn-primary" onClick={() => handleOpen()}>
-                        + Nueva Categoría
+                    <button onClick={() => handleOpen()}>
+                        <i className="bx bx-plus"></i>
+                        Nueva Categoría
                     </button>
                 </div>
 
-                <table>
-                    <thead>
-                        <tr>
-                            <th>ID</th>
-                            <th>Nombre</th>
-                            <th>Descripción</th>
-                            <th>Acciones</th>
-                        </tr>
-                    </thead>
-                    <tbody>
-                        {categoriasPaginadas.map((c) => (
-                            <tr key={c.id_categoria}>
-                                <td>{c.id_categoria}</td>
-                                <td>{c.nombre}</td>
-                                <td>{c.descripcion}</td>
-                                <td>
-                                    <button
-                                        className="btn-outline-primary"
-                                        onClick={() => handleOpen(c)}
-                                    >
-                                        Editar
-                                    </button>
-                                </td>
+                <div className={styles.tablaWrapper}>
+                    <table>
+                        <thead>
+                            <tr>
+                                <th>Nombre</th>
+                                <th>Descripción</th>
+                                <th>Acciones</th>
                             </tr>
-                        ))}
-                    </tbody>
-                </table>
+                        </thead>
+                        <tbody>
+                            {categoriasPaginadas.map((c) => (
+                                <tr key={c.id_categoria}>
+                                    <td data-label="Nombre">{c.nombre}</td>
+                                    <td data-label="Descripción">{c.descripcion || "—"}</td>
+                                    <td data-label="Acciones">
+                                        <div className={styles.acciones}>
+                                            <button
+                                                className={styles.tablaAccionesBtn}
+                                                onClick={() => handleOpen(c)}
+                                            >
+                                                <i className="bx bx-edit"></i>
+                                                Editar
+                                            </button>
+                                        </div>
+                                    </td>
+                                </tr>
+                            ))}
+                        </tbody>
+                    </table>
+                </div>
             </section>
 
-            {/* 🔸 Modal */}
             {open && (
                 <CategoriaForm
                     open={open}
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/CrudCategorias/CrudCategorias.jsx b/Frontend/src/Components/Vendedor/GestionInventario/CrudCategorias/CrudCategorias.jsx
index 54686cb..42fd085 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/CrudCategorias/CrudCategorias.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/CrudCategorias/CrudCategorias.jsx
@@ -36,29 +36,37 @@ export default function CategoriaForm({ open, onClose, onSubmit, modoEdicion, ca
                 <h3>{modoEdicion ? "Editar Categoría" : "Nueva Categoría"}</h3>
 
                 <div className={styles.ModalContent}>
-                    <label>Nombre de la categoría
+                    <label>
+                        Nombre de la categoría
                         <input
+                            type="text"
                             name="nombre"
                             value={form.nombre}
                             onChange={handleChange}
                             placeholder="Ej: Bebidas"
+                            required
                         />
                     </label>
 
-                    <label>Descripción
+                    <label>
+                        Descripción
                         <textarea
                             name="descripcion"
                             value={form.descripcion}
                             onChange={handleChange}
                             placeholder="Describe esta categoría..."
-                            rows="3"
+                            rows="4"
                         />
                     </label>
                 </div>
 
                 <div className={styles.ModalActions}>
-                    <button className="btn-secondary" onClick={onClose}>Cancelar</button>
-                    <button className="btn-primary" onClick={handleSubmit}>
+                    <button className={`${styles.ModalActions} btn-secondary`} onClick={onClose}>
+                        <i className="bx bx-x"></i>
+                        Cancelar
+                    </button>
+                    <button className={`${styles.ModalActions} btn-primary`} onClick={handleSubmit}>
+                        <i className={`bx ${modoEdicion ? "bx-check" : "bx-plus"}`}></i>
                         {modoEdicion ? "Actualizar" : "Guardar"}
                     </button>
                 </div>
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/CrudMarcas/CrudMarcas.jsx b/Frontend/src/Components/Vendedor/GestionInventario/CrudMarcas/CrudMarcas.jsx
index a82cc62..6208482 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/CrudMarcas/CrudMarcas.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/CrudMarcas/CrudMarcas.jsx
@@ -62,71 +62,80 @@ export default function MarcaForm({ open, onClose, onSubmit, modoEdicion, marca
                 <h3>{modoEdicion ? "Editar Marca" : "Nueva Marca"}</h3>
 
                 <div className={styles.ModalContent}>
-                    <label>Nombre de la marca
+                    <label>
+                        Nombre de la marca
                         <input
+                            type="text"
                             name="nombre"
                             value={form.nombre}
                             onChange={handleChange}
                             placeholder="Ej: Coca-Cola"
+                            required
                         />
                     </label>
 
-                    <label>Categoría</label>
-                    {isLoadingCategorias ? (
-                        <p>Cargando categorías...</p>
-                    ) : (
-                        <Select
-                            options={optionsCategorias}
-                            value={categoriaSeleccionada}
-                            onChange={handleCategoriaChange}
-                            placeholder="Selecciona una categoría..."
-                            isClearable
-                            className={styles.SelectCategoria}
-                            styles={{
-                                control: (base, state) => ({
-                                    ...base,
-                                    backgroundColor: "var(--input-bg)",
-                                    borderColor: state.isFocused
-                                        ? "var(--input-focus-border)"
-                                        : "var(--input-border)",
-                                    color: "var(--input-text)",
-                                    borderRadius: "var(--radius-sm)",
-                                    padding: "0.2rem 0.3rem",
-                                    boxShadow: "none",
-                                    "&:hover": {
-                                        borderColor: "var(--input-focus-border)",
-                                    },
-                                }),
-                                menu: (base) => ({
-                                    ...base,
-                                    backgroundColor: "var(--input-bg)",
-                                    color: "var(--input-text)",
-                                    borderRadius: "var(--radius-sm)",
-                                    border: "1px solid var(--input-border)",
-                                }),
-                                option: (base, state) => ({
-                                    ...base,
-                                    backgroundColor: state.isFocused
-                                        ? "var(--input-focus-border)"
-                                        : "var(--input-bg)",
-                                    color: "var(--input-text)",
-                                }),
-                                singleValue: (base) => ({
-                                    ...base,
-                                    color: "var(--input-text)",
-                                }),
-                                placeholder: (base) => ({
-                                    ...base,
-                                    color: "var(--input-placeholder)",
-                                }),
-                            }}
-                        />
-                    )}
+                    <label>
+                        Categoría
+                        {isLoadingCategorias ? (
+                            <div style={{ padding: "10px", color: "var(--texto-sec)" }}>
+                                Cargando categorías...
+                            </div>
+                        ) : (
+                            <Select
+                                options={optionsCategorias}
+                                value={categoriaSeleccionada}
+                                onChange={handleCategoriaChange}
+                                placeholder="Selecciona una categoría..."
+                                isClearable
+                                className={styles.SelectCategoria}
+                                styles={{
+                                    control: (base, state) => ({
+                                        ...base,
+                                        backgroundColor: "var(--input-bg)",
+                                        borderColor: state.isFocused ? "var(--rojo-500)" : "var(--input-border)",
+                                        color: "var(--input-text)",
+                                        borderRadius: "var(--radius-md)",
+                                        padding: "0.2rem",
+                                        boxShadow: state.isFocused ? "var(--focus-ring)" : "none",
+                                        "&:hover": {
+                                            borderColor: "var(--rojo-700)",
+                                        },
+                                    }),
+                                    menu: (base) => ({
+                                        ...base,
+                                        backgroundColor: "var(--input-bg)",
+                                        color: "var(--input-text)",
+                                        borderRadius: "var(--radius-md)",
+                                        border: "1px solid var(--input-border)",
+                                        zIndex: 100,
+                                    }),
+                                    option: (base, state) => ({
+                                        ...base,
+                                        backgroundColor: state.isFocused ? "rgba(198, 40, 40, 0.15)" : "var(--input-bg)",
+                                        color: "var(--input-text)",
+                                        cursor: "pointer",
+                                    }),
+                                    singleValue: (base) => ({
+                                        ...base,
+                                        color: "var(--input-text)",
+                                    }),
+                                    placeholder: (base) => ({
+                                        ...base,
+                                        color: "var(--placeholder)",
+                                    }),
+                                }}
+                            />
+                        )}
+                    </label>
                 </div>
 
                 <div className={styles.ModalActions}>
-                    <button className="btn-secondary" onClick={onClose}>Cancelar</button>
-                    <button className="btn-primary" onClick={handleSubmit}>
+                    <button className={`${styles.ModalActions} btn-secondary`} onClick={onClose}>
+                        <i className="bx bx-x"></i>
+                        Cancelar
+                    </button>
+                    <button className={`${styles.ModalActions} btn-primary`} onClick={handleSubmit}>
+                        <i className={`bx ${modoEdicion ? "bx-check" : "bx-plus"}`}></i>
                         {modoEdicion ? "Actualizar" : "Guardar"}
                     </button>
                 </div>
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/CrudMarcas/MarcasView.jsx b/Frontend/src/Components/Vendedor/GestionInventario/CrudMarcas/MarcasView.jsx
index 6fe3544..595d9ba 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/CrudMarcas/MarcasView.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/CrudMarcas/MarcasView.jsx
@@ -1,12 +1,11 @@
 import { useState, useMemo } from "react";
 import Swal from "sweetalert2";
-import { Error, Loading } from "../../../../Utils/Cargando";
+import { Error, Loading } from "../../../../Utils/Components/Cargando";
 import useCrudCategorias from "../../../../Hooks/Vendedor/GestionInventario/useCrudCategorias";
 import MarcaForm from "./CrudMarcas";
 import Filtro from "../Filtro";
 import Paginacion from "../Paginacion";
 import styles from "../../../../assets/Css/crud.module.scss";
-import stylesFiltro from "../../../../assets/Css/deuda.module.scss";
 
 export default function MarcasView() {
     const { marcas, categorias, isLoadingMarcas, errorMarcas, crearMarca, actualizarMarca } = useCrudCategorias();
@@ -75,10 +74,9 @@ export default function MarcasView() {
     const categoriasUnicas = categorias.map((c) => c.nombre);
 
     return (
-        <main className={`${styles.Contenedor} ${stylesFiltro.Container}`}>
+        <main className={styles.Contenedor}>
             <h2>Gestión de Marcas</h2>
 
-            {/* 🔸 Filtro con paginación */}
             <Filtro
                 busqueda={busqueda}
                 setBusqueda={setBusqueda}
@@ -97,44 +95,46 @@ export default function MarcasView() {
                 />
             </Filtro>
 
-            {/* 🔸 Tabla */}
             <section className={styles.TablaSection}>
                 <div className={styles.Header}>
-                    <button className="btn-primary" onClick={() => handleOpen()}>
-                        + Nueva Marca
+                    <button onClick={() => handleOpen()}>
+                        <i className="bx bx-plus"></i>
+                        Nueva Marca
                     </button>
                 </div>
 
-                <table>
-                    <thead>
-                        <tr>
-                            <th>ID</th>
-                            <th>Nombre</th>
-                            <th>Categoría</th>
-                            <th>Acciones</th>
-                        </tr>
-                    </thead>
-                    <tbody>
-                        {marcasPaginadas.map((m) => (
-                            <tr key={m.id_marca}>
-                                <td>{m.id_marca}</td>
-                                <td>{m.nombre}</td>
-                                <td>{m.categoria?.nombre}</td>
-                                <td>
-                                    <button
-                                        className="btn-outline-primary"
-                                        onClick={() => handleOpen(m)}
-                                    >
-                                        Editar
-                                    </button>
-                                </td>
+                <div className={styles.tablaWrapper}>
+                    <table>
+                        <thead>
+                            <tr>
+                                <th>Nombre</th>
+                                <th>Categoría</th>
+                                <th>Acciones</th>
                             </tr>
-                        ))}
-                    </tbody>
-                </table>
+                        </thead>
+                        <tbody>
+                            {marcasPaginadas.map((m) => (
+                                <tr key={m.id_marca}>
+                                    <td data-label="Nombre">{m.nombre}</td>
+                                    <td data-label="Categoría">{m.categoria?.nombre || "—"}</td>
+                                    <td data-label="Acciones">
+                                        <div className={styles.acciones}>
+                                            <button
+                                                className={styles.tablaAccionesBtn}
+                                                onClick={() => handleOpen(m)}
+                                            >
+                                                <i className="bx bx-edit"></i>
+                                                Editar
+                                            </button>
+                                        </div>
+                                    </td>
+                                </tr>
+                            ))}
+                        </tbody>
+                    </table>
+                </div>
             </section>
 
-            {/* 🔸 Modal */}
             {open && (
                 <MarcaForm
                     open={open}
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/Filtro.jsx b/Frontend/src/Components/Vendedor/GestionInventario/Filtro.jsx
index 6df62a7..276bcc8 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/Filtro.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/Filtro.jsx
@@ -1,5 +1,5 @@
 import Select from "react-select";
-import stylesFiltro from "../../../assets/Css/deuda.module.scss";
+import stylesFiltro from "../../../assets/Css/Dependencias/filtro.module.scss";
 import { useAuthContext } from "../../../Pages/Context/AuthContext";
 
 export default function Filtro({
@@ -21,7 +21,6 @@ export default function Filtro({
 }) {
     const { user } = useAuthContext();
 
-    // 🔧 Convertimos las listas en opciones para react-select
     const opcionesCategorias = [
         { value: "", label: "Todas las categorías" },
         ...categorias.map((c) => ({ value: c, label: c })),
@@ -36,39 +35,33 @@ export default function Filtro({
         option: (provided, state) => ({
             ...provided,
             backgroundColor: state.isSelected
-                ? "var(--rojo-500)"
+                ? "linear-gradient(135deg, var(--rojo-600), var(--azul-600))"
                 : state.isFocused
-                ? "rgba(255, 0, 0, 0.15)"
-                : "#fff",
-            color: state.isSelected
-                ? "#fff"
-                : "var(--texto-negro)",
+                ? "rgba(198, 40, 40, 0.15)"
+                : "var(--input-bg)",
+            color: state.isSelected ? "#fff" : "var(--texto)",
             cursor: "pointer",
-            transition: "background-color 0.2s ease-in-out, color 0.2s ease-in-out",
+            transition: "background-color var(--transition-fast)",
         }),
         control: (provided, state) => ({
             ...provided,
-            backgroundColor: "#fff",
-            borderColor: state.isFocused
-                ? "var(--rojo-500)"
-                : "var(--glass)",
-            borderRadius: "8px",
-            padding: "0.1rem",
-            boxShadow: state.isFocused
-                ? "0 0 0 3px rgba(255, 0, 0, 0.2)"
-                : "none",
-            transition: "all 0.2s ease-in-out",
+            backgroundColor: "var(--input-bg)",
+            borderColor: state.isFocused ? "var(--rojo-500)" : "var(--input-border)",
+            borderRadius: "var(--radius-md)",
+            padding: "0.2rem",
+            boxShadow: state.isFocused ? "var(--focus-ring)" : "none",
+            transition: "all var(--transition-fast)",
             "&:hover": {
-                borderColor: "var(--rojo-500)",
+                borderColor: "var(--rojo-700)",
             },
         }),
         menu: (provided) => ({
             ...provided,
-            backgroundColor: "#fff",
-            border: "1px solid var(--glass)",
-            borderRadius: "8px",
-            boxShadow: "0 2px 6px rgba(0, 0, 0, 0.2)",
-            zIndex: 20,
+            backgroundColor: "var(--input-bg)",
+            border: "1px solid var(--input-border)",
+            borderRadius: "var(--radius-md)",
+            boxShadow: "var(--shadow-md)",
+            zIndex: 100,
         }),
         menuList: (provided) => ({
             ...provided,
@@ -76,7 +69,7 @@ export default function Filtro({
         }),
         singleValue: (provided) => ({
             ...provided,
-            color: "var(--texto-negro)",
+            color: "var(--texto)",
         }),
         placeholder: (provided) => ({
             ...provided,
@@ -84,23 +77,19 @@ export default function Filtro({
         }),
         input: (provided) => ({
             ...provided,
-            color: "var(--texto-negro)",
+            color: "var(--texto)",
         }),
         dropdownIndicator: (provided, state) => ({
             ...provided,
-            color: state.isFocused
-                ? "var(--rojo-500)"
-                : "var(--texto-negro)",
-            transition: "color 0.2s ease-in-out",
-            "&:hover": {
-                color: "var(--rojo-400)",
-            },
+            color: state.isFocused ? "var(--rojo-500)" : "var(--texto-sec)",
+            transition: "color var(--transition-fast)",
         }),
         clearIndicator: (provided) => ({
             ...provided,
-            color: "var(--texto-negro)",
+            color: "var(--texto-sec)",
+            transition: "color var(--transition-fast)",
             "&:hover": {
-                color: "var(--rojo-400)",
+                color: "var(--rojo-500)",
             },
         }),
     };
@@ -123,7 +112,7 @@ export default function Filtro({
             {/* 🧾 Código de barras */}
             {mostrarCodigo && (user?.rol === 1 || user?.rol === 2) && (
                 <label>
-                    Buscar por código de barras
+                    Código de Barras
                     <input
                         type="text"
                         placeholder="Ej: 770200100001"
@@ -135,17 +124,20 @@ export default function Filtro({
 
             {/* 🗂️ Categoría */}
             {mostrarCategoria && (
-                <label style={{ width: "100%" }}>
+                <label>
                     Categoría
                     <Select
                         options={opcionesCategorias}
-                        value={opcionesCategorias.find((opt) => opt.value === categoria) || opcionesCategorias[0]}
+                        value={
+                            opcionesCategorias.find((opt) => opt.value === categoria) ||
+                            opcionesCategorias[0]
+                        }
                         onChange={(selected) => {
                             setCategoria(selected.value);
                             if (setMarca) setMarca("");
                         }}
                         isClearable
-                        placeholder="Seleccionar categoría..."
+                        placeholder="Seleccionar..."
                         classNamePrefix="react-select"
                         styles={customStyles}
                     />
@@ -154,22 +146,25 @@ export default function Filtro({
 
             {/* 🏷️ Marca */}
             {mostrarMarca && (
-                <label style={{ width: "100%" }}>
+                <label>
                     Marca
                     <Select
                         options={opcionesMarcas}
-                        value={opcionesMarcas.find((opt) => opt.value === marca) || opcionesMarcas[0]}
+                        value={
+                            opcionesMarcas.find((opt) => opt.value === marca) ||
+                            opcionesMarcas[0]
+                        }
                         onChange={(selected) => setMarca(selected.value)}
                         isClearable
-                        placeholder="Seleccionar marca..."
+                        placeholder="Seleccionar..."
                         classNamePrefix="react-select"
                         styles={customStyles}
                     />
                 </label>
             )}
 
+            {/* Paginación o botones adicionales */}
             {children}
         </div>
     );
-}
-
+}
\ No newline at end of file
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/GestionInventario.jsx b/Frontend/src/Components/Vendedor/GestionInventario/GestionInventario.jsx
index f28b30c..806acb8 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/GestionInventario.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/GestionInventario.jsx
@@ -1,19 +1,20 @@
-import styles from "../../../assets/Css/index.module.scss";
-import stylesInicio from "../../../assets/Css/Inicio.module.scss";
+import stylesInicio from "../../../assets/Css/Principales/Inicio.module.scss";
 import RutasInventario from "../../../Utils/GestionInventario.json";
 import { TareaContainer } from "../../Client/Tareas/ComponentsTareas";
 
 export default function GestionInventario() {
     return (
-        <main className= {`${styles.Container} ${stylesInicio.ItemContainer}`}>
-            <h2>Gestión de Inventario</h2>
-            {RutasInventario.map((ruta, index) => (
-                <TareaContainer
-                    key={index}
-                    titulo={ruta.Titulo}
-                    items={ruta.items}
-                />
-            ))}
+        <main className={`${stylesInicio.inicioMain}`}>
+            <h1 className={stylesInicio.pageTitle}>Gestión de Inventario</h1>
+            <section className={stylesInicio.inventarioContent}>
+                {RutasInventario.map((ruta, index) => (
+                    <TareaContainer
+                        key={index}
+                        titulo={ruta.Titulo}
+                        items={ruta.items}
+                    />
+                ))}
+            </section>
         </main>
     );
 }
diff --git a/Frontend/src/Components/Vendedor/GestionInventario/Paginacion.jsx b/Frontend/src/Components/Vendedor/GestionInventario/Paginacion.jsx
index 1d71351..f1776a8 100644
--- a/Frontend/src/Components/Vendedor/GestionInventario/Paginacion.jsx
+++ b/Frontend/src/Components/Vendedor/GestionInventario/Paginacion.jsx
@@ -1,4 +1,4 @@
-import styles from "../../../assets/Css/crud.module.scss";
+import styles from "../../../assets/Css/Dependencias/Paginacion.module.scss";
 
 export default function Paginacion({
     page,
@@ -7,35 +7,51 @@ export default function Paginacion({
     handleChangePage,
     handleChangeRowsPerPage,
 }) {
+    const totalPages = Math.ceil(total / rowsPerPage);
+    const isFirstPage = page === 0;
+    const isLastPage = (page + 1) * rowsPerPage >= total;
+
     return (
         <div className={styles.Paginacion}>
             <h6>Paginación</h6>
+            
             <div className={styles.PaginacionBotones}>
                 <button
                     className="btn-secondary"
                     disabled={page === 0}
                     onClick={() => handleChangePage(page - 1)}
+                    title="Página anterior"
+                    aria-label="Ir a página anterior"
                 >
                     <i className="bx bxs-chevrons-left"></i>
                 </button>
-                <span> {page + 1} / {Math.ceil(total / rowsPerPage)}</span>
+
+                <span aria-live="polite" aria-atomic="true">
+                    {page + 1} / {Math.ceil(total / rowsPerPage)}
+                </span>
+
                 <button
                     className="btn-secondary"
                     disabled={(page + 1) * rowsPerPage >= total}
                     onClick={() => handleChangePage(page + 1)}
+                    title="Página siguiente"
+                    aria-label="Ir a página siguiente"
                 >
                     <i className="bx bxs-chevrons-right"></i>
                 </button>
             </div>
+
             <select
                 value={rowsPerPage}
                 onChange={handleChangeRowsPerPage}
-                className="form-control"
+                aria-label="Filas por página"
             >
                 {[5, 10, 15, 20, 25].map((n) => (
-                    <option key={n} value={n}>{n} por página</option>
+                    <option key={n} value={n}>
+                        {n} por página
+                    </option>
                 ))}
             </select>
         </div>
     );
-}
+}
\ No newline at end of file
diff --git a/Frontend/src/Pages/Context/ColorContexts.jsx b/Frontend/src/Pages/Context/ColorContexts.jsx
new file mode 100644
index 0000000..1a9bf91
--- /dev/null
+++ b/Frontend/src/Pages/Context/ColorContexts.jsx
@@ -0,0 +1,92 @@
+import { createContext, useContext, useEffect, useMemo, useState } from "react";
+
+/**
+ * ColorContexts — gestor de tema (oscuro / claro / sistema).
+ *
+ * Mejoras Fase 2:
+ *  - Default = "system" (sigue al SO)
+ *  - Detecta `prefers-color-scheme` y reacciona si cambia el ajuste del SO
+ *  - Persiste preferencia explícita en `localStorage`
+ *  - Aplica `data-theme` en <body> y `color-scheme` CSS para mejor render nativo
+ */
+
+const STORAGE_KEY = "daily.theme"; // "claro" | "oscuro" | "system"
+
+const ThemeContext = createContext(null);
+
+function getSystemTheme() {
+    if (typeof window === "undefined") return "oscuro";
+    return window.matchMedia("(prefers-color-scheme: light)").matches
+        ? "claro"
+        : "oscuro";
+}
+
+function readStoredPreference() {
+    if (typeof window === "undefined") return "system";
+    const v = localStorage.getItem(STORAGE_KEY);
+    return v === "claro" || v === "oscuro" || v === "system" ? v : "system";
+}
+
+export function ColorContexts({ children }) {
+    // Preferencia explícita del usuario
+    const [preference, setPreference] = useState(readStoredPreference);
+    // Tema efectivo aplicado (tras resolver "system")
+    const [systemTheme, setSystemTheme] = useState(getSystemTheme);
+
+    const theme = preference === "system" ? systemTheme : preference;
+
+    // Reaccionar a cambios del SO si la preferencia es "system"
+    useEffect(() => {
+        if (typeof window === "undefined") return;
+        const mq = window.matchMedia("(prefers-color-scheme: light)");
+        const onChange = () =>
+            setSystemTheme(mq.matches ? "claro" : "oscuro");
+        mq.addEventListener?.("change", onChange);
+        return () => mq.removeEventListener?.("change", onChange);
+    }, []);
+
+    // Aplicar al DOM
+    useEffect(() => {
+        document.body.dataset.theme = theme;
+        document.documentElement.style.colorScheme =
+            theme === "claro" ? "light" : "dark";
+    }, [theme]);
+
+    // Persistir preferencia
+    useEffect(() => {
+        try {
+            localStorage.setItem(STORAGE_KEY, preference);
+        } catch {
+            /* ignore quota / private mode */
+        }
+    }, [preference]);
+
+    const value = useMemo(
+        () => ({
+            theme,             // resuelto: "claro" | "oscuro"
+            preference,        // explícita: "claro" | "oscuro" | "system"
+            setPreference,
+            toggleTheme: () =>
+                setPreference((prev) =>
+                    prev === "oscuro"
+                        ? "claro"
+                        : prev === "claro"
+                          ? "system"
+                          : "oscuro",
+                ),
+        }),
+        [theme, preference],
+    );
+
+    return (
+        <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>
+    );
+}
+
+export function useTheme() {
+    const ctx = useContext(ThemeContext);
+    if (!ctx) {
+        throw new Error("useTheme debe usarse dentro de <ColorContexts>");
+    }
+    return ctx;
+}
diff --git a/Frontend/src/Pages/Funcionales/Error404.jsx b/Frontend/src/Pages/Funcionales/Error404.jsx
index 8cdf239..9fb7598 100644
--- a/Frontend/src/Pages/Funcionales/Error404.jsx
+++ b/Frontend/src/Pages/Funcionales/Error404.jsx
@@ -1,6 +1,6 @@
 import { useNavigate } from "react-router-dom";
 import { Skull } from "lucide-react";
-import styles from "../../assets/Css/Error404.module.scss";
+import styles from "../../assets/Css/Principales/Error404.module.scss";
 
 export default function Error404() {
     const navigate = useNavigate();
diff --git a/Frontend/src/Pages/Layouts/LayoutPrivado.jsx b/Frontend/src/Pages/Layouts/LayoutPrivado.jsx
index ebd3622..a160ca5 100644
--- a/Frontend/src/Pages/Layouts/LayoutPrivado.jsx
+++ b/Frontend/src/Pages/Layouts/LayoutPrivado.jsx
@@ -5,7 +5,7 @@ import Header from '../../Components/Client/Fijos/Header';
 import Footer from '../../Components/Client/Fijos/Footer';
 import { Outlet } from "react-router-dom";
 import "../../assets/Css/Transiciones.scss";
-import { BtnVolver } from "../../Utils/Cargando";
+import { BtnVolver } from "../../Utils/Components/Cargando";
 
 export default function LayoutPrivado({ requiredRole }) {
     const location = useLocation();
@@ -19,9 +19,10 @@ export default function LayoutPrivado({ requiredRole }) {
     }
 
     return (
-        <PrivateRoute requiredRole={requiredRole} >
+        <PrivateRoute requiredRole={requiredRole}>
             <Header />
-            <BtnVolver/>
+            <BtnVolver />
+
             <Outlet key={location.pathname} />
 
             {/* Capa flotante que anima cada cambio de ruta */}
@@ -37,5 +38,5 @@ export default function LayoutPrivado({ requiredRole }) {
             </AnimatePresence>
             <Footer />
         </PrivateRoute>
-    )
+    );
 }
diff --git a/Frontend/src/Utils/Cargando.jsx b/Frontend/src/Utils/Components/Cargando.jsx
similarity index 100%
rename from Frontend/src/Utils/Cargando.jsx
rename to Frontend/src/Utils/Components/Cargando.jsx
diff --git a/Frontend/src/Utils/Components/Color.jsx b/Frontend/src/Utils/Components/Color.jsx
new file mode 100644
index 0000000..d5e830b
--- /dev/null
+++ b/Frontend/src/Utils/Components/Color.jsx
@@ -0,0 +1,22 @@
+import { useTheme } from "../../Pages/Context/ColorContexts";
+import { Sun, Moon } from "lucide-react";
+import styles from "../../assets/Css/Dependencias/Color.module.scss";
+
+export default function Color() {
+    const { theme, toggleTheme } = useTheme();
+
+    return (
+        <button
+            onClick={toggleTheme}
+            type="button"
+            className={styles.themeButton}
+            aria-label="Cambiar tema"
+        >
+            {theme === "claro" ? (
+                <Sun size={40} />
+            ) : (
+                <Moon size={40} />
+            )}
+        </button>
+    );
+}
\ No newline at end of file
diff --git a/Frontend/src/Utils/Components/ImagePreview.jsx b/Frontend/src/Utils/Components/ImagePreview.jsx
new file mode 100644
index 0000000..b3c906f
--- /dev/null
+++ b/Frontend/src/Utils/Components/ImagePreview.jsx
@@ -0,0 +1,51 @@
+import { useState } from "react";
+import { createPortal } from "react-dom";
+import styles from "../../assets/Css/Dependencias/ImagePreview.module.scss";
+
+export default function ImagePreview({
+    src,
+    alt = "Imagen",
+    className = "",
+    fallback = null,
+}) {
+    const [open, setOpen] = useState(false);
+
+    return (
+        <>
+            {src ? (
+                <img
+                    src={src}
+                    alt={alt}
+                    className={className}
+                    loading="lazy"
+                    onClick={() => setOpen(true)}
+                />
+            ) : (
+                fallback ? ( fallback ):(
+                    <div className={styles.fallback}>
+                        <i className="bx bx-image"></i>
+                    </div>
+                )
+            )}
+
+            {open &&
+                createPortal(
+                    <div
+                        className={styles.overlay}
+                        onClick={() => setOpen(false)}
+                    >
+                        {src ? (
+                            <img
+                                src={src}
+                                alt={alt}
+                                loading="lazy"
+                            />
+                        ) : (
+                            fallback
+                        )}
+                    </div>,
+                    document.body
+                )}
+        </>
+    );
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Admin/DashboardAdmin.module.scss b/Frontend/src/assets/Css/Admin/DashboardAdmin.module.scss
new file mode 100644
index 0000000..08eb385
--- /dev/null
+++ b/Frontend/src/assets/Css/Admin/DashboardAdmin.module.scss
@@ -0,0 +1,271 @@
+.dashboard{
+    display:flex;
+    flex-direction:column;
+    gap:var(--space-xl);
+    padding:var(--space-lg);
+}
+
+.header{
+    display:flex;
+    justify-content:space-between;
+    align-items:center;
+    padding:var(--space-xl);
+    background:linear-gradient(135deg,var(--rojo-800),var(--azul-800));
+    border:1px solid var(--card-border);
+    border-radius:var(--radius-lg);
+    box-shadow:var(--shadow-1);
+
+    h1{
+        margin:0;
+        font-size:2rem;
+        color:var(--texto);
+        font-weight:700;
+    }
+
+    p{
+        margin-top:var(--space-xs);
+        color:var(--texto-sec);
+    }
+}
+
+.stats{
+    display:grid;
+    grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
+    gap:var(--space-lg);
+}
+
+.statCard{
+    display:flex;
+    flex-direction:column;
+    align-items:center;
+    justify-content:center;
+    gap:var(--space-sm);
+    min-height:160px;
+    background:var(--card-bg);
+    border:1px solid var(--card-border);
+    border-radius:var(--radius-lg);
+    box-shadow:var(--shadow-1);
+    transition:var(--transition-fast);
+    cursor:default;
+
+    svg{
+        color:var(--azul-300);
+    }
+
+    span{
+        font-size:2.3rem;
+        font-weight:700;
+        color:var(--texto);
+    }
+
+    small{
+        color:var(--texto-sec);
+        font-size:.95rem;
+    }
+
+    &:hover{
+        transform:translateY(-6px);
+        border-color:var(--rojo-600);
+        box-shadow:0 10px 25px rgba(0,0,0,.35);
+    }
+}
+
+.searchContainer{
+    display:flex;
+    align-items:center;
+    gap:var(--space-sm);
+    padding:0 var(--space-md);
+    height:55px;
+    background:var(--input-bg);
+    border:1px solid var(--input-border);
+    border-radius:var(--radius-md);
+
+    svg{
+        color:var(--placeholder);
+    }
+
+    input{
+        flex:1;
+        border:none;
+        outline:none;
+        background:transparent;
+        color:var(--texto);
+        font-size:1rem;
+
+        &::placeholder{
+            color:var(--placeholder);
+        }
+    }
+
+    &:focus-within{
+        border-color:var(--input-focus-border);
+        box-shadow:var(--input-focus-shadow);
+    }
+}
+
+.usersGrid{
+    display:grid;
+    grid-template-columns:repeat(auto-fill,minmax(360px,1fr));
+    gap:var(--space-lg);
+}
+
+.userCard{
+    display:flex;
+    align-items:center;
+    gap:var(--space-lg);
+    padding:var(--space-lg);
+    background:var(--card-bg);
+    border:1px solid var(--card-border);
+    border-radius:var(--radius-lg);
+    box-shadow:var(--shadow-1);
+    transition:var(--transition-fast);
+
+    &:hover{
+        transform:translateY(-5px);
+        border-color:var(--rojo-600);
+        box-shadow:0 12px 30px rgba(0,0,0,.4);
+    }
+}
+
+.avatar{
+    flex-shrink:0;
+    width:72px;
+    height:72px;
+    border-radius:50%;
+    overflow:hidden;
+    background:var(--gradient-primary);
+    display:flex;
+    align-items:center;
+    justify-content:center;
+    border:3px solid var(--rojo-600);
+
+    img{
+        width:100%;
+        height:100%;
+        object-fit:cover;
+    }
+
+    span{
+        color:white;
+        font-size:1.8rem;
+        font-weight:700;
+    }
+}
+
+.userInfo{
+    flex:1;
+    display:flex;
+    flex-direction:column;
+    gap:var(--space-sm);
+
+    h3{
+        margin:0;
+        color:var(--texto);
+        font-size:1.2rem;
+    }
+
+    p{
+        display:flex;
+        align-items:center;
+        gap:var(--space-xs);
+        margin:0;
+        color:var(--texto-sec);
+        font-size:.92rem;
+        word-break:break-word;
+
+        svg{
+            color:var(--azul-300);
+            flex-shrink:0;
+        }
+    }
+}
+
+.top{
+    display:flex;
+    justify-content:space-between;
+    align-items:center;
+    gap:var(--space-sm);
+}
+
+.activo,
+.inactivo,
+.rol{
+    display:flex;
+    align-items:center;
+    justify-content:center;
+    width:max-content;
+    padding:6px 14px;
+    border-radius:999px;
+    font-size:.8rem;
+    font-weight:600;
+}
+
+.activo{
+    background:var(--success-soft);
+    color:var(--success);
+}
+
+.inactivo{
+    background:var(--danger-soft);
+    color:var(--danger);
+}
+
+.rol{
+    margin-top:var(--space-xs);
+    background:var(--azul-700);
+    color:white;
+}
+
+.error,
+.empty{
+    display:flex;
+    justify-content:center;
+    align-items:center;
+    min-height:180px;
+    background:var(--card-bg);
+    border:1px dashed var(--card-border);
+    border-radius:var(--radius-lg);
+    color:var(--texto-sec);
+    font-size:1rem;
+}
+
+@media(max-width:992px){
+    .usersGrid{
+        grid-template-columns:1fr;
+    }
+}
+
+@media(max-width:768px){
+    .dashboard{
+        padding:var(--space-md);
+    }
+
+    .header{
+        padding:var(--space-lg);
+
+        h1{
+            font-size:1.6rem;
+        }
+    }
+
+    .userCard{
+        flex-direction:column;
+        align-items:center;
+        text-align:center;
+    }
+
+    .top{
+        flex-direction:column;
+    }
+
+    .userInfo{
+
+        p{
+            justify-content:center;
+        }
+
+        .rol{
+            align-self:center;
+        }
+    }
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Catalogo.module.scss b/Frontend/src/assets/Css/Catalogo.module.scss
index c1cc400..08c0cce 100644
--- a/Frontend/src/assets/Css/Catalogo.module.scss
+++ b/Frontend/src/assets/Css/Catalogo.module.scss
@@ -1,64 +1,83 @@
-.cartProducto{
+.container {
+    display: grid;
+    grid-template-columns: 1fr 3fr;
+    gap: var(--space-2xl);
+    padding: var(--space-lg);
+
+    h2 {
+        grid-column: span 2 / span 2;
+        font-size: var(--font-2xl);
+        font-weight: var(--weight-bold);
+        color: var(--texto);
+        text-align: center;
+        padding-bottom: var(--space-md);
+        border-bottom: var(--border-width) solid var(--card-border);
+    }
+}
+
+.cartProducto {
     display: flex;
     flex-wrap: wrap;
     justify-content: center;
-    gap: 1rem;
+    gap: var(--space-md);
 }
-.cartProductoItem{
+
+.cartProductoItem {
     position: relative;
     width: 250px;
-    background: var(--card-bg);
-    box-shadow: var(--shadow-1);
-    padding: 1rem;
-    border-radius: var(--radius-md);
     display: flex;
     flex-direction: column;
     justify-content: space-between;
-    gap: 0.5rem;
+    gap: var(--space-xs);
+    padding: var(--space-md);
+    background: var(--card-bg);
+    border-radius: var(--radius-md);
+    box-shadow: var(--shadow-md);
 
-    h3{
+    h3 {
         text-align: center;
     }
-    
-    p{
-        text-transform: capitalize;
+
+    p {
         margin: 0;
         display: flex;
         flex-direction: column;
         justify-content: space-between;
-        gap: 0.2rem;
+        gap: var(--space-xxs);
         text-align: center;
+        text-transform: capitalize;
 
-        b{
+        b {
             color: var(--azul-300);
         }
     }
 
-    &:hover{
-        box-shadow: var(--shadow-2);
+    &:hover {
+        box-shadow: var(--shadow-lg);
         transition: box-shadow var(--transition-fast);
 
-        .ImagenProducto{
+        .ImagenProducto {
             cursor: pointer;
             transform: scale(1.1) translateY(-10px) rotate(3deg);
             transition: transform var(--transition-fast);
         }
     }
 }
-.ImagenProducto{
+
+.ImagenProducto {
     width: 100%;
     height: 200px;
     object-fit: cover;
     border-radius: var(--radius-md);
 
-    img{
+    img {
         width: 100%;
         height: 100%;
         object-fit: cover;
         border-radius: var(--radius-md);
     }
 
-    i{
+    i {
         width: 100%;
         height: 100%;
         display: flex;
@@ -69,32 +88,93 @@
     }
 }
 
-.InfoProducto{
+.InfoProducto {
     display: flex;
     justify-content: space-around;
     align-items: center;
 }
-.ProductoInactivo{
-    color: var(--rojo-300);
-    font-weight: bold;
+
+.ProductoInactivo {
     text-align: center;
+    font-weight: var(--weight-bold);
+    color: var(--rojo-300);
 }
 
-.FiltroActivo{
+.FiltroActivo {
+    padding-top: var(--space-md);
     border-top: 1px solid var(--texto-sec);
-    padding-top: 1rem;
 }
 
-.BotonesProducto{
-    background: var(--texto-sec);
-}
-
-.IconoProducto{
+.IconoProducto {
     display: flex;
     justify-content: center;
     align-items: center;
+    position: absolute;
+    z-index: var(--z-dropdown);
     font-size: 40px;
     color: var(--azul-300);
-    position: absolute;
-    z-index: 10;
+}
+
+/* ======================================================
+   SWITCH (antes en crud.module.scss)
+   ====================================================== */
+
+.switchLabel {
+    display: inline-flex;
+    align-items: center;
+    gap: var(--space-sm);
+    cursor: pointer;
+
+    input {
+        position: absolute;
+        opacity: 0;
+        width: 0;
+        height: 0;
+
+        &:checked + .switch {
+            background: var(--btn-primary-bg);
+
+            &::before {
+                transform: translateX(20px);
+            }
+        }
+    }
+}
+
+.switchText {
+    font-size: var(--font-sm);
+    color: var(--texto-sec);
+}
+
+.switch {
+    position: relative;
+    display: inline-block;
+    width: 44px;
+    height: 24px;
+    background: var(--input-border);
+    border-radius: var(--radius-full);
+    transition: background var(--transition-fast);
+
+    &::before {
+        content: "";
+        position: absolute;
+        top: 2px;
+        left: 2px;
+        width: 20px;
+        height: 20px;
+        background: #fff;
+        border-radius: var(--radius-full);
+        box-shadow: var(--shadow-xs);
+        transition: transform var(--transition-fast);
+    }
+}
+
+@media (max-width: 768px) {
+    .container {
+        width: 100%;
+        display: flex;
+        flex-direction: column;
+        padding: var(--space-md);
+        gap: var(--space-lg);
+    }
 }
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/CierraDia/Bolsillos.module.scss b/Frontend/src/assets/Css/CierraDia/Bolsillos.module.scss
index c12bee0..2ec1c7b 100644
--- a/Frontend/src/assets/Css/CierraDia/Bolsillos.module.scss
+++ b/Frontend/src/assets/Css/CierraDia/Bolsillos.module.scss
@@ -1,12 +1,12 @@
 ﻿.panel {
-    background: var(--card-bg);
-    color: var(--texto);
-    border-radius: 1.25rem;
-    padding: 1rem;
     display: grid;
-    gap: 1rem;
+    gap: var(--space-md);
+    padding: var(--space-md);
+    color: var(--texto);
+    background: var(--card-bg);
     border: 1px solid var(--card-border);
-    box-shadow: var(--shadow-1);
+    border-radius: var(--radius-xl);
+    box-shadow: var(--shadow-lg);
 }
 
 .panelHeader,
@@ -18,7 +18,7 @@
     display: flex;
     align-items: center;
     justify-content: space-between;
-    gap: 0.8rem;
+    gap: var(--space-sm);
     flex-wrap: wrap;
 }
 
@@ -43,20 +43,19 @@
 
 .kicker {
     display: block;
-    color: var(--texto-muted);
-    font-size: 0.78rem;
-    letter-spacing: 0.08em;
+    color: var(--texto-sec);
+    font-size: var(--font-xs);
+    letter-spacing: var(--tracking-wide);
     text-transform: uppercase;
 }
 
 .summaryGrid,
 .listGrid,
 .colorGrid,
-.iconGrid,
 .timeline,
 .formGrid {
     display: grid;
-    gap: 1rem;
+    gap: var(--space-md);
 }
 
 .summaryGrid {
@@ -64,35 +63,27 @@
 }
 
 .summaryCard {
-    border-radius: 1rem;
-    padding: 1rem;
-    border: 1px solid var(--card-border);
-    background: color-mix(in srgb, var(--card-bg) 90%, black);
-    display: grid;
-    gap: 0.55rem;
     min-height: 125px;
+    display: grid;
+    gap: var(--space-xs);
+    padding: var(--space-md);
+    background: color-mix(in srgb, var(--card-bg) 90%, black);
+    border: 1px solid var(--card-border);
+    border-radius: var(--radius-lg);
 }
 
 .summaryLabel,
-.statLabel,
 .optionTitle,
 .fieldLabel,
-.previewTitle,
-.sectionKicker {
-    display: block;
-    font-size: 0.9rem;
-}
-
-.summaryLabel,
-.statLabel {
-    color: var(--texto-sec);
+.previewTitle {
     display: flex;
     align-items: center;
-    gap: 0.45rem;
+    gap: var(--space-xxs);
+    font-size: var(--font-sm);
+    color: var(--texto-sec);
 }
 
-.summaryLabel i,
-.statLabel i {
+.summaryLabel svg {
     color: var(--azul-200);
 }
 
@@ -100,52 +91,45 @@
 .moneyMain,
 .previewBox strong,
 .bolsilloName {
-    font-weight: 800;
-    letter-spacing: -0.03em;
+    font-weight: var(--weight-bold);
+    letter-spacing: var(--tracking-tight);
 }
 
 .summaryValue {
-    font-size: clamp(1.4rem, 2vw, 2rem);
-}
-
-.summaryNote {
-    font-size: 0.85rem;
+    font-size: clamp(1.4rem, 2vw, var(--font-4xl));
 }
 
 .progressWrap {
     display: grid;
-    gap: 0.55rem;
+    gap: var(--space-xs);
 }
 
 .progressTrack,
 .cardProgress {
     width: 100%;
     height: 0.9rem;
-    border-radius: 999px;
     overflow: hidden;
     background: color-mix(in srgb, var(--card-border) 85%, black);
     border: 1px solid var(--card-border);
+    border-radius: var(--radius-full);
 }
 
 .progressFill,
 .cardProgress span {
+    display: block;
     height: 100%;
     border-radius: inherit;
-    display: block;
     transition: width var(--transition-fast);
-}
-
-.progressFill {
     background: linear-gradient(90deg, var(--rojo-500), var(--azul-400));
 }
 
 .progressFooter {
     display: flex;
     justify-content: space-between;
-    gap: 0.75rem;
+    gap: var(--space-sm);
     flex-wrap: wrap;
     color: var(--texto-sec);
-    font-size: 0.9rem;
+    font-size: var(--font-sm);
 }
 
 .listGrid {
@@ -155,22 +139,22 @@
 .bolsilloCard {
     position: relative;
     overflow: hidden;
-    border-radius: 1.2rem;
-    padding: 1rem;
+    display: grid;
+    gap: var(--space-sm);
+    padding: var(--space-md);
     border: 1px solid var(--card-border);
+    border-radius: var(--radius-xl);
     background:
         radial-gradient(circle at top right, color-mix(in srgb, var(--accent) 28%, transparent), transparent 34%),
         color-mix(in srgb, var(--card-bg) 94%, black);
-    display: grid;
-    gap: 0.85rem;
-}
+    transition: transform var(--transition-fast), background var(--transition-fast);
 
-.bolsilloCard:hover {
-    transform: scale(1.05);
-    transition: transform var(--transition-fast);
-    background:
-        radial-gradient(circle at top right, color-mix(in srgb, var(--accent) 80%, transparent), transparent 50%),
-        color-mix(in srgb, var(--card-bg) 94%, black);
+    &:hover {
+        transform: scale(1.05);
+        background:
+            radial-gradient(circle at top right, color-mix(in srgb, var(--accent) 80%, transparent), transparent 50%),
+            color-mix(in srgb, var(--card-bg) 94%, black);
+    }
 }
 
 .bolsilloAccent {
@@ -185,68 +169,68 @@
     display: flex;
     align-items: center;
     justify-content: space-between;
-    gap: 0.8rem;
+    gap: var(--space-sm);
     flex-wrap: wrap;
 }
 
 .bolsilloIdentity {
     display: flex;
     align-items: center;
-    gap: 0.8rem;
+    gap: var(--space-sm);
     min-width: 0;
 }
 
 .bolsilloAvatar {
-    width: 72px;
-    height: 72px;
-    border-radius: 1rem;
-    overflow: hidden;
     display: grid;
     place-items: center;
     flex-shrink: 0;
-    background: var(--accent);
+    width: 72px;
+    height: 72px;
     color: #fff;
+    background: var(--accent);
+    border-radius: var(--radius-lg);
+    overflow: hidden;
 
-    i {
-        font-size: 1.8rem;
+    svg {
+        font-size: var(--font-2xl);
     }
 }
 
 .bolsilloInfo {
-    min-width: 0;
     display: grid;
-    gap: 0.35rem;
+    gap: var(--space-xxs);
+    min-width: 0;
 }
 
 .bolsilloName {
-    font-size: 1rem;
+    font-size: var(--font-md);
 }
 
-.activitySection {
-    display: grid;
-    gap: 0.9rem;
-    padding-top: 0.25rem;
+.bolsilloDesc {
+    font-size: var(--font-sm);
 }
 
-.bolsilloDesc {
-    font-size: 0.88rem;
+.activitySection {
+    display: grid;
+    gap: var(--space-sm);
+    padding-top: var(--space-xxs);
 }
 
 .tag {
     display: inline-flex;
     align-items: center;
-    gap: 0.45rem;
-    padding: 0.45rem 0.8rem;
-    border-radius: 999px;
-    background: var(--success-soft);
-    color: var(--texto);
-    font-size: 0.9rem;
+    gap: var(--space-xxs);
+    padding: var(--space-xs) var(--space-sm);
     white-space: nowrap;
+    font-size: var(--font-sm);
+    color: var(--texto);
+    background: var(--success-soft);
+    border-radius: var(--radius-full);
 }
 
 .moneyRow {
     display: grid;
-    gap: 0.15rem;
+    gap: var(--space-xxs);
 }
 
 .moneyMain {
@@ -254,30 +238,26 @@
 }
 
 .moneyMeta {
-    font-size: 0.85rem;
+    font-size: var(--font-sm);
 }
 
 .cardFooter {
     align-items: flex-end;
 }
 
-.metaLabel {
-    color: var(--texto-muted);
-    margin-bottom: 0.15rem;
-}
-
 .cardActions {
-    width: 100%;
     display: grid;
+    width: 100%;
     gap: var(--space-md);
     grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
 }
+
 .actionButton,
-.dangerButton{
+.dangerButton {
     display: flex;
-    justify-content: center;
     align-items: center;
-    gap: 0.45rem;
+    justify-content: center;
+    gap: var(--space-xxs);
 }
 
 .actionButton,
@@ -287,13 +267,19 @@
 .cancelButton,
 .saveButton,
 .modalClose,
-.buttons button,
-.colorSwatch,
-.iconOption {
-    border: 1px solid transparent;
-    border-radius: 999px;
+.colorSwatch {
     cursor: pointer;
-    transition: transform var(--transition-fast), background var(--transition-fast), border-color var(--transition-fast), opacity var(--transition-fast);
+    border: 1px solid transparent;
+    border-radius: var(--radius-full);
+    transition:
+        transform var(--transition-fast),
+        background var(--transition-fast),
+        border-color var(--transition-fast),
+        opacity var(--transition-fast);
+
+    &:hover {
+        transform: translateY(-1px);
+    }
 }
 
 .primaryButton,
@@ -302,155 +288,133 @@
     display: inline-flex;
     align-items: center;
     justify-content: center;
-    gap: 0.45rem;
-    font-weight: 700;
+    gap: var(--space-xxs);
+    font-weight: var(--weight-bold);
 }
 
 .actionButton,
 .primaryButton,
 .saveButton {
-    background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
+    padding: var(--space-sm) var(--space-md);
     color: #fff;
-    padding: 0.75rem 1rem;
+    background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
 }
 
 .secondaryButton,
 .cancelButton {
-    background: transparent;
+    padding: var(--space-xs) var(--space-sm);
     color: var(--texto);
+    background: transparent;
     border-color: var(--card-border);
-    padding: 0.65rem 0.95rem;
 }
 
 .dangerButton {
+    padding: var(--space-xs) var(--space-sm);
+    color: var(--rojo-100);
     background: color-mix(in srgb, var(--danger) 22%, transparent);
-    color: #ffdede;
     border-color: color-mix(in srgb, var(--danger) 42%, transparent);
-    padding: 0.65rem 0.95rem;
-}
-
-.buttons {
-    display: flex;
-    flex-wrap: wrap;
-    gap: 0.65rem;
-}
-
-.buttons button:hover,
-.primaryButton:hover,
-.secondaryButton:hover,
-.actionButton:hover,
-.dangerButton:hover,
-.cancelButton:hover,
-.saveButton:hover,
-.modalClose:hover,
-.colorSwatch:hover,
-.iconOption:hover {
-    transform: translateY(-1px);
-}
-
-.buttons button {
-    padding: 0.65rem 0.95rem;
-    background: transparent;
-    color: var(--texto);
-    border-color: var(--card-border);
-}
-
-.active {
-    background: linear-gradient(135deg, var(--rojo-600), var(--azul-600)) !important;
-    border-color: transparent !important;
-    color: #fff !important;
 }
 
 .emptyState {
-    border: 1px dashed var(--card-border);
-    border-radius: 1rem;
-    padding: 1.5rem;
     display: grid;
     place-items: center;
-    gap: 0.65rem;
-    color: var(--texto-muted);
+    gap: var(--space-sm);
+    padding: var(--space-lg);
     text-align: center;
-}
-
-.emptyState i {
-    font-size: 2.2rem;
-    color: var(--azul-200);
-}
+    color: var(--texto-sec);
+    border: 1px dashed var(--card-border);
+    border-radius: var(--radius-lg);
 
-.activitySection {
-    display: grid;
-    gap: 0.9rem;
-    padding-top: 0.25rem;
+    svg {
+        font-size: var(--font-3xl);
+        color: var(--azul-200);
+    }
 }
 
 .timeline {
-    gap: 0.85rem;
+    gap: var(--space-sm);
 }
 
 .timelineItem {
     display: grid;
     grid-template-columns: 15px repeat(auto-fill, minmax(250px, 1fr));
     gap: var(--space-md);
+    padding-bottom: var(--space-sm);
     border-bottom: 1px solid var(--card-border);
-    padding-bottom: 0.85rem;
 }
 
 .timelineDot {
+    flex-shrink: 0;
     width: 12px;
     height: 12px;
-    border-radius: 50%;
-    margin-top: 0.35rem;
+    margin-top: var(--space-xxs);
+    border-radius: var(--radius-full);
     background: var(--rojo-500);
     box-shadow: 0 0 0 4px color-mix(in srgb, var(--rojo-500) 18%, transparent);
-    flex-shrink: 0;
+}
+
+.timelineDotEntrada {
+    background: var(--success);
+    box-shadow: 0 0 0 4px color-mix(in srgb, var(--success) 18%, transparent);
+}
+
+.timelineDotSalida {
+    background: var(--info);
+    box-shadow: 0 0 0 4px color-mix(in srgb, var(--info) 18%, transparent);
+}
+
+.timelineDotPago {
+    background: var(--danger);
+    box-shadow: 0 0 0 4px color-mix(in srgb, var(--danger) 18%, transparent);
 }
 
 .timelineBody p {
     margin: 0;
-    font-size: 0.9rem;
+    font-size: var(--font-sm);
 }
 
 .modalBackdrop {
     position: fixed;
     inset: 0;
-    z-index: 2000;
-    background: rgba(0, 0, 0, 0.65);
+    z-index: var(--z-modal);
     display: grid;
     place-items: center;
-    padding: 1rem;
+    padding: var(--space-md);
+    background: var(--overlay);
+    backdrop-filter: blur(4px);
 }
 
 .modal {
+    display: flex;
+    flex-direction: column;
+    gap: var(--space-lg);
     width: min(720px, 100%);
     max-height: 92vh;
     overflow-y: auto;
-    border-radius: 1.4rem;
-    padding: 1rem;
+    padding: var(--space-md);
     border: 1px solid var(--card-border);
+    border-radius: var(--radius-2xl);
     background: linear-gradient(180deg, color-mix(in srgb, var(--card-bg) 94%, black), var(--fondo-elevado));
-    box-shadow: var(--shadow-1);
-    display: flex;
-    flex-direction: column;
-    gap: var(--space-lg);
+    box-shadow: var(--shadow-lg);
 }
 
 .modalTitle {
     margin: 0;
-    font-size: 1.25rem;
+    font-size: var(--font-xl);
 }
 
 .modalClose {
+    display: grid;
+    place-items: center;
     width: 44px;
     height: 44px;
     padding: 0;
-    display: grid;
-    place-items: center;
-    background: transparent;
     color: var(--texto);
+    background: transparent;
     border-color: var(--card-border);
 
-    i {
-        font-size: 1.2rem;
+    svg {
+        font-size: var(--font-xl);
     }
 }
 
@@ -460,7 +424,7 @@
 
 .field {
     display: grid;
-    gap: 0.45rem;
+    gap: var(--space-xs);
 }
 
 .fieldFull {
@@ -470,117 +434,135 @@
 .fieldInput,
 .fieldTextarea {
     width: 100%;
-    border: 1px solid var(--input-border);
+    padding: var(--space-sm) var(--space-md);
+    color: var(--texto);
     background: var(--input-bg);
-    color: var(--input-text);
-    padding: 0.75rem 0.9rem;
+    border: 1px solid var(--input-border);
     border-radius: var(--radius-md);
     outline: none;
-}
 
-.fieldInput:focus,
-.fieldTextarea:focus {
-    border-color: var(--input-focus-border);
-    box-shadow: var(--input-focus-shadow);
+    &:focus {
+        border-color: var(--rojo-500);
+        box-shadow: var(--focus-ring);
+    }
 }
 
 .fieldTextarea {
-    resize: vertical;
     min-height: 88px;
+    resize: vertical;
 }
 
-.optionTitle {
-    color: var(--texto-sec);
-}
-
-.colorGrid,
-.iconGrid {
+.colorGrid {
     grid-template-columns: repeat(auto-fit, minmax(44px, 1fr));
 }
 
-.colorSwatch,
-.iconOption {
-    width: 44px;
-    height: 44px;
+.colorSwatch {
     display: grid;
     place-items: center;
+    width: 44px;
+    height: 44px;
     color: #fff;
     background: var(--card-bg);
     border-color: var(--card-border);
 }
 
-.colorSwatch i,
-.iconOption i {
-    font-size: 1.2rem;
-}
-
-.iconOption {
-    color: var(--texto);
-    background: color-mix(in srgb, var(--card-bg) 90%, black);
-}
-
 .isSelected {
     transform: translateY(-2px) scale(1.03);
     box-shadow: 0 0 0 2px color-mix(in srgb, var(--azul-200) 40%, transparent);
 }
 
-.acciones{
-    width: 80%;
-    margin: auto;
-    display: flex;
-    flex-direction: column;
-    justify-content: center;
-    gap: var(--space-sm);
-}
-
 .previewBox {
     position: relative;
     overflow: hidden;
-    border-radius: 1rem;
-    padding: 1rem;
-    min-height: 150px;
     display: grid;
-    gap: 0.35rem;
     align-content: start;
+    gap: var(--space-xs);
+    min-height: 150px;
+    padding: var(--space-md);
+    border: 1px solid var(--card-border);
+    border-radius: var(--radius-lg);
     background:
         radial-gradient(circle at top right, color-mix(in srgb, var(--accent) 40%, transparent), transparent 45%),
         color-mix(in srgb, var(--card-bg) 92%, black);
-    border: 1px solid var(--card-border);
 }
 
 .previewBox strong {
-    font-size: 1.2rem;
+    font-size: var(--font-xl);
 }
 
-.previewBox i {
+.previewIcon {
     position: absolute;
-    right: 1rem;
-    bottom: 1rem;
-    font-size: 3rem;
+    right: var(--space-md);
+    bottom: var(--space-md);
+    opacity: .9;
     color: color-mix(in srgb, var(--accent) 70%, white);
-    opacity: 0.9;
+}
+
+.swatchCheck {
+    font-size: var(--font-md);
+    font-weight: var(--weight-bold);
+    text-shadow: 0 1px 2px rgba(0, 0, 0, .2);
 }
 
 .alert {
     grid-column: 1 / -1;
-    padding: 0.9rem 1rem;
-    border-radius: 0.9rem;
+    padding: var(--space-sm) var(--space-md);
+    color: var(--rojo-100);
     background: var(--danger-soft);
-    color: #ffdede;
     border: 1px solid color-mix(in srgb, var(--danger) 45%, transparent);
+    border-radius: var(--radius-lg);
+}
+
+.notice {
+    padding: var(--space-sm) var(--space-md);
+    color: var(--success);
+    background: var(--success-soft);
+    border: 1px solid color-mix(in srgb, var(--success) 45%, transparent);
+    border-radius: var(--radius-lg);
 }
 
 .modalFooter {
     grid-column: 1 / -1;
 }
 
+.sectionActions {
+    display: flex;
+    align-items: center;
+    justify-content: flex-end;
+    gap: var(--space-sm);
+    flex-wrap: wrap;
+}
+
+.iconPickerGrid {
+    display: grid;
+    gap: var(--space-sm);
+    grid-template-columns: repeat(auto-fit, minmax(112px, 1fr));
+}
+
+.iconPickerButton {
+    display: grid;
+    place-items: center;
+    gap: var(--space-xs);
+    min-height: 92px;
+    padding: var(--space-sm);
+    color: var(--texto);
+    background: color-mix(in srgb, var(--card-bg) 92%, black);
+    border: 1px solid var(--card-border);
+    border-radius: var(--radius-lg);
+}
+
+.iconPickerGlyph {
+    color: inherit;
+}
+
 @media (max-width: 768px) {
     .timelineItem {
         grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
     }
+
     .panel,
     .modal {
-        padding: 0.95rem;
+        padding: var(--space-sm);
     }
 
     .formGrid {
@@ -599,7 +581,6 @@
         justify-content: flex-start;
     }
 
-    .buttons button,
     .primaryButton,
     .secondaryButton,
     .actionButton,
@@ -608,109 +589,8 @@
     .saveButton {
         width: 100%;
     }
-}
-
-.sectionActions {
-    display: flex;
-    align-items: center;
-    justify-content: flex-end;
-    gap: 0.75rem;
-    flex-wrap: wrap;
-}
-
-.notice {
-    padding: 0.9rem 1rem;
-    border-radius: 0.9rem;
-    background: var(--success-soft);
-    color: var(--success);
-    border: 1px solid color-mix(in srgb, var(--success) 45%, transparent);
-}
-
-.success {
-    padding: 0.9rem 1rem;
-    border-radius: 0.9rem;
-    background: var(--success-soft);
-    color: var(--success);
-    border: 1px solid color-mix(in srgb, var(--success) 45%, transparent);
-}
-.summaryLabel svg,
-.statLabel svg,
-.emptyState svg,
-.modalClose svg,
-.actionButton svg,
-.primaryButton svg,
-.secondaryButton svg,
-.dangerButton svg,
-.cancelButton svg,
-.saveButton svg,
-.tag svg,
-.kicker svg {
-    width: 1em;
-    height: 1em;
-    flex-shrink: 0;
-}
-
-.iconPickerGrid {
-    display: grid;
-    gap: 0.75rem;
-    grid-template-columns: repeat(auto-fit, minmax(112px, 1fr));
-}
-
-.iconPickerButton {
-    border: 1px solid var(--card-border);
-    border-radius: 1rem;
-    padding: 0.85rem 0.75rem;
-    background: color-mix(in srgb, var(--card-bg) 92%, black);
-    color: var(--texto);
-    display: grid;
-    gap: 0.4rem;
-    place-items: center;
-    min-height: 92px;
-}
-
-.iconPickerGlyph {
-    color: inherit;
-}
-
-.swatchCheck {
-    font-size: 1rem;
-    font-weight: 800;
-    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
-}
 
-.previewIcon {
-    position: absolute;
-    right: 1rem;
-    bottom: 1rem;
-    color: color-mix(in srgb, var(--accent) 70%, white);
-    opacity: 0.9;
-}
-
-.notice {
-    padding: 0.9rem 1rem;
-    border-radius: 0.9rem;
-    background: var(--success-soft);
-    color: var(--success);
-    border: 1px solid color-mix(in srgb, var(--success) 45%, transparent);
-}
-
-.timelineDotEntrada {
-    background: var(--success);
-    box-shadow: 0 0 0 4px color-mix(in srgb, var(--success) 18%, transparent);
-}
-
-.timelineDotSalida {
-    background: var(--info);
-    box-shadow: 0 0 0 4px color-mix(in srgb, var(--info) 18%, transparent);
-}
-
-.timelineDotPago {
-    background: var(--danger);
-    box-shadow: 0 0 0 4px color-mix(in srgb, var(--danger) 18%, transparent);
-}
-
-@media (max-width: 768px) {
     .iconPickerGrid {
         grid-template-columns: repeat(auto-fit, minmax(96px, 1fr));
     }
-}
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/CierraDia/CierreDia.module.scss b/Frontend/src/assets/Css/CierraDia/CierreDia.module.scss
index f563dde..98fca47 100644
--- a/Frontend/src/assets/Css/CierraDia/CierreDia.module.scss
+++ b/Frontend/src/assets/Css/CierraDia/CierreDia.module.scss
@@ -1,336 +1,301 @@
 ﻿.container {
     display: flex;
     flex-direction: column;
-    gap: 1rem;
-    padding: 1rem 0 2rem;
+    gap: var(--space-md);
+    padding: var(--space-md) 0 var(--space-2xl);
     color: var(--texto);
 }
 
 .hero {
     position: relative;
     overflow: hidden;
-    border-radius: 1.4rem;
-    padding: 1.25rem;
+    padding: var(--space-lg);
+    border: 1px solid var(--card-border);
+    border-radius: var(--radius-2xl);
+    box-shadow: var(--shadow-lg);
     background:
         radial-gradient(circle at top right, color-mix(in srgb, var(--azul-400) 24%, transparent), transparent 36%),
         linear-gradient(145deg, color-mix(in srgb, var(--card-bg) 92%, black), color-mix(in srgb, var(--azul-900) 25%, var(--card-bg)));
-    border: 1px solid var(--card-border);
-    box-shadow: var(--shadow-1);
 }
 
 .heroHeader,
-.panelHeader,
 .sectionHeader,
 .cardTopLine,
-.bolsilloHead,
-.progressHeader,
-.cardFooter,
 .modalHeader,
 .modalFooter {
     display: flex;
     align-items: center;
     justify-content: space-between;
-    gap: 0.8rem;
+    gap: var(--space-sm);
     flex-wrap: wrap;
 }
 
 .heroTitle {
     display: grid;
-    gap: 0.35rem;
+    gap: var(--space-xs);
 
     h2 {
-        font-size: clamp(1.7rem, 2.5vw, 2.4rem);
         margin: 0;
+        font-size: clamp(1.7rem, 2.5vw, var(--font-4xl));
     }
 }
 
-.heroLead,
-.panelLead,
-.bolsilloDesc,
-.summaryNote,
-.statHint,
-.fieldHint,
-.metaLabel,
-.timelineBody p {
-    color: var(--texto-sec);
-}
-
-.heroLead,
-.panelLead {
+.heroLead {
     margin: 0;
     max-width: 56rem;
+    color: var(--texto-sec);
 }
 
 .kicker,
 .sectionKicker {
     display: block;
-    color: var(--texto-muted);
-    font-size: 0.78rem;
-    letter-spacing: 0.08em;
+    font-size: var(--font-xs);
+    letter-spacing: var(--tracking-wide);
     text-transform: uppercase;
+    color: var(--texto-sec);
 }
 
-.heroBadge,
-.tag {
+.heroBadge {
     display: inline-flex;
     align-items: center;
-    gap: 0.45rem;
-    padding: 0.45rem 0.8rem;
-    border-radius: 999px;
-    background: var(--success-soft);
-    color: var(--texto);
-    font-size: 0.9rem;
+    gap: var(--space-xxs);
+    padding: var(--space-xs) var(--space-sm);
     white-space: nowrap;
+    font-size: var(--font-sm);
+    color: var(--texto);
+    background: var(--success-soft);
+    border-radius: var(--radius-full);
 }
 
 .badgePending {
-    background: color-mix(in srgb, var(--warning) 90%, white);
     color: #1e1300;
+    background: color-mix(in srgb, var(--warning) 90%, white);
 }
 
 .summaryGrid {
     display: grid;
+    gap: var(--space-sm);
+    margin-top: var(--space-md);
     grid-template-columns: repeat(auto-fit, minmax(190px, 1fr));
-    gap: 0.9rem;
-    margin-top: 1.1rem;
 }
 
 .statCard,
 .summaryCard {
-    border-radius: 1rem;
-    padding: 1rem;
+    display: grid;
+    gap: var(--space-xs);
+    min-height: 110px;
+    padding: var(--space-md);
     border: 1px solid var(--card-border);
+    border-radius: var(--radius-lg);
     background: color-mix(in srgb, var(--card-bg) 90%, black);
-    display: grid;
-    gap: 0.55rem;
-    min-height: 125px;
 }
 
 .statLabel,
 .summaryLabel {
-    font-size: 0.9rem;
-    color: var(--texto-sec);
     display: flex;
     align-items: center;
-    gap: 0.45rem;
-}
-
-.statLabel i,
-.summaryLabel i {
-    color: var(--azul-200);
-}
+    gap: var(--space-xxs);
+    font-size: var(--font-sm);
+    color: var(--texto-sec);
 
-.statValue,
-.summaryValue,
-.moneyMain,
-.previewBox strong,
-.bolsilloName {
-    font-weight: 800;
-    letter-spacing: -0.03em;
+    svg {
+        color: var(--azul-200);
+    }
 }
 
 .statValue,
 .summaryValue {
-    font-size: clamp(1.4rem, 2vw, 2rem);
+    font-size: clamp(1.4rem, 2vw, var(--font-4xl));
+    font-weight: var(--weight-bold);
+    letter-spacing: var(--tracking-tight);
 }
 
-.statHint,
-.summaryNote {
-    font-size: 0.85rem;
+.statHint {
+    font-size: var(--font-sm);
+    color: var(--texto-sec);
 }
 
-.progressSection,
-.progressWrap {
+.progressSection {
     display: grid;
-    gap: 0.55rem;
-    margin-top: 1rem;
+    gap: var(--space-xs);
+    margin-top: var(--space-md);
 }
 
-.progressTrack,
-.cardProgress {
+.progressTrack {
     width: 100%;
-    height: 0.9rem;
-    border-radius: 999px;
+    height: .9rem;
     overflow: hidden;
-    background: color-mix(in srgb, var(--card-border) 85%, black);
     border: 1px solid var(--card-border);
+    border-radius: var(--radius-full);
+    background: color-mix(in srgb, var(--card-border) 85%, black);
 }
 
-.progressFill,
-.cardProgress span {
+.progressFill {
+    display: block;
     height: 100%;
     border-radius: inherit;
-    display: block;
     transition: width var(--transition-fast);
-}
-
-.progressFill {
     background: linear-gradient(90deg, var(--rojo-500), var(--azul-400));
 }
 
-.progressLegend,
-.progressFooter {
+.progressLegend {
     display: flex;
     justify-content: space-between;
-    gap: 0.75rem;
+    gap: var(--space-sm);
     flex-wrap: wrap;
+    font-size: var(--font-sm);
     color: var(--texto-sec);
-    font-size: 0.9rem;
 }
 
 .range {
     display: grid;
+    gap: var(--space-md);
     grid-template-columns: repeat(2, minmax(0, 1fr));
-    gap: 1rem;
 }
 
 .rangeItem,
-.field,
-.bolsilloInfo,
-.timelineBody {
+.field {
     display: grid;
-    gap: 0.45rem;
+    gap: var(--space-xs);
 }
 
 .rangeItem label,
-.fieldLabel,
-.optionTitle,
-.previewTitle,
-.metaLabel {
-    font-size: 0.9rem;
+.fieldLabel {
+    font-size: var(--font-sm);
 }
 
 .rangeItem input,
 .fieldInput,
 .fieldTextarea {
     width: 100%;
-    border: 1px solid var(--input-border);
+    padding: var(--space-sm) var(--space-md);
+    color: var(--texto);
     background: var(--input-bg);
-    color: var(--input-text);
-    padding: 0.75rem 0.9rem;
+    border: 1px solid var(--input-border);
     border-radius: var(--radius-md);
     outline: none;
+
+    &:focus {
+        border-color: var(--rojo-500);
+        box-shadow: var(--focus-ring);
+    }
+}
+
+.fieldHint {
+    color: var(--texto-sec);
 }
 
-.rangeItem input:focus,
-.fieldInput:focus,
-.fieldTextarea:focus {
-    border-color: var(--input-focus-border);
-    box-shadow: var(--input-focus-shadow);
+.fieldFull {
+    grid-column: 1 / -1;
 }
 
 .fieldTextarea {
-    resize: vertical;
     min-height: 88px;
+    resize: vertical;
 }
 
-.containerCart,
-.panel {
-    background: var(--card-bg);
-    color: var(--texto);
-    border-radius: 1.25rem;
-    padding: 1rem;
+.rangeActions {
+    display: flex;
+    align-items: flex-end;
+    justify-content: flex-end;
+}
+
+.containerCart {
     display: grid;
-    gap: 1rem;
+    gap: var(--space-md);
+    padding: var(--space-md);
+    color: var(--texto);
+    background: var(--card-bg);
     border: 1px solid var(--card-border);
-    box-shadow: var(--shadow-1);
+    border-radius: var(--radius-xl);
+    box-shadow: var(--shadow-lg);
+}
+
+.sectionActions {
+    display: flex;
+    align-items: center;
+    justify-content: flex-end;
+    gap: var(--space-sm);
+    flex-wrap: wrap;
 }
 
 .sectionTag {
-    padding: 0.45rem 0.75rem;
-    border-radius: 999px;
-    background: color-mix(in srgb, var(--azul-600) 22%, transparent);
+    padding: var(--space-xs) var(--space-sm);
+    font-size: var(--font-sm);
     color: var(--texto);
-    font-size: 0.85rem;
+    background: color-mix(in srgb, var(--azul-600) 22%, transparent);
+    border-radius: var(--radius-full);
 }
 
 .total {
-    font-weight: 700;
+    font-weight: var(--weight-bold);
     color: var(--success);
 }
 
-.buttons,
-.cardActions,
-.modalFooter {
+.buttons {
     display: flex;
     flex-wrap: wrap;
-    gap: 0.65rem;
+    gap: var(--space-sm);
+
+    button {
+        padding: var(--space-xs) var(--space-sm);
+        color: var(--texto);
+        background: transparent;
+        border: 1px solid var(--card-border);
+        border-radius: var(--radius-full);
+        cursor: pointer;
+        display: flex;
+        align-items: center;
+        justify-content: center;
+        gap: var(--space-xxs);
+        transition: transform var(--transition-fast), background var(--transition-fast), border-color var(--transition-fast);
+
+        &:hover {
+            transform: translateY(-1px);
+        }
+    }
 }
 
-.buttons button,
 .primaryButton,
 .secondaryButton,
-.actionButton,
-.dangerButton,
 .cancelButton,
 .saveButton,
 .modalClose {
-    display: flex;
+    display: inline-flex;
     align-items: center;
     justify-content: center;
-    gap: var(--space-xs);
-    padding: 0.75rem 1rem;
-    border: 1px solid transparent;
-    border-radius: 999px;
+    gap: var(--space-xxs);
     cursor: pointer;
-    transition: transform var(--transition-fast), background var(--transition-fast), border-color var(--transition-fast), opacity var(--transition-fast);
-}
-
-.buttons button,
-.actionButton,
-.dangerButton,
-.cancelButton,
-.saveButton,
-.modalClose {
-    padding: 0.65rem 0.95rem;
-}
-
-.buttons button,
-.actionButton,
-.cancelButton {
-    background: transparent;
-    color: var(--texto);
-    border-color: var(--card-border);
-}
-
-.buttons button:hover,
-.actionButton:hover,
-.cancelButton:hover,
-.primaryButton:hover,
-.secondaryButton:hover,
-.dangerButton:hover,
-.saveButton:hover,
-.modalClose:hover {
-    transform: translateY(-1px);
-}
+    border: 1px solid transparent;
+    border-radius: var(--radius-full);
+    transition: transform var(--transition-fast), background var(--transition-fast), border-color var(--transition-fast);
 
-.primaryButton,
-.secondaryButton,
-.saveButton {
-    display: inline-flex;
-    align-items: center;
-    justify-content: center;
-    gap: 0.45rem;
-    font-weight: 700;
+    &:hover {
+        transform: translateY(-1px);
+    }
 }
 
 .primaryButton,
 .saveButton {
-    background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
+    padding: var(--space-sm) var(--space-md);
     color: #fff;
+    font-weight: var(--weight-bold);
+    background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
 }
 
 .secondaryButton {
-    background: color-mix(in srgb, var(--azul-600) 22%, transparent);
+    padding: var(--space-sm) var(--space-md);
     color: var(--texto);
+    font-weight: var(--weight-bold);
+    background: color-mix(in srgb, var(--azul-600) 22%, transparent);
     border-color: color-mix(in srgb, var(--azul-600) 42%, transparent);
-    padding: 0.75rem 1rem;
 }
 
-.dangerButton {
-    background: color-mix(in srgb, var(--danger) 22%, transparent);
-    color: #ffdede;
-    border-color: color-mix(in srgb, var(--danger) 42%, transparent);
+.cancelButton {
+    padding: var(--space-xs) var(--space-sm);
+    color: var(--texto);
+    background: transparent;
+    border-color: var(--card-border);
 }
 
 .active {
@@ -340,13 +305,9 @@
 }
 
 .grid,
-.gridVentas,
-.listGrid,
-.colorGrid,
-.iconGrid,
-.timeline {
+.gridVentas {
     display: grid;
-    gap: 1rem;
+    gap: var(--space-md);
 }
 
 .grid {
@@ -357,72 +318,79 @@
     grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
 }
 
-.listGrid {
-    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
-}
-
 .card {
-    border-radius: 1rem;
-    padding: 1rem;
+    display: grid;
+    gap: var(--space-xs);
+    padding: var(--space-md);
     border: 1px solid var(--card-border);
+    border-radius: var(--radius-lg);
     background:
         radial-gradient(circle at top right, color-mix(in srgb, var(--azul-300) 12%, transparent), transparent 32%),
         color-mix(in srgb, var(--card-bg) 92%, black);
-    display: grid;
-    gap: 0.55rem;
 }
 
-.cardTopLine {
-    i {
-        color: var(--azul-200);
-        font-size: 1.35rem;
-    }
+.cardTopLine svg {
+    font-size: var(--font-xl);
+    color: var(--azul-200);
+}
+
+.nombre {
+    font-weight: var(--weight-medium);
+}
+
+.saldo {
+    font-size: var(--font-xl);
+    font-weight: var(--weight-bold);
+}
+
+.helperText {
+    font-size: var(--font-xs);
+    color: var(--texto-sec);
 }
 
 .emptyState {
-    border: 1px dashed var(--card-border);
-    border-radius: 1rem;
-    padding: 1.5rem;
     display: grid;
     place-items: center;
-    gap: 0.65rem;
-    color: var(--texto-muted);
+    gap: var(--space-sm);
+    padding: var(--space-lg);
     text-align: center;
-}
+    color: var(--texto-sec);
+    border: 1px dashed var(--card-border);
+    border-radius: var(--radius-lg);
 
-.emptyState i {
-    font-size: 2.2rem;
-    color: var(--azul-200);
+    svg {
+        font-size: var(--font-3xl);
+        color: var(--azul-200);
+    }
 }
 
 .ventaCard {
-    border: 1px solid var(--card-border);
-    background: color-mix(in srgb, var(--card-bg) 94%, black);
-    border-radius: 1rem;
-    padding: 1rem;
     display: flex;
-    gap: 0.9rem;
     align-items: center;
+    gap: var(--space-sm);
     min-height: 128px;
+    padding: var(--space-md);
+    border: 1px solid var(--card-border);
+    border-radius: var(--radius-lg);
+    background: color-mix(in srgb, var(--card-bg) 94%, black);
 }
 
 .ventaImageWrap,
-.ventaBadge,
-.bolsilloAvatar {
-    width: 72px;
-    height: 72px;
-    border-radius: 1rem;
-    overflow: hidden;
+.ventaBadge {
     display: grid;
     place-items: center;
     flex-shrink: 0;
+    width: 72px;
+    height: 72px;
+    border-radius: var(--radius-lg);
+    overflow: hidden;
 }
 
 .ventaImageWrap {
     background: color-mix(in srgb, var(--azul-600) 20%, transparent);
 
-    i {
-        font-size: 2.3rem;
+    svg {
+        font-size: var(--font-3xl);
         color: var(--azul-200);
     }
 }
@@ -436,8 +404,8 @@
 .ventaBadge {
     background: linear-gradient(135deg, color-mix(in srgb, var(--rojo-600) 32%, transparent), color-mix(in srgb, var(--azul-600) 28%, transparent));
 
-    i {
-        font-size: 2rem;
+    svg {
+        font-size: var(--font-2xl);
         color: var(--texto);
     }
 }
@@ -446,15 +414,15 @@
     flex: 1;
     min-width: 0;
     display: grid;
-    gap: 0.35rem;
+    gap: var(--space-xs);
 
     strong {
-        font-size: 1rem;
+        font-size: var(--font-md);
     }
 
     span {
+        font-size: var(--font-sm);
         color: var(--texto-sec);
-        font-size: 0.9rem;
     }
 }
 
@@ -462,255 +430,67 @@
     display: flex;
     align-items: center;
     justify-content: space-between;
-    gap: 0.8rem;
+    gap: var(--space-sm);
     flex-wrap: wrap;
-    font-size: 0.92rem;
+    font-size: var(--font-sm);
     color: var(--texto-sec);
 }
 
-.panelHeader {
-    align-items: flex-start;
-}
-
-.summaryCard {
-    min-height: 110px;
-}
-
-.progressHeader strong,
-.previewBox span,
-.timelineBody strong,
-.bolsilloCard strong {
-    font-weight: 700;
-}
-
-.bolsilloCard {
-    position: relative;
-    overflow: hidden;
-    border-radius: 1.2rem;
-    padding: 1rem;
-    border: 1px solid var(--card-border);
-    background:
-        radial-gradient(circle at top right, color-mix(in srgb, var(--accent) 28%, transparent), transparent 34%),
-        color-mix(in srgb, var(--card-bg) 94%, black);
-    display: grid;
-    gap: 0.85rem;
-}
-
-.bolsilloAccent {
-    position: absolute;
-    inset: auto auto 0 0;
-    width: 100%;
-    height: 4px;
-    background: var(--accent);
-}
-
-.bolsilloIdentity {
-    display: flex;
-    align-items: center;
-    gap: 0.8rem;
-    min-width: 0;
-}
-
-.bolsilloAvatar {
-    background: var(--accent);
-    color: #fff;
-
-    i {
-        font-size: 1.8rem;
-    }
-}
-
-.bolsilloInfo {
-    min-width: 0;
-}
-
-.bolsilloName {
-    font-size: 1rem;
-}
-
-.bolsilloDesc {
-    font-size: 0.88rem;
-}
-
-.moneyRow {
-    display: grid;
-    gap: 0.15rem;
-}
-
-.moneyMain {
-    font-size: 1.45rem;
-}
-
-.moneyMeta {
-    font-size: 0.85rem;
-    color: var(--texto-muted);
-}
-
-.cardFooter {
-    align-items: flex-end;
-}
-
-.metaLabel {
-    display: block;
-    color: var(--texto-muted);
-    margin-bottom: 0.15rem;
-}
-
-.cardActions {
-    justify-content: flex-end;
-}
-
-.actionButton,
-.cancelButton {
-    background: transparent;
-    color: var(--texto);
-    border-color: var(--card-border);
-}
-
-.previewBox {
-    position: relative;
-    overflow: hidden;
-    border-radius: 1rem;
-    padding: 1rem;
-    min-height: 150px;
-    display: grid;
-    gap: 0.35rem;
-    align-content: start;
-    background:
-        radial-gradient(circle at top right, color-mix(in srgb, var(--accent) 40%, transparent), transparent 45%),
-        color-mix(in srgb, var(--card-bg) 92%, black);
-    border: 1px solid var(--card-border);
-}
-
-.previewBox i {
-    position: absolute;
-    right: 1rem;
-    bottom: 1rem;
-    font-size: 3rem;
-    color: color-mix(in srgb, var(--accent) 70%, white);
-    opacity: 0.9;
-}
-
-.formGrid {
-    display: grid;
-    gap: 1rem;
-}
-
-.fieldFull {
-    grid-column: 1 / -1;
-}
-
-.colorGrid,
-.iconGrid {
-    grid-template-columns: repeat(auto-fit, minmax(44px, 1fr));
-}
-
-.colorSwatch,
-.iconOption {
-    width: 44px;
-    height: 44px;
-    border-radius: 0.9rem;
-    border: 1px solid var(--card-border);
-    display: grid;
-    place-items: center;
-    color: #fff;
-    background: var(--card-bg);
-    cursor: pointer;
-}
-
-.colorSwatch i,
-.iconOption i {
-    font-size: 1.2rem;
-}
-
-.iconOption {
-    color: var(--texto);
-    background: color-mix(in srgb, var(--card-bg) 90%, black);
-}
-
-.isSelected {
-    transform: translateY(-2px) scale(1.03);
-    box-shadow: 0 0 0 2px color-mix(in srgb, var(--azul-200) 40%, transparent);
-}
-
-.alert {
-    padding: 0.9rem 1rem;
-    border-radius: 0.9rem;
-    background: var(--danger-soft);
-    color: #ffdede;
-    border: 1px solid color-mix(in srgb, var(--danger) 45%, transparent);
-}
-
 .modalBackdrop {
     position: fixed;
     inset: 0;
-    z-index: 2000;
-    background: rgba(0, 0, 0, 0.65);
+    z-index: var(--z-modal);
     display: grid;
     place-items: center;
-    padding: 1rem;
+    padding: var(--space-md);
+    background: var(--overlay);
+    backdrop-filter: blur(4px);
 }
 
 .modal {
     width: min(720px, 100%);
     max-height: 92vh;
     overflow-y: auto;
-    border-radius: 1.4rem;
-    padding: 1rem;
+    padding: var(--space-md);
     border: 1px solid var(--card-border);
+    border-radius: var(--radius-2xl);
+    box-shadow: var(--shadow-lg);
     background: linear-gradient(180deg, color-mix(in srgb, var(--card-bg) 94%, black), var(--fondo-elevado));
-    box-shadow: var(--shadow-1);
 }
 
 .modalTitle {
     margin: 0;
-    font-size: 1.25rem;
+    font-size: var(--font-xl);
 }
 
 .modalClose {
+    display: grid;
+    place-items: center;
     width: 44px;
     height: 44px;
     padding: 0;
-    display: grid;
-    place-items: center;
-    background: transparent;
     color: var(--texto);
+    background: transparent;
     border-color: var(--card-border);
 
-    i {
-        font-size: 1.2rem;
+    svg {
+        font-size: var(--font-xl);
     }
 }
 
-.timeline {
-    gap: 0.85rem;
-}
-
-.timelineItem {
-    display: flex;
-    align-items: flex-start;
-    gap: 0.75rem;
-}
-
-.timelineDot {
-    width: 12px;
-    height: 12px;
-    border-radius: 50%;
-    margin-top: 0.35rem;
-    background: var(--rojo-500);
-    box-shadow: 0 0 0 4px color-mix(in srgb, var(--rojo-500) 18%, transparent);
-    flex-shrink: 0;
+.formGrid {
+    display: grid;
+    gap: var(--space-md);
 }
 
-.timelineBody p {
-    margin: 0;
-    font-size: 0.9rem;
+.alert {
+    padding: var(--space-sm) var(--space-md);
+    color: var(--rojo-100);
+    background: var(--danger-soft);
+    border: 1px solid color-mix(in srgb, var(--danger) 45%, transparent);
+    border-radius: var(--radius-lg);
 }
 
-/* ===========================
-   Modal resultado cierre
-=========================== */
-
 .resultadoObservaciones {
     grid-column: 1 / -1;
     min-height: auto;
@@ -719,57 +499,53 @@
 .resultadoTexto {
     display: flex;
     flex-direction: column;
-    gap: .45rem;
-
-    margin-top: .35rem;
-    padding: 1rem;
-
-    border-radius: .85rem;
+    gap: var(--space-xs);
+    margin-top: var(--space-xs);
+    padding: var(--space-md);
     border: 1px solid var(--card-border);
+    border-radius: var(--radius-lg);
     background: color-mix(in srgb, var(--card-bg) 92%, black);
 }
 
 .resultadoLinea {
     margin: 0;
-    font-size: .95rem;
-    line-height: 1.5;
+    font-size: var(--font-sm);
+    line-height: var(--line-relaxed);
     color: var(--texto);
     word-break: break-word;
 }
 
 .resultadoLineaTitulo {
-    margin: .35rem 0 0;
-    font-weight: 700;
-    color: var(--azul-200);
+    margin: var(--space-xs) 0 0;
+    font-size: var(--font-xs);
+    font-weight: var(--weight-bold);
+    letter-spacing: var(--tracking-wide);
     text-transform: uppercase;
-    letter-spacing: .04em;
-    font-size: .85rem;
+    color: var(--azul-200);
 }
 
 .resultadoSeparador {
     height: 1px;
+    margin: var(--space-xxs) 0;
     background: var(--card-border);
-    margin: .25rem 0;
 }
 
 @media (max-width: 768px) {
     .container {
-        padding-top: 0.5rem;
+        padding-top: var(--space-sm);
     }
 
     .hero,
     .containerCart,
-    .panel,
     .modal {
-        padding: 0.95rem;
+        padding: var(--space-sm);
     }
 
     .range {
         grid-template-columns: 1fr;
     }
 
-    .gridVentas,
-    .listGrid {
+    .gridVentas {
         grid-template-columns: 1fr;
     }
 
@@ -777,68 +553,17 @@
         align-items: flex-start;
     }
 
-    .cardFooter,
     .modalFooter,
     .progressLegend {
         flex-direction: column;
         align-items: flex-start;
     }
 
-    .cardActions {
-        width: 100%;
-        justify-content: flex-start;
-    }
-
     .buttons button,
     .primaryButton,
     .secondaryButton,
-    .actionButton,
-    .dangerButton,
     .cancelButton,
     .saveButton {
         width: 100%;
     }
-}
-
-.sectionActions {
-    display: flex;
-    align-items: center;
-    justify-content: flex-end;
-    gap: 0.75rem;
-    flex-wrap: wrap;
-}
-
-.rangeActions {
-    display: flex;
-    align-items: end;
-    justify-content: flex-end;
-}
-
-.success {
-    padding: 0.9rem 1rem;
-    border-radius: 0.9rem;
-    background: var(--success-soft);
-    color: var(--success);
-    border: 1px solid color-mix(in srgb, var(--success) 45%, transparent);
-}
-.heroBadge svg,
-.statLabel svg,
-.summaryLabel svg,
-.emptyState svg,
-.modalClose svg,
-.buttons button svg,
-.primaryButton svg,
-.secondaryButton svg,
-.actionButton svg,
-.dangerButton svg,
-.cancelButton svg,
-.saveButton svg,
-.sectionTag svg,
-.total svg,
-.cardTopLine svg,
-.ventaBadge svg,
-.ventaImageWrap svg {
-    width: 1em;
-    height: 1em;
-    flex-shrink: 0;
-}
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Dependencias/Color.module.scss b/Frontend/src/assets/Css/Dependencias/Color.module.scss
new file mode 100644
index 0000000..2e405df
--- /dev/null
+++ b/Frontend/src/assets/Css/Dependencias/Color.module.scss
@@ -0,0 +1,13 @@
+.themeButton{
+    border: 1px solid var(--card-border);
+    background: none;
+    cursor: pointer;
+    padding: Var(--space-sm) Var(--space-md);
+    border-radius: var(--radius-sm);
+    transition: all 0.15s ease;
+}
+
+.themeButton:hover{
+    border-color: var(--input-focus-border);
+    box-shadow: 0 0 0 2px var(--input-focus-shadow);
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Dependencias/ImagePreview.module.scss b/Frontend/src/assets/Css/Dependencias/ImagePreview.module.scss
new file mode 100644
index 0000000..658d9ef
--- /dev/null
+++ b/Frontend/src/assets/Css/Dependencias/ImagePreview.module.scss
@@ -0,0 +1,36 @@
+.overlay {
+    width: 100%;
+    height: 100vh;
+    position: fixed;
+    inset: 0;
+    background: rgba(0,0,0,.9);
+    display: flex;
+    justify-content: center;
+    align-items: center;
+    z-index: var(--z-loader);
+    cursor: pointer;
+    animation: fadeIn var(--transition-base) ease;
+
+    img {
+        width: 90%;
+        height: 90%;
+        object-fit: contain;
+        border-radius: var(--radius-lg);
+        box-shadow: var(--shadow-xl);
+    }
+}
+
+i {
+    font-size: var(--font-3xl);
+    color: var(--azul-400);
+}
+
+@keyframes fadeIn {
+    from {
+        opacity: 0;
+    }
+
+    to {
+        opacity: 1;
+    }
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Dependencias/Paginacion.module.scss b/Frontend/src/assets/Css/Dependencias/Paginacion.module.scss
new file mode 100644
index 0000000..2f99d4e
--- /dev/null
+++ b/Frontend/src/assets/Css/Dependencias/Paginacion.module.scss
@@ -0,0 +1,260 @@
+/* ======================================================
+   PAGINACIÓN - CONTENEDOR PRINCIPAL
+   ====================================================== */
+
+.Paginacion {
+    display: flex;
+    flex-direction: column;
+    gap: var(--space-lg);
+    width: 100%;
+    padding: var(--space-lg);
+    background: linear-gradient(135deg, rgba(198, 40, 40, 0.05), rgba(43, 111, 163, 0.05));
+    border: var(--border-width) solid var(--card-border);
+    border-radius: var(--radius-lg);
+    box-shadow: var(--shadow-sm);
+}
+
+/* ======================================================
+   TÍTULO DE PAGINACIÓN
+   ====================================================== */
+
+.Paginacion h6 {
+    margin: 0;
+    font-size: var(--font-md);
+    font-weight: var(--weight-semibold);
+    color: var(--texto);
+    text-transform: uppercase;
+    letter-spacing: var(--tracking-wide);
+    text-align: center;
+}
+
+/* ======================================================
+   CONTENEDOR DE BOTONES
+   ====================================================== */
+
+.PaginacionBotones {
+    display: flex;
+    gap: var(--space-md);
+    justify-content: center;
+    align-items: center;
+    flex-wrap: wrap;
+}
+
+/* ======================================================
+   BOTONES DE NAVEGACIÓN
+   ====================================================== */
+
+.Paginacion button {
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    width: 40px;
+    height: 40px;
+    padding: 0;
+    background: var(--btn-secondary-bg);
+    color: #fff;
+    border: var(--border-width) solid var(--btn-secondary-bg);
+    border-radius: var(--radius-md);
+    font-size: var(--font-md);
+    font-weight: var(--weight-semibold);
+    cursor: pointer;
+    transition:
+        background var(--transition-fast),
+        border-color var(--transition-fast),
+        transform var(--transition-fast),
+        box-shadow var(--transition-fast);
+    box-shadow: var(--shadow-sm);
+
+    i {
+        font-size: var(--font-lg);
+    }
+
+    &:hover:not(:disabled) {
+        background: var(--btn-secondary-bg-hover);
+        border-color: var(--btn-secondary-bg-hover);
+        transform: translateY(-2px);
+        box-shadow: var(--shadow-md);
+    }
+
+    &:active:not(:disabled) {
+        transform: translateY(0);
+    }
+
+    &:disabled {
+        background: var(--card-border);
+        border-color: var(--card-border);
+        color: var(--texto-sec);
+        opacity: var(--opacity-disabled);
+        cursor: not-allowed;
+        box-shadow: none;
+    }
+
+    &:focus {
+        outline: none;
+        box-shadow: var(--focus-ring);
+    }
+}
+
+/* ======================================================
+   TEXTO DE PAGINACIÓN
+   ====================================================== */
+
+.PaginacionBotones span {
+    font-size: var(--font-md);
+    font-weight: var(--weight-semibold);
+    color: var(--texto);
+    padding: var(--space-sm) var(--space-md);
+    background: rgba(198, 40, 40, 0.08);
+    border-radius: var(--radius-md);
+    border: var(--border-width) solid var(--card-border);
+    min-width: 90px;
+    text-align: center;
+}
+
+/* ======================================================
+   SELECT DE FILAS POR PÁGINA
+   ====================================================== */
+
+.Paginacion select {
+    width: 100%;
+    padding: var(--space-md);
+    background: var(--input-bg);
+    color: var(--texto);
+    border: var(--border-width) solid var(--input-border);
+    border-radius: var(--radius-md);
+    font-size: var(--font-md);
+    font-weight: var(--weight-medium);
+    font-family: inherit;
+    cursor: pointer;
+    transition:
+        border-color var(--transition-fast),
+        box-shadow var(--transition-fast),
+        background var(--transition-fast);
+
+    &:hover {
+        border-color: var(--rojo-700);
+    }
+
+    &:focus {
+        outline: none;
+        border-color: var(--rojo-500);
+        box-shadow: var(--focus-ring);
+        background: rgba(198, 40, 40, 0.05);
+    }
+
+    option {
+        background: var(--fondo-elevado);
+        color: var(--texto);
+        padding: var(--space-md);
+    }
+}
+
+/* ======================================================
+   RESPONSIVE - TABLET
+   ====================================================== */
+
+@media (max-width: 768px) {
+    .Paginacion {
+        padding: var(--space-md);
+        gap: var(--space-md);
+    }
+
+    .Paginacion h6 {
+        font-size: var(--font-sm);
+    }
+
+    .PaginacionBotones {
+        gap: var(--space-sm);
+    }
+
+    .Paginacion button {
+        width: 36px;
+        height: 36px;
+        font-size: var(--font-md);
+
+        i {
+            font-size: var(--font-md);
+        }
+    }
+
+    .PaginacionBotones span {
+        font-size: var(--font-sm);
+        padding: var(--space-sm);
+        min-width: 80px;
+    }
+
+    .Paginacion select {
+        max-width: 100%;
+        font-size: var(--font-sm);
+        padding: var(--space-sm) var(--space-md);
+    }
+}
+
+/* ======================================================
+   RESPONSIVE - MÓVIL
+   ====================================================== */
+
+@media (max-width: 576px) {
+    .Paginacion {
+        padding: var(--space-md);
+        gap: var(--space-md);
+    }
+
+    .PaginacionBotones {
+        gap: var(--space-xs);
+        width: 100%;
+    }
+
+    .Paginacion button {
+        width: 32px;
+        height: 32px;
+        font-size: var(--font-sm);
+
+        i {
+            font-size: var(--font-sm);
+        }
+    }
+
+    .PaginacionBotones span {
+        font-size: var(--font-xs);
+        padding: var(--space-xs) var(--space-sm);
+        min-width: 70px;
+    }
+
+    .Paginacion select {
+        width: 100%;
+        max-width: unset;
+        font-size: var(--font-sm);
+    }
+
+    .Paginacion h6 {
+        font-size: var(--font-xs);
+    }
+}
+
+/* ======================================================
+   ESTADO DESHABILITADO
+   ====================================================== */
+
+.Paginacion button:disabled {
+    cursor: not-allowed;
+
+    &:hover {
+        transform: none;
+    }
+}
+
+/* ======================================================
+   ACCESIBILIDAD
+   ====================================================== */
+
+.Paginacion button:focus-visible,
+.Paginacion select:focus-visible {
+    outline: 2px solid var(--rojo-500);
+    outline-offset: 2px;
+}
+
+.Paginacion select:disabled {
+    opacity: var(--opacity-disabled);
+    cursor: not-allowed;
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Dependencias/filtro.module.scss b/Frontend/src/assets/Css/Dependencias/filtro.module.scss
new file mode 100644
index 0000000..8496c87
--- /dev/null
+++ b/Frontend/src/assets/Css/Dependencias/filtro.module.scss
@@ -0,0 +1,270 @@
+/* ======================================================
+   FILTRO - CONTENEDOR PRINCIPAL
+   ====================================================== */
+
+.Item {
+    display: flex;
+    flex-direction: column;
+    gap: var(--space-lg);
+    width: 100%;
+}
+
+/* ======================================================
+   FILTROS - SECCIÓN PRINCIPAL
+   ====================================================== */
+
+.Filtros {
+    position: sticky;
+    top: 120px;
+    width: 100%;
+    max-height: max-content;
+    display: flex;
+    flex-wrap: wrap;
+    gap: var(--space-md);
+    padding: var(--space-lg);
+    background: var(--card-bg);
+    border: var(--border-width) solid var(--card-border);
+    border-radius: var(--radius-lg);
+    box-shadow: var(--shadow-md);
+    backdrop-filter: blur(8px);
+    z-index: var(--z-sticky);
+    animation: slideDown var(--transition-base) ease;
+}
+
+/* ======================================================
+   LABEL - CAMPO DE FILTRO
+   ====================================================== */
+
+.Filtros label {
+    display: flex;
+    flex-direction: column;
+    gap: var(--space-sm);
+    flex: 1;
+    min-width: 180px;
+    font-size: var(--font-xs);
+    font-weight: var(--weight-semibold);
+    color: var(--texto);
+    text-transform: uppercase;
+    letter-spacing: var(--tracking-wide);
+}
+
+/* ======================================================
+   INPUTS Y SELECTS EN FILTRO
+   ====================================================== */
+
+.Filtros input,
+.Filtros select {
+    width: 100%;
+    padding: var(--space-md) var(--space-md);
+    background: var(--input-bg);
+    color: var(--texto);
+    border: var(--border-width) solid var(--input-border);
+    border-radius: var(--radius-md);
+    font-size: var(--font-md);
+    font-family: inherit;
+    transition:
+        border-color var(--transition-fast),
+        box-shadow var(--transition-fast),
+        background var(--transition-fast);
+
+    &::placeholder {
+        color: var(--placeholder);
+    }
+
+    &:hover {
+        border-color: var(--rojo-700);
+    }
+
+    &:focus {
+        outline: none;
+        border-color: var(--rojo-500);
+        box-shadow: var(--focus-ring);
+        background: rgba(198, 40, 40, 0.05);
+    }
+}
+
+.Filtros select {
+    cursor: pointer;
+
+    option {
+        background: var(--fondo-elevado);
+        color: var(--texto);
+    }
+}
+
+/* ======================================================
+   REACT-SELECT PERSONALIZADO EN FILTRO
+   ====================================================== */
+
+.Filtros :global(.react-select__control) {
+    background: var(--input-bg);
+    border-color: var(--input-border);
+    border-radius: var(--radius-md);
+    padding: 0.2rem;
+    min-height: 42px;
+    transition:
+        border-color var(--transition-fast),
+        box-shadow var(--transition-fast);
+
+    &:hover {
+        border-color: var(--rojo-700);
+    }
+
+    &:is(.react-select__control--is-focused) {
+        border-color: var(--rojo-500);
+        box-shadow: var(--focus-ring);
+    }
+}
+
+.Filtros :global(.react-select__menu) {
+    background: var(--input-bg);
+    border: var(--border-width) solid var(--input-border);
+    border-radius: var(--radius-md);
+    box-shadow: var(--shadow-md);
+    z-index: 100;
+}
+
+.Filtros :global(.react-select__option) {
+    background: var(--input-bg);
+    color: var(--texto);
+    cursor: pointer;
+    transition: background var(--transition-fast);
+
+    &:is(.react-select__option--is-focused) {
+        background: rgba(198, 40, 40, 0.15);
+    }
+
+    &:is(.react-select__option--is-selected) {
+        background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
+        color: #fff;
+    }
+}
+
+.Filtros :global(.react-select__single-value) {
+    color: var(--texto);
+}
+
+.Filtros :global(.react-select__placeholder) {
+    color: var(--placeholder);
+}
+
+.Filtros :global(.react-select__input-container) {
+    color: var(--texto);
+}
+
+.Filtros :global(.react-select__dropdown-indicator) {
+    color: var(--texto-sec);
+    transition: color var(--transition-fast);
+
+    &:hover {
+        color: var(--rojo-500);
+    }
+}
+
+.Filtros :global(.react-select__clear-indicator) {
+    color: var(--texto-sec);
+    transition: color var(--transition-fast);
+
+    &:hover {
+        color: var(--rojo-500);
+    }
+}
+
+/* ======================================================
+   CONTENEDOR DE PAGINACIÓN Y BOTONES
+   ====================================================== */
+
+.Filtros > * {
+    flex: 1;
+    min-width: 100%;
+}
+
+/* ======================================================
+   ANIMACIONES
+   ====================================================== */
+
+@keyframes slideDown {
+    from {
+        opacity: 0;
+        transform: translateY(-10px);
+    }
+    to {
+        opacity: 1;
+        transform: translateY(0);
+    }
+}
+
+/* ======================================================
+   RESPONSIVE - TABLET
+   ====================================================== */
+
+@media (max-width: 1024px) {
+    .Filtros {
+        position: static;
+        gap: var(--space-md);
+    }
+
+    .Filtros label {
+        flex: 1 1 calc(50% - var(--space-md));
+        min-width: 150px;
+    }
+}
+
+/* ======================================================
+   RESPONSIVE - MÓVIL
+   ====================================================== */
+
+@media (max-width: 768px) {
+    .Filtros {
+        flex-direction: column;
+        padding: var(--space-md);
+        gap: var(--space-md);
+    }
+
+    .Filtros label {
+        flex: 1 1 100%;
+        min-width: unset;
+    }
+
+    .Filtros input,
+    .Filtros select {
+        padding: var(--space-md);
+        font-size: var(--font-sm);
+    }
+
+    .Filtros > * {
+        width: 100%;
+    }
+}
+
+@media (max-width: 576px) {
+    .Filtros {
+        padding: var(--space-md);
+        gap: var(--space-sm);
+    }
+
+    .Filtros label {
+        font-size: var(--font-xs);
+    }
+
+    .Filtros input,
+    .Filtros select {
+        padding: var(--space-sm) var(--space-md);
+        font-size: var(--font-sm);
+    }
+}
+
+/* ======================================================
+   ACCESIBILIDAD
+   ====================================================== */
+
+.Filtros input:disabled,
+.Filtros select:disabled {
+    opacity: var(--opacity-disabled);
+    cursor: not-allowed;
+}
+
+.Filtros label:has(input:invalid),
+.Filtros label:has(select:invalid) {
+    --input-border: var(--danger);
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Inicio.module.scss b/Frontend/src/assets/Css/Inicio.module.scss
deleted file mode 100644
index 4fa216a..0000000
--- a/Frontend/src/assets/Css/Inicio.module.scss
+++ /dev/null
@@ -1,103 +0,0 @@
-.ItemContainer{
-    width: 100%;
-    display: flex;
-    flex-direction: column;
-    justify-content: center;
-    align-items: center;
-    text-align: center;
-    gap: 1rem;
-
-    h2{
-        width: 100%;
-        text-align: center;
-        border-bottom: 1px solid var(--texto);
-        padding-bottom: 0.5rem;
-    }
-}
-.GrupoIconos{
-    display: flex;
-    gap: 3rem;
-}
-.GrupoBotones{
-    padding: 1rem 2rem;
-    background: var(--rojo-200);
-    border-radius: var(--radius-md);
-
-    a{
-        display: flex;
-        flex-direction: column;
-        text-align: center;
-    }
-
-    i{
-        font-size: 1.5rem;
-    }
-}
-
-.TareaContainer{
-    width: 100%;
-    display: flex;
-    flex-wrap: wrap;
-    justify-content: center;
-    align-items: center;
-    gap: 1rem;
-
-    h3{
-        width: 100%;
-        text-align: center;
-        border-bottom: 1px solid var(--texto);
-        padding-bottom: 0.5rem;
-        margin-top: 1rem;
-    }
-}
-
-.Tarea {
-    width: 100%;
-    max-width: 250px;
-    height: 200px;
-    background: var(--card-bg);
-    border: 1px solid var(--card-border);
-    color: var(--texto);
-    border-radius: var(--radius-md);
-    box-shadow: var(--shadow-2);
-    padding: 1rem;
-    display: flex;
-    flex-direction: column;
-    gap: 1rem;
-    position: relative;
-    z-index: 1;
-    overflow: hidden;
-
-    a {
-        color: var(--texto);
-        text-decoration: none;
-        display: flex;
-        flex-direction: column;
-        gap: 1rem;
-        padding: 1rem 0;
-        border-radius: var(--radius-md);
-        box-shadow: var(--shadow-1);
-    }
-    
-    i {
-        color: var(--azul-500);
-        font-size: 3rem;
-    }
-}
-
-.Tarea::before {
-    content: "";
-    position: absolute;
-    top: 0;
-    left: 0;
-    width: 0%;
-    height: 100%;
-    border-radius: var(--radius-md);
-    background: var(--gradient-alt);
-    transition: width var(--transition-fast);
-    z-index: -1;
-}
-
-.Tarea:hover::before {
-    width: 100%;
-}
diff --git a/Frontend/src/assets/Css/PerfilPersonal.module.scss b/Frontend/src/assets/Css/PerfilPersonal.module.scss
index 2226574..31ca6c2 100644
--- a/Frontend/src/assets/Css/PerfilPersonal.module.scss
+++ b/Frontend/src/assets/Css/PerfilPersonal.module.scss
@@ -1,131 +1,142 @@
-.PerfilContainer {
+.perfil-container {
     display: flex;
     justify-content: center;
     align-items: center;
     animation: fadeIn var(--transition-fast);
 }
 
-.ImagenSection {
-    padding: 15px 30px;
+.container {
+    width: 100%;
+    position: relative;
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    gap: var(--space-md);
+}
+
+.title {
+    color: var(--texto);
+    font-size: var(--font-2xl);
+    font-weight: var(--weight-bold);
+}
+
+.imagen-section {
     width: 90%;
     display: flex;
     flex-direction: column;
     align-items: center;
-    gap: 15px;
+    gap: var(--space-md);
+    padding: var(--space-md) var(--space-xl);
 
-    i{
+    i {
         display: flex;
         justify-content: center;
         align-items: center;
     }
 
     i,
-    .ImagenPerfil {
-        font-size: 100px;
+    .imagen-perfil {
         width: 120px;
         height: 120px;
-        border-radius: 50%;
-        border: 3px solid var(--rojo-500);
+        font-size: 100px;
+        color: var(--rojo-500);
+        border: var(--border-width-lg) solid var(--rojo-500);
+        border-radius: var(--radius-full);
         object-fit: cover;
-        box-shadow: var(--shadow-2);
-        transition: transform var(--transition-fast);
         overflow: hidden;
-        color: var(--rojo-500);
+        box-shadow: var(--shadow-sm);
+        transition: transform var(--transition-fast);
     }
 
-    .BtnSubir {
+    .btn-subir {
         position: relative;
         overflow: hidden;
         border-radius: var(--radius-md);
+        font-weight: var(--weight-medium);
         cursor: pointer;
         transition: background-color var(--transition-fast);
-        font-weight: 500;
 
         &:hover {
-            background-color: var(--btn-terciario-bg-hover);
+            background-color: var(--btn-secondary-bg-hover);
         }
 
         input {
             position: absolute;
-            top: 0;
-            left: 0;
+            inset: 0;
             opacity: 0;
-            width: 100%;
-            height: 100%;
             cursor: pointer;
         }
     }
 }
 
-.Datos,
-.PasswordSection {
-    padding: 15px 30px;
+.datos,
+.password-section {
     width: 90%;
     display: flex;
     flex-direction: column;
-    gap: 15px;
+    gap: var(--space-md);
+    padding: var(--space-md) var(--space-xl);
 
     h3 {
-        font-size: 20px;
-        color: var(--rojo-200);
+        color: var(--texto);
+        font-size: var(--font-xl);
     }
 
-    .Grupo {
-        display: flex;
-        flex-direction: column;
-        gap: 5px;
+    button {
+        align-self: flex-end;
+        margin-top: var(--space-sm);
+    }
+}
 
-        label {
-            font-size: 14px;
-            color: var(--texto-sec);
-        }
+.grupo {
+    display: flex;
+    flex-direction: column;
+    gap: var(--space-xs);
 
-        input {
-            background-color: var(--input-bg);
-            border: 1px solid var(--input-border);
-            color: var(--input-text);
-            border-radius: var(--radius-sm);
-            padding: 10px;
-            outline: none;
-            transition: border-color var(--transition-fast);
-
-            &:focus {
-                border-color: var(--input-focus-border);
-            }
-        }
+    label {
+        color: var(--texto-sec);
+        font-size: var(--font-sm);
     }
 
-    button {
-        align-self: flex-end;
-        margin-top: 10px;
+    input {
+        padding: var(--space-sm);
+        color: var(--texto);
+        background-color: var(--input-bg);
+        border: var(--border-width) solid var(--input-border);
+        border-radius: var(--radius-sm);
+        outline: none;
+        transition: border-color var(--transition-fast);
+
+        &:focus {
+            border-color: var(--rojo-500);
+        }
     }
 }
 
-.InputPassword {
+.input-password {
     position: relative;
     display: flex;
     align-items: center;
 
     input {
         width: 100%;
-        padding-right: 2.5rem;
+        padding-right: var(--space-2xl);
     }
 
     i {
         position: absolute;
-        right: 10px;
-        cursor: pointer;
+        right: var(--space-sm);
         color: var(--texto);
-        font-size: 1.3rem;
-        transition: 0.2s;
+        font-size: var(--font-xl);
+        cursor: pointer;
+        transition: color var(--transition-fast);
 
         &:hover {
-            color: var(--primario);
+            color: var(--rojo-400);
         }
     }
 }
 
-
 @keyframes fadeIn {
     from { opacity: 0; }
     to { opacity: 1; }
@@ -134,4 +145,4 @@
 @keyframes floatIn {
     0% { transform: translateY(30px); opacity: 0; }
     100% { transform: translateY(0); opacity: 1; }
-}
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Pos/mesas.module.scss b/Frontend/src/assets/Css/Pos/mesas.module.scss
index 7490fb1..ac4cb28 100644
--- a/Frontend/src/assets/Css/Pos/mesas.module.scss
+++ b/Frontend/src/assets/Css/Pos/mesas.module.scss
@@ -15,7 +15,7 @@
     border-radius: var(--radius-sm);
     border: 1px solid var(--pos-divider);
     background-color: var(--pos-mesa-inactiva-bg);
-    color: var(--texto);
+    color: var(--azul-100);
     font-weight: 600;
     cursor: pointer;
     transition: var(--transition-fast);
@@ -23,6 +23,7 @@
 
 .mesa:hover {
     background-color: var(--pos-item-hover);
+    color: var(--texto);
 }
 
 /* =========================
@@ -59,7 +60,7 @@
     @extend .mesa;
     background-color: var(--pos-mesa-activa-bg);
     border-color: var(--pos-mesa-activa-bg);
-    color: var(--btn-primary-text);
+    color: var(--azul-100);
 }
 
 /* =========================
@@ -95,6 +96,7 @@
 
         &:hover {
             background-color: var(--pos-item-hover);
+            color: var(--texto);
         }
 
         &:active {
diff --git a/Frontend/src/assets/Css/Principales/Auth.module.scss b/Frontend/src/assets/Css/Principales/Auth.module.scss
new file mode 100644
index 0000000..ea3cbbb
--- /dev/null
+++ b/Frontend/src/assets/Css/Principales/Auth.module.scss
@@ -0,0 +1,404 @@
+/* ======================================================
+   AUTH FORMS (Login & Register)
+   ====================================================== */
+
+.authMain {
+    width: 100%;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    padding: var(--space-lg) var(--space-md);
+    animation: fadeIn var(--transition-base) ease;
+}
+
+.authContainer {
+    width: 100%;
+    max-width: 450px;
+}
+
+.authCard {
+    background: var(--card-bg);
+    border: var(--border-width) solid var(--card-border);
+    border-radius: var(--radius-lg);
+    padding: var(--space-2xl) var(--space-lg);
+    box-shadow: var(--shadow-lg);
+    animation: fadeUp var(--transition-base) ease;
+}
+
+.authTitle {
+    font-size: var(--font-2xl);
+    font-weight: var(--weight-bold);
+    text-align: center;
+    color: var(--texto);
+    margin-bottom: var(--space-2xl);
+    text-transform: capitalize;
+}
+
+.authForm {
+    display: flex;
+    flex-direction: column;
+    gap: var(--space-lg);
+}
+
+/* ======================================================
+   FORM GROUPS
+   ====================================================== */
+
+.formGroup {
+    display: flex;
+    flex-direction: column;
+    gap: var(--space-sm);
+}
+
+.formLabel {
+    font-size: var(--font-sm);
+    font-weight: var(--weight-semibold);
+    color: var(--texto);
+    display: flex;
+    align-items: center;
+    gap: var(--space-xs);
+    cursor: default;
+
+    i {
+        font-size: var(--font-md);
+        color: var(--rojo-500);
+    }
+}
+
+.formInput,
+.formInputPassword {
+    width: 100%;
+    padding: var(--space-md) var(--space-md);
+    background: var(--input-bg);
+    color: var(--texto);
+    border: var(--border-width) solid var(--input-border);
+    border-radius: var(--radius-md);
+    font-size: var(--font-md);
+    font-family: inherit;
+    transition:
+        border-color var(--transition-fast),
+        box-shadow var(--transition-fast),
+        background var(--transition-fast);
+
+    &::placeholder {
+        color: var(--placeholder);
+    }
+
+    &:hover {
+        border-color: var(--rojo-700);
+    }
+
+    &:focus {
+        outline: none;
+        border-color: var(--rojo-500);
+        box-shadow: var(--focus-ring);
+        background: rgba(198, 40, 40, 0.05);
+    }
+
+    &:disabled {
+        opacity: var(--opacity-disabled);
+        cursor: not-allowed;
+    }
+}
+
+.inputGroup {
+    display: flex;
+    gap: 0;
+    align-items: center;
+}
+
+.inputGroup .formInputPassword {
+    border-radius: var(--radius-md) 0 0 var(--radius-md);
+    margin-bottom: 0;
+}
+
+.togglePasswordBtn {
+    padding: var(--space-md);
+    background: var(--input-bg);
+    border: var(--border-width) solid var(--input-border);
+    border-left: none;
+    border-radius: 0 var(--radius-md) var(--radius-md) 0;
+    color: var(--texto);
+    cursor: pointer;
+    transition:
+        border-color var(--transition-fast),
+        background var(--transition-fast);
+    display: flex;
+    align-items: center;
+    justify-content: center;
+
+    i {
+        font-size: var(--font-md);
+    }
+
+    &:hover {
+        border-color: var(--rojo-700);
+        background: rgba(198, 40, 40, 0.05);
+    }
+
+    &:focus {
+        outline: none;
+        border-color: var(--rojo-500);
+    }
+}
+
+/* ======================================================
+   PROFILE PICTURE UPLOAD
+   ====================================================== */
+
+.profileUploadGroup {
+    display: flex;
+    justify-content: center;
+    margin-bottom: var(--space-lg);
+}
+
+.profileUploadLabel {
+    cursor: pointer;
+    width: 150px;
+    height: 150px;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    border-radius: var(--radius-full);
+    border: 2px dashed var(--card-border);
+    transition:
+        border-color var(--transition-fast),
+        background var(--transition-fast),
+        transform var(--transition-fast);
+    background: rgba(198, 40, 40, 0.05);
+
+    &:hover {
+        border-color: var(--rojo-500);
+        background: rgba(198, 40, 40, 0.1);
+        transform: scale(1.05);
+    }
+}
+
+.profileImage {
+    width: 150px;
+    height: 150px;
+    object-fit: cover;
+    border-radius: var(--radius-full);
+    border: 3px solid var(--rojo-500);
+    box-shadow: var(--shadow-md);
+}
+
+.profilePlaceholder {
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    justify-content: center;
+    gap: var(--space-sm);
+    color: var(--texto-sec);
+    text-align: center;
+
+    i {
+        font-size: var(--font-3xl);
+        color: var(--azul-400);
+    }
+
+    p {
+        font-size: var(--font-sm);
+        font-weight: var(--weight-medium);
+        margin: 0;
+    }
+}
+
+.profileFileInput {
+    display: none;
+}
+
+/* ======================================================
+   SUBMIT BUTTON
+   ====================================================== */
+
+.submitBtn {
+    width: 100%;
+    padding: var(--space-md) var(--space-lg);
+    background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
+    color: #fff;
+    border: none;
+    border-radius: var(--radius-md);
+    font-size: var(--font-md);
+    font-weight: var(--weight-semibold);
+    cursor: pointer;
+    transition:
+        transform var(--transition-fast),
+        box-shadow var(--transition-fast),
+        opacity var(--transition-fast);
+    text-transform: uppercase;
+    letter-spacing: var(--tracking-wide);
+
+    &:hover:not(:disabled) {
+        transform: translateY(-2px);
+        box-shadow: var(--shadow-lg);
+    }
+
+    &:active:not(:disabled) {
+        transform: translateY(0);
+    }
+
+    &:disabled {
+        opacity: var(--opacity-disabled);
+        cursor: not-allowed;
+    }
+
+    i {
+        margin-right: var(--space-xs);
+    }
+}
+
+/* ======================================================
+   ERROR MESSAGES
+   ====================================================== */
+
+.errorAlert {
+    padding: var(--space-md);
+    background: rgba(198, 40, 40, 0.1);
+    border: var(--border-width) solid var(--danger);
+    border-radius: var(--radius-md);
+    color: var(--danger);
+    font-size: var(--font-sm);
+    font-weight: var(--weight-medium);
+    display: flex;
+    align-items: center;
+    gap: var(--space-sm);
+    margin-bottom: var(--space-lg);
+    animation: slideDown var(--transition-base) ease;
+
+    i {
+        font-size: var(--font-md);
+        flex-shrink: 0;
+    }
+}
+
+/* ======================================================
+   ANIMATIONS
+   ====================================================== */
+
+@keyframes slideDown {
+    from {
+        opacity: 0;
+        transform: translateY(-10px);
+    }
+    to {
+        opacity: 1;
+        transform: translateY(0);
+    }
+}
+
+@keyframes fadeUp {
+    from {
+        opacity: 0;
+        transform: translateY(15px);
+    }
+    to {
+        opacity: 1;
+        transform: translateY(0);
+    }
+}
+
+@keyframes fadeIn {
+    from {
+        opacity: 0;
+    }
+    to {
+        opacity: 1;
+    }
+}
+
+/* ======================================================
+   RESPONSIVE DESIGN
+   ====================================================== */
+
+@media (max-width: 768px) {
+    .authCard {
+        padding: var(--space-xl) var(--space-lg);
+    }
+
+    .authTitle {
+        font-size: var(--font-xl);
+        margin-bottom: var(--space-lg);
+    }
+
+    .authForm {
+        gap: var(--space-md);
+    }
+
+    .formGroup {
+        gap: var(--space-xs);
+    }
+
+    .profileUploadLabel {
+        width: 120px;
+        height: 120px;
+    }
+
+    .profileImage {
+        width: 120px;
+        height: 120px;
+        border-width: 2px;
+    }
+
+    .profilePlaceholder i {
+        font-size: var(--font-2xl);
+    }
+}
+
+@media (max-width: 576px) {
+    .authMain {
+        padding: var(--space-md) var(--space-sm);
+        min-height: auto;
+    }
+
+    .authCard {
+        padding: var(--space-lg) var(--space-md);
+        border-radius: var(--radius-md);
+    }
+
+    .authTitle {
+        font-size: var(--font-lg);
+        margin-bottom: var(--space-lg);
+    }
+
+    .authForm {
+        gap: var(--space-md);
+    }
+
+    .formLabel {
+        font-size: var(--font-xs);
+    }
+
+    .formInput,
+    .formInputPassword {
+        padding: var(--space-sm) var(--space-md);
+        font-size: var(--font-sm);
+    }
+
+    .togglePasswordBtn {
+        padding: var(--space-sm);
+    }
+
+    .submitBtn {
+        padding: var(--space-md) var(--space-md);
+        font-size: var(--font-sm);
+    }
+
+    .profileUploadLabel {
+        width: 100px;
+        height: 100px;
+    }
+
+    .profileImage {
+        width: 100px;
+        height: 100px;
+    }
+
+    .profilePlaceholder i {
+        font-size: var(--font-xl);
+    }
+
+    .profilePlaceholder p {
+        font-size: var(--font-xs);
+    }
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Error404.module.scss b/Frontend/src/assets/Css/Principales/Error404.module.scss
similarity index 100%
rename from Frontend/src/assets/Css/Error404.module.scss
rename to Frontend/src/assets/Css/Principales/Error404.module.scss
diff --git a/Frontend/src/assets/Css/Principales/Global.Claro.scss b/Frontend/src/assets/Css/Principales/Global.Claro.scss
new file mode 100644
index 0000000..66c0c62
--- /dev/null
+++ b/Frontend/src/assets/Css/Principales/Global.Claro.scss
@@ -0,0 +1,65 @@
+﻿body[data-theme="claro"] {
+    --rojo-900: #3d0e0e;
+    --rojo-800: #5b1d1d;
+    --rojo-700: #7b2323;
+    --rojo-600: #c62828;
+    --rojo-500: #e53935;
+    --rojo-400: #f44336;
+    --rojo-300: #f57373;
+    --rojo-200: #ffcaca;
+    --rojo-100: #fff0f0;
+
+    --azul-900: #1a3a52;
+    --azul-800: #22465f;
+    --azul-700: #2d5975;
+    --azul-600: #3d7ca6;
+    --azul-500: #5197c0;
+    --azul-400: #6eadd3;
+    --azul-300: #91c5e0;
+    --azul-200: #c5dff0;
+    --azul-100: #e8f4fb;
+
+    --fondo: linear-gradient(135deg, #f5f2f2 0%, #f0f7fb 100%);
+    --fondo-elevado: #ffffff;
+    --superficie: #fafafa;
+    --texto: #1a1616;
+    --texto-negro: #1a1616;
+    --texto-sec: #4a4040;
+    --placeholder: #9a8f8f;
+    --input-bg: #ffffff;
+    --input-border: #d4c9c9;
+    --card-bg: #fefefe;
+    --card-border: #e0d6d6;
+    --header-bg: #dfb0b0;
+    --footer-bg: #b5bbcf;
+    --btn-primary-bg: #c62828;
+    --btn-primary-bg-hover: #e53935;
+    --btn-primary-bg-active: #a02020;
+    --btn-secondary-bg: #2d5975;
+    --btn-secondary-bg-hover: #3d7ca6;
+    --btn-secondary-bg-active: #1a3a52;
+    --focus-ring:
+        0 0 0 2px rgba(255, 255, 255, 0.95),
+        0 0 0 4px rgba(198, 40, 40, 0.35);
+    --scrollbar-track: #f0f0f0;
+    --scrollbar-thumb: #c62828;
+    --scrollbar-thumb-hover: #9b1f1f;
+    --glass-bg: rgba(255, 255, 255, 0.70);
+    --glass-border: rgba(0, 0, 0, 0.10);
+    --glass-hover: rgba(255, 255, 255, 0.90);
+    --overlay: rgba(0, 0, 0, 0.50);
+    --overlay-soft: rgba(0, 0, 0, 0.25);
+    --pos-item-bg: var(--card-bg);
+    --pos-item-hover: rgba(198, 40, 40, 0.322);
+    --pos-item-active: var(--rojo-600);
+    --pos-divider: var(--card-border);
+    --pos-total-bg: #fff3f3;
+    --pos-total-text: var(--texto);
+    --pos-badge-bg: var(--azul-600);
+    --pos-mesa-activa-bg: var(--rojo-600);
+    --pos-mesa-inactiva-bg: var(--azul-700);
+    --pos-row-header: #fafafa;
+    --modal-dark-bg: var(--fondo-elevado);
+    --modal-dark-text: var(--texto);
+    --modal-dark-divider: var(--card-border);
+}
diff --git a/Frontend/src/assets/Css/Principales/Global.Oscuro.scss b/Frontend/src/assets/Css/Principales/Global.Oscuro.scss
new file mode 100644
index 0000000..c217e5c
--- /dev/null
+++ b/Frontend/src/assets/Css/Principales/Global.Oscuro.scss
@@ -0,0 +1,71 @@
+﻿/* ======================================================
+   THEME TOKENS
+   ====================================================== */
+
+:root,
+body:not([data-theme]),
+body[data-theme="oscuro"]{
+    --rojo-900:#2b0000;
+    --rojo-800:#420a0a;
+    --rojo-700:#6d1414;
+    --rojo-600:#9b1f1f;
+    --rojo-500:#c62828;
+    --rojo-400:#e53935;
+    --rojo-300:#f57373;
+    --rojo-200:#ffabab;
+    --rojo-100:#ffdede;
+
+    --azul-900:#0a1a26;
+    --azul-800:#0f2536;
+    --azul-700:#15354c;
+    --azul-600:#1e4a6a;
+    --azul-500:#2b6fa3;
+    --azul-400:#3d8bbd;
+    --azul-300:#5fa8d3;
+    --azul-200:#91c9e8;
+    --azul-100:#cfeaf7;
+
+    --fondo:linear-gradient(var(--rojo-900),var(--azul-900));
+    --fondo-elevado:#121012;
+    --superficie:#372525;
+    --texto:#f5f3f3;
+    --texto-negro: #1a1616;
+    --texto-sec:#cfc9c9;
+    --placeholder:#7e7373;
+    --header-bg:var(--rojo-800);
+    --footer-bg:var(--azul-800);
+    --btn-primary-bg:var(--rojo-600);
+    --btn-primary-bg-hover:var(--rojo-500);
+    --btn-primary-bg-active:var(--rojo-700);
+    --btn-secondary-bg:var(--azul-600);
+    --btn-secondary-bg-hover:var(--azul-500);
+    --btn-secondary-bg-active:var(--azul-700);
+    --input-bg:#110e0e;
+    --input-border:#2a1f1f;
+    --card-bg:var(--superficie);
+    --card-border:#201515;
+    --focus-ring:
+        0 0 0 2px rgba(18,16,18,.9),
+        0 0 0 4px rgba(198,40,40,.45);
+    --scrollbar-track:#140d0d;
+    --scrollbar-thumb:#5b1d1d;
+    --scrollbar-thumb-hover:#8f2626;
+    --glass-bg:rgba(255,255,255,.04);
+    --glass-border:rgba(255,255,255,.08);
+    --glass-hover:rgba(255,255,255,.08);
+    --overlay:rgba(0,0,0,.65);
+    --overlay-soft:rgba(0,0,0,.35);
+    --pos-item-bg:var(--card-bg);
+    --pos-item-hover:rgba(0,0,0,.04);
+    --pos-item-active:var(--rojo-600);
+    --pos-divider:var(--card-border);
+    --pos-total-bg:var(--rojo-100);
+    --pos-total-text:var(--texto);
+    --pos-badge-bg:var(--azul-600);
+    --pos-mesa-activa-bg:var(--rojo-600);
+    --pos-mesa-inactiva-bg:var(--azul-700);
+    --pos-row-header:var(--card-bg);
+    --modal-dark-bg:var(--fondo-elevado);
+    --modal-dark-text:var(--texto);
+    --modal-dark-divider:var(--card-border);
+}
diff --git a/Frontend/src/assets/Css/Principales/Global.css b/Frontend/src/assets/Css/Principales/Global.css
new file mode 100644
index 0000000..de1588d
--- /dev/null
+++ b/Frontend/src/assets/Css/Principales/Global.css
@@ -0,0 +1,585 @@
+﻿/* ======================================================
+   BASE RESET
+   ====================================================== */
+
+*,
+*::before,
+*::after{
+    margin:0;
+    padding:0;
+    box-sizing:border-box;
+}
+
+html{
+    scroll-behavior:smooth;
+    -webkit-text-size-adjust:100%;
+}
+
+html,
+body,
+#root{
+    width:100%;
+    min-height:100vh;
+}
+
+body{
+    background:var(--fondo);
+    background-repeat:no-repeat;
+    background-size:cover;
+    background-attachment:fixed;
+    color:var(--texto);
+    font-family:var(--font-family-sans);
+    font-size:var(--font-md);
+    line-height:var(--line-normal);
+    display:flex;
+    flex-direction:column;
+    -webkit-font-smoothing:antialiased;
+    -moz-osx-font-smoothing:grayscale;
+    text-rendering:optimizeLegibility;
+    transition:
+        background var(--transition-slow),
+        color var(--transition-slow);
+}
+
+::selection{
+    background:rgba(198,40,40,.35);
+    color:#fff;
+}
+
+a{
+    color:var(--rojo-400);
+    text-decoration:none;
+    transition:color var(--transition-fast);
+}
+
+a:hover{
+    color:var(--rojo-300);
+}
+
+img{
+    max-width:100%;
+    display:block;
+}
+
+picture{
+    display:block;
+}
+
+button{
+    font:inherit;
+    border:none;
+    outline:none;
+    background:none;
+    cursor:pointer;
+    transition:all var(--transition-fast);
+}
+
+button:disabled{
+    opacity:var(--opacity-disabled);
+    cursor:not-allowed;
+}
+
+input,
+textarea,
+select{
+    font:inherit;
+    outline:none;
+}
+
+input,
+textarea,
+select{
+    width:100%;
+    background:var(--input-bg);
+    color:var(--texto);
+    border:1px solid var(--input-border);
+    border-radius:var(--radius-sm);
+    padding:12px 14px;
+    transition:
+        border-color var(--transition-fast),
+        box-shadow var(--transition-fast),
+        background var(--transition-fast);
+}
+
+input::placeholder,
+textarea::placeholder{
+    color:var(--placeholder);
+}
+
+input:hover,
+textarea:hover,
+select:hover{
+    border-color:var(--rojo-700);
+}
+
+input:focus,
+textarea:focus,
+select:focus{
+    border-color:var(--rojo-500);
+    box-shadow:var(--focus-ring);
+}
+
+::-webkit-scrollbar{
+    width:var(--scrollbar-size);
+    height:var(--scrollbar-size);
+}
+
+::-webkit-scrollbar-track{
+    background:var(--scrollbar-track);
+}
+
+::-webkit-scrollbar-thumb{
+    background:var(--scrollbar-thumb);
+    border-radius:999px;
+}
+
+::-webkit-scrollbar-thumb:hover{
+    background:var(--scrollbar-thumb-hover);
+}
+
+*{
+    scrollbar-width:thin;
+    scrollbar-color:
+        var(--scrollbar-thumb)
+        var(--scrollbar-track);
+}
+
+:focus-visible{
+    outline:none;
+    box-shadow:var(--focus-ring);
+}
+
+/* ======================================================
+   TYPOGRAPHY
+   ====================================================== */
+
+:root{
+    --font-family-sans:
+        "Inter",
+        system-ui,
+        -apple-system,
+        BlinkMacSystemFont,
+        "Segoe UI",
+        sans-serif;
+
+    --font-family-mono:
+        "SFMono-Regular",
+        Consolas,
+        "Liberation Mono",
+        monospace;
+
+    --font-xs:0.75rem;
+    --font-sm:0.875rem;
+    --font-md:1rem;
+    --font-lg:1.125rem;
+    --font-xl:1.25rem;
+    --font-2xl:1.5rem;
+    --font-3xl:1.875rem;
+    --font-4xl:2.25rem;
+
+    --weight-light:300;
+    --weight-regular:400;
+    --weight-medium:500;
+    --weight-semibold:600;
+    --weight-bold:700;
+
+    --line-tight:1.2;
+    --line-normal:1.5;
+    --line-relaxed:1.7;
+
+    --tracking-tight:-.02em;
+    --tracking-normal:0;
+    --tracking-wide:.05em;
+}
+
+/* ======================================================
+   SPACING
+   ====================================================== */
+
+:root{
+    --space-xxs:4px;
+    --space-xs:8px;
+    --space-sm:12px;
+    --space-md:16px;
+    --space-lg:24px;
+    --space-xl:32px;
+    --space-2xl:48px;
+    --space-3xl:72px;
+    --space-4xl:96px;
+}
+
+/* ======================================================
+   BORDER RADIUS
+   ====================================================== */
+
+:root{
+    --radius-xs:4px;
+    --radius-sm:6px;
+    --radius-md:10px;
+    --radius-lg:16px;
+    --radius-xl:24px;
+    --radius-2xl:32px;
+    --radius-full:9999px;
+}
+
+/* ======================================================
+   BORDERS
+   ====================================================== */
+
+:root{
+    --border-width:1px;
+    --border-width-md:2px;
+    --border-width-lg:3px;
+}
+
+/* ======================================================
+   SHADOWS
+   ====================================================== */
+
+:root{
+    --shadow-xs:0 1px 2px rgba(0,0,0,.12);
+    --shadow-sm:0 2px 6px rgba(0,0,0,.18);
+    --shadow-md:0 6px 16px rgba(0,0,0,.25);
+    --shadow-lg:0 12px 28px rgba(0,0,0,.35);
+    --shadow-xl:0 24px 50px rgba(0,0,0,.45);
+    --shadow-glow-red:0 0 18px rgba(198,40,40,.35);
+    --shadow-glow-blue:0 0 18px rgba(43,111,163,.30);
+}
+
+/* ======================================================
+   TRANSITIONS
+   ====================================================== */
+
+:root{
+    --transition-fast:.15s ease;
+    --transition-base:.25s ease;
+    --transition-slow:.45s ease;
+}
+
+/* ======================================================
+   OPACITY
+   ====================================================== */
+
+:root{
+    --opacity-disabled:.45;
+    --opacity-hover:.85;
+}
+
+/* ======================================================
+   LAYOUT
+   ====================================================== */
+
+:root{
+    --container-sm:640px;
+    --container-md:768px;
+    --container-lg:1024px;
+    --container-xl:1280px;
+    --container-2xl:1440px;
+}
+
+.container-app{
+    width:100%;
+    max-width:1440px;
+    margin-inline:auto;
+    padding-inline:var(--space-lg);
+}
+
+main{
+    width:90%;
+    margin-inline:auto;
+}
+
+.cargando{
+    width:100%;
+    height:80vh;
+    display:flex;
+    flex-direction:column;
+    justify-content:center;
+    align-items:center;
+    gap:var(--space-lg);
+}
+
+.cargando i{
+    font-size:150px;
+    color:var(--azul-500);
+}
+
+.divider{
+    width:100%;
+    height:1px;
+    background:var(--card-border);
+}
+
+.glass{
+    background:var(--glass-bg);
+    backdrop-filter:blur(12px);
+    border:1px solid var(--glass-border);
+}
+
+.elevation-xs{
+    box-shadow:var(--shadow-xs);
+}
+
+.elevation-sm{
+    box-shadow:var(--shadow-sm);
+}
+
+.elevation-md{
+    box-shadow:var(--shadow-md);
+}
+
+.elevation-lg{
+    box-shadow:var(--shadow-lg);
+}
+
+.elevation-xl{
+    box-shadow:var(--shadow-xl);
+}
+
+/* ======================================================
+   Z-INDEX
+   ====================================================== */
+
+:root{
+    --z-base:1;
+    --z-dropdown:100;
+    --z-sticky:300;
+    --z-tooltip:500;
+    --z-toast:800;
+    --z-modal:1000;
+    --z-loader:2000;
+}
+
+.volver{
+    width:95%;
+    z-index:var(--z-tooltip);
+    position:relative;
+}
+
+.volver .butnvolver{
+    position:fixed;
+    top:120px;
+}
+
+.volver button{
+    padding:10px 16px;
+    border-radius:var(--radius-md);
+    background:var(--btn-secondary-bg);
+    color:#fff;
+    transition:background var(--transition-fast);
+}
+
+.volver button:hover{
+    background:var(--btn-secondary-bg-hover);
+}
+
+/* ======================================================
+   SCROLLBAR
+   ====================================================== */
+
+:root{
+    --scrollbar-size:8px;
+}
+
+/* ======================================================
+   HELPERS
+   ====================================================== */
+
+.card-alt{
+    background:linear-gradient(
+        135deg,
+        var(--rojo-700),
+        var(--azul-800)
+    );
+    color:#fff;
+}
+
+.badge-azul{
+    background:var(--azul-600);
+    color:#fff;
+}
+
+.badge-rojo{
+    background:var(--rojo-600);
+    color:#fff;
+}
+
+.badge-success{
+    background:var(--success);
+    color:#fff;
+}
+
+.badge-warning{
+    background:var(--warning);
+    color:#111;
+}
+
+.text-success{color:var(--success)!important;}
+.text-warning{color:var(--warning)!important;}
+.text-danger{color:var(--danger)!important;}
+.text-info{color:var(--info)!important;}
+
+.bg-success{
+    background:var(--success)!important;
+    color:#fff!important;
+}
+
+.bg-warning{
+    background:var(--warning)!important;
+    color:#111!important;
+}
+
+.bg-danger{
+    background:var(--danger)!important;
+    color:#fff!important;
+}
+
+.bg-info{
+    background:var(--info)!important;
+    color:#fff!important;
+}
+
+.border-primary{border-color:var(--btn-primary-bg)!important;}
+.border-secondary{border-color:var(--btn-secondary-bg)!important;}
+.border-card{border-color:var(--card-border)!important;}
+
+.shadow-xs{box-shadow:var(--shadow-xs)!important;}
+.shadow-sm{box-shadow:var(--shadow-sm)!important;}
+.shadow-md{box-shadow:var(--shadow-md)!important;}
+.shadow-lg{box-shadow:var(--shadow-lg)!important;}
+.shadow-xl{box-shadow:var(--shadow-xl)!important;}
+
+.radius-xs{border-radius:var(--radius-xs)!important;}
+.radius-sm{border-radius:var(--radius-sm)!important;}
+.radius-md{border-radius:var(--radius-md)!important;}
+.radius-lg{border-radius:var(--radius-lg)!important;}
+.radius-xl{border-radius:var(--radius-xl)!important;}
+.radius-full{border-radius:var(--radius-full)!important;}
+
+.glass-light{
+    background:rgba(255,255,255,.04);
+    backdrop-filter:blur(10px);
+    border:1px solid rgba(255,255,255,.08);
+}
+
+.glass-heavy{
+    background:rgba(255,255,255,.08);
+    backdrop-filter:blur(18px);
+    border:1px solid rgba(255,255,255,.12);
+}
+
+.flex{display:flex;}
+.inline-flex{display:inline-flex;}
+
+.flex-column{flex-direction:column;}
+.flex-wrap{flex-wrap:wrap;}
+
+.items-center{align-items:center;}
+.items-start{align-items:flex-start;}
+.items-end{align-items:flex-end;}
+
+.justify-center{justify-content:center;}
+.justify-between{justify-content:space-between;}
+.justify-around{justify-content:space-around;}
+.justify-end{justify-content:flex-end;}
+
+.gap-xs{gap:var(--space-xs);}
+.gap-sm{gap:var(--space-sm);}
+.gap-md{gap:var(--space-md);}
+.gap-lg{gap:var(--space-lg);}
+.gap-xl{gap:var(--space-xl);}
+
+.p-xs{padding:var(--space-xs);}
+.p-sm{padding:var(--space-sm);}
+.p-md{padding:var(--space-md);}
+.p-lg{padding:var(--space-lg);}
+.p-xl{padding:var(--space-xl);}
+
+.m-xs{margin:var(--space-xs);}
+.m-sm{margin:var(--space-sm);}
+.m-md{margin:var(--space-md);}
+.m-lg{margin:var(--space-lg);}
+.m-xl{margin:var(--space-xl);}
+
+.w-100{width:100%;}
+.h-100{height:100%;}
+
+.text-center{text-align:center;}
+.text-left{text-align:left;}
+.text-right{text-align:right;}
+
+/* ======================================================
+   SHARED STATUS TOKENS
+   ====================================================== */
+
+:root{
+    --success:#27ae60;
+    --warning:#f39c12;
+    --info:#3498db;
+    --danger:#c62828;
+
+    --success-soft:color-mix(in srgb,var(--success) 18%,transparent);
+    --warning-soft:color-mix(in srgb,var(--warning) 18%,transparent);
+    --danger-soft:color-mix(in srgb,var(--danger) 18%,transparent);
+    --info-soft:color-mix(in srgb,var(--info) 18%,transparent);
+
+    --pos-badge-text:#fff;
+}
+
+/* ======================================================
+   ANIMATIONS
+   ====================================================== */
+
+@keyframes fadeIn{
+    from{
+        opacity:0;
+    }
+
+    to{
+        opacity:1;
+    }
+}
+
+@keyframes fadeUp{
+    from{
+        opacity:0;
+        transform:translateY(15px);
+    }
+
+    to{
+        opacity:1;
+        transform:translateY(0);
+    }
+}
+
+.fade-in{
+    animation:fadeIn .25s ease;
+}
+
+.fade-up{
+    animation:fadeUp .30s ease;
+}
+
+/* ======================================================
+   MEDIA QUERIES
+   ====================================================== */
+
+@media (max-width:768px){
+    main{
+        width:95%;
+    }
+
+    .container-app{
+        padding-inline:var(--space-md);
+    }
+
+    .volver .butnvolver{
+        top:80px;
+    }
+}
+
+@media (max-width:576px){
+    .hide-mobile{
+        display:none!important;
+    }
+}
diff --git a/Frontend/src/assets/Css/Principales/Inicio.module.scss b/Frontend/src/assets/Css/Principales/Inicio.module.scss
new file mode 100644
index 0000000..c79112e
--- /dev/null
+++ b/Frontend/src/assets/Css/Principales/Inicio.module.scss
@@ -0,0 +1,344 @@
+/* ======================================================
+   MAIN CONTENT
+   ====================================================== */
+
+.inicioMain {
+    width: 100%;
+    padding: var(--space-lg) var(--space-md);
+    gap: var(--space-xl);
+}
+
+.authenticatedSection,
+.inventarioContent {
+    width: 100%;
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    gap: var(--space-lg);
+}
+
+.pageTitle {
+    width: 100%;
+    font-size: var(--font-2xl);
+    font-weight: var(--weight-semibold);
+    text-align: center;
+    color: var(--texto);
+    padding-bottom: var(--space-md);
+    border-bottom: var(--border-width) solid var(--card-border);
+}
+
+/* ======================================================
+   TAREAS SECTION
+   ====================================================== */
+
+.tareasSection {
+    width: 100%;
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    gap: var(--space-lg);
+}
+
+.sectionTitle {
+    width: 100%;
+    font-size: var(--font-xl);
+    font-weight: var(--weight-medium);
+    text-align: center;
+    color: var(--texto);
+    padding: var(--space-lg) 0 var(--space-md) 0;
+    border-bottom: var(--border-width) solid var(--card-border);
+    margin-top: var(--space-md);
+}
+
+.tareasGrid {
+    width: 100%;
+    display: flex;
+    flex-wrap: wrap;
+    justify-content: center;
+    align-items: stretch;
+    gap: var(--space-lg);
+}
+
+/* ======================================================
+   TAREA CARD
+   ====================================================== */
+
+.tareaCard {
+    flex: 0 1 250px;
+    background: var(--card-bg);
+    border: var(--border-width) solid var(--card-border);
+    border-radius: var(--radius-md);
+    padding: var(--space-md);
+    display: flex;
+    flex-direction: column;
+    gap: var(--space-md);
+    position: relative;
+    overflow: hidden;
+    transition: 
+        transform var(--transition-base),
+        border-color var(--transition-base);
+    cursor: pointer;
+    z-index: 1;
+
+    &::before {
+        content: "";
+        position: absolute;
+        top: 0;
+        left: 0;
+        width: 0;
+        height: 100%;
+        background: linear-gradient(135deg, var(--rojo-700), var(--azul-800));
+        border-radius: var(--radius-md);
+        transition: width var(--transition-base);
+        z-index: -1;
+    }
+
+    &:hover {
+        transform: translateY(-4px);
+        border-color: var(--rojo-500);
+        box-shadow: var(--shadow-md);
+
+        &::before {
+            width: 100%;
+        }
+
+        .tareaTitle {
+            color: #fff;
+        }
+
+        .tareaDescription {
+            color: rgba(255, 255, 255, 0.9);
+        }
+    }
+}
+
+.tareaLink {
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    gap: var(--space-md);
+    color: var(--texto);
+    text-decoration: none;
+    transition: color var(--transition-fast);
+}
+
+.tareaIconWrapper {
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    width: 60px;
+    height: 60px;
+    border-radius: var(--radius-md);
+    background: rgba(43, 111, 163, 0.15);
+    transition: 
+        background var(--transition-fast),
+        transform var(--transition-fast);
+
+    i {
+        font-size: var(--font-3xl);
+        color: var(--azul-400);
+        transition: color var(--transition-fast);
+    }
+}
+
+.tareaCard:hover .tareaIconWrapper {
+    background: rgba(198, 40, 40, 0.2);
+    transform: scale(1.1);
+
+    i {
+        color: #fff;
+    }
+}
+
+.tareaTitle {
+    font-size: var(--font-lg);
+    font-weight: var(--weight-semibold);
+    color: var(--texto);
+    text-align: center;
+    transition: color var(--transition-fast);
+}
+
+.tareaDescription {
+    font-size: var(--font-sm);
+    color: var(--texto-sec);
+    text-align: center;
+    flex-grow: 1;
+    transition: color var(--transition-fast);
+    line-height: var(--line-normal);
+}
+
+/* ======================================================
+   UNAUTHENTICATED STATE
+   ====================================================== */
+
+.ItemContainer {
+    width: 100%;
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    justify-content: center;
+    text-align: center;
+    gap: var(--space-xl);
+    padding: var(--space-2xl) var(--space-md);
+    min-height: 60vh;
+
+    h2 {
+        width: 100%;
+        font-size: var(--font-3xl);
+        font-weight: var(--weight-semibold);
+        color: var(--texto);
+        text-align: center;
+        padding-bottom: var(--space-md);
+        border-bottom: var(--border-width) solid var(--card-border);
+    }
+
+    p {
+        font-size: var(--font-md);
+        color: var(--texto-sec);
+        max-width: 500px;
+        line-height: var(--line-relaxed);
+    }
+}
+
+.GrupoIconos {
+    display: flex;
+    gap: var(--space-2xl);
+    justify-content: center;
+    align-items: center;
+    flex-wrap: wrap;
+}
+
+.GrupoBotones {
+    display: flex;
+    gap: var(--space-md);
+    flex-wrap: wrap;
+    justify-content: center;
+    align-items: center;
+    padding: var(--space-lg) var(--space-2xl);
+    background: linear-gradient(135deg, var(--rojo-700), var(--azul-800));
+    border-radius: var(--radius-md);
+    box-shadow: var(--shadow-md);
+
+    a {
+        display: flex;
+        flex-direction: column;
+        align-items: center;
+        gap: var(--space-sm);
+        text-align: center;
+        color: #fff;
+        text-decoration: none;
+        padding: var(--space-md) var(--space-lg);
+        border-radius: var(--radius-sm);
+        transition: 
+            background var(--transition-fast),
+            transform var(--transition-fast);
+        font-weight: var(--weight-medium);
+
+        &:hover {
+            background: rgba(255, 255, 255, 0.1);
+            transform: translateY(-2px);
+        }
+
+        i {
+            font-size: var(--font-2xl);
+        }
+    }
+}
+
+/* ======================================================
+   RESPONSIVE DESIGN
+   ====================================================== */
+
+@media (max-width: 768px) {
+    .inicioMain {
+        padding: var(--space-md) var(--space-sm);
+        gap: var(--space-lg);
+    }
+
+    .pageTitle,
+    .sectionTitle {
+        font-size: var(--font-xl);
+    }
+
+    .tareasGrid {
+        gap: var(--space-md);
+    }
+
+    .tareaCard {
+        flex: 0 1 calc(50% - var(--space-sm));
+    }
+
+    .GrupoBotones {
+        flex-direction: column;
+        padding: var(--space-md) var(--space-lg);
+
+        a {
+            width: 100%;
+        }
+    }
+
+    .ItemContainer {
+        min-height: 50vh;
+        padding: var(--space-lg) var(--space-md);
+
+        h2 {
+            font-size: var(--font-2xl);
+        }
+    }
+}
+
+@media (max-width: 576px) {
+    .inicioMain {
+        padding: var(--space-md) var(--space-sm);
+    }
+
+    .pageTitle,
+    .sectionTitle {
+        font-size: var(--font-lg);
+    }
+
+    .tareasGrid {
+        justify-content: stretch;
+    }
+
+    .tareaCard {
+        flex: 0 1 100%;
+        max-width: 100%;
+    }
+
+    .GrupoIconos {
+        flex-direction: column;
+        gap: var(--space-lg);
+    }
+
+    .tareaIconWrapper {
+        width: 50px;
+        height: 50px;
+
+        i {
+            font-size: var(--font-2xl);
+        }
+    }
+
+    .tareaTitle {
+        font-size: var(--font-md);
+    }
+
+    .tareaDescription {
+        font-size: var(--font-sm);
+    }
+
+    .GrupoBotones {
+        padding: var(--space-md);
+        gap: var(--space-sm);
+
+        a {
+            padding: var(--space-sm) var(--space-md);
+            font-size: var(--font-sm);
+
+            i {
+                font-size: var(--font-lg);
+            }
+        }
+    }
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Principales/index.module.scss b/Frontend/src/assets/Css/Principales/index.module.scss
new file mode 100644
index 0000000..6ad6f05
--- /dev/null
+++ b/Frontend/src/assets/Css/Principales/index.module.scss
@@ -0,0 +1,249 @@
+/* ======================================================
+   HEADER
+   ====================================================== */
+.header {
+    width: 100%;
+    display: flex;
+    justify-content: center;
+    align-items: center;
+    padding: var(--space-sm);
+    position: sticky;
+    top: 0;
+    z-index: var(--z-tooltip);
+}
+
+.header-container {
+    width: 95%;
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    flex-wrap: wrap;
+    gap: var(--space-lg);
+    padding: var(--space-sm) var(--space-lg);
+    background: var(--header-bg);
+    border-radius: var(--radius-md);
+    box-shadow: var(--shadow-md);
+}
+
+.header-logo,
+.overlay-logo {
+    width: 80px;
+    height: 80px;
+
+    img {
+        width: 100%;
+        height: 100%;
+        object-fit: contain;
+    }
+}
+
+.header-user {
+    display: flex;
+    width: 80px;
+    height: 80px;
+    border-radius: var(--radius-full);
+    overflow: hidden;
+    text-decoration: none;
+    cursor: pointer;
+    transition: box-shadow var(--transition-fast);
+
+    img {
+        width: 100%;
+        height: 100%;
+        object-fit: cover;
+    }
+
+    i {
+        margin: auto;
+        font-size: 2.5rem;
+        color: var(--texto);
+    }
+
+    &:hover {
+        box-shadow: var(--shadow-glow-red);
+    }
+}
+
+.header-title {
+    color: var(--texto);
+    font-size: var(--font-2xl);
+    font-weight: var(--weight-bold);
+    letter-spacing: var(--tracking-tight);
+}
+
+.header-nav {
+    flex: 1;
+    display: flex;
+    align-items: center;
+    justify-content: flex-end;
+    gap: var(--space-xl);
+}
+
+.header-list {
+    display: flex;
+    list-style: none;
+    gap: var(--space-lg);
+}
+
+.header-link {
+    position: relative;
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    gap: var(--space-xxs);
+    padding: var(--space-xs) var(--space-sm);
+    border-radius: var(--radius-md);
+    color: var(--texto);
+    font-size: var(--font-md);
+    font-weight: var(--weight-medium);
+    text-align: center;
+    text-decoration: none;
+    overflow: hidden;
+    isolation: isolate;
+    transition: color var(--transition-fast);
+
+    i {
+        font-size: var(--font-xl);
+    }
+
+    &::before {
+        content: "";
+        position: absolute;
+        inset: auto 0 0 0;
+        height: 0%;
+        background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
+        transition: height var(--transition-slow);
+        z-index: -1;
+    }
+
+    &:hover {
+        color: var(--rojo-900);
+    }
+
+    &:hover::before {
+        height: 100%;
+    }
+}
+
+/* Botón hamburguesa */
+.header-burger {
+    display: none;
+    font-size: var(--font-3xl);
+    color: var(--texto);
+}
+
+/* Overlay lateral */
+.header-overlay {
+    position: fixed;
+    inset: 0 0 0 -100%;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    width: 100%;
+    height: 100%;
+    background: var(--header-bg);
+    transition: left var(--transition-base);
+    z-index: var(--z-tooltip);
+
+    &.is-open {
+        left: 0;
+    }
+}
+
+.overlay-content {
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    gap: var(--space-2xl);
+}
+
+@media (max-width: 850px) {
+    .only-desktop {
+        display: none;
+    }
+
+    .only-mobile {
+        display: block;
+    }
+
+    .header-burger {
+        display: block;
+    }
+
+    .header-nav {
+        display: none;
+    }
+
+    .header-list {
+        flex-direction: column;
+        gap: var(--space-lg);
+    }
+
+    .header-title {
+        font-size: var(--font-lg);
+    }
+}
+
+/* ======================================================
+   CONTAINER
+   ====================================================== */
+.Container {
+    position: relative;
+    width: 90%;
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    gap: var(--space-md);
+}
+
+/* ======================================================
+   FOOTER
+   ====================================================== */
+.footer {
+    width: 100%;
+    display: flex;
+    justify-content: center;
+    align-items: center;
+    padding: var(--space-sm);
+}
+
+.footer-container {
+    width: 95%;
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    flex-wrap: wrap;
+    gap: var(--space-md);
+    padding: var(--space-md) var(--space-lg);
+    color: var(--texto);
+    background: var(--footer-bg);
+    border-radius: var(--radius-md);
+    box-shadow: var(--shadow-md);
+}
+
+.footer-brand {
+    font-weight: var(--weight-medium);
+}
+
+.footer-contador {
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    justify-content: center;
+    gap: var(--space-xs);
+    text-align: center;
+}
+
+.footer-tiempo {
+    display: flex;
+    align-items: center;
+    gap: var(--space-xs);
+}
+
+@media (max-width: 850px) {
+    .footer-container {
+        flex-direction: column;
+        text-align: center;
+        gap: var(--space-xs);
+    }
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/Transiciones.scss b/Frontend/src/assets/Css/Transiciones.scss
index 44bcbe4..240e4dd 100644
--- a/Frontend/src/assets/Css/Transiciones.scss
+++ b/Frontend/src/assets/Css/Transiciones.scss
@@ -1,18 +1,14 @@
 .transition-overlay {
     position: fixed;
-    top: 0;
-    left: 0;
+    inset: 0;
     width: 100%;
     height: 100vh;
     pointer-events: none;
-    z-index: 9999;
-}
-
-.transition-overlay {
+    z-index: var(--z-loader);
     background: radial-gradient(
         circle at center,
-        rgba(47, 101, 165, 0.3),
-        rgba(0, 0, 0, 0.8)
+        color-mix(in srgb, var(--azul-500) 30%, transparent),
+        color-mix(in srgb, black 80%, transparent)
     );
     backdrop-filter: blur(10px);
-}
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/crud.module.scss b/Frontend/src/assets/Css/crud.module.scss
index 3ef31e6..4201b0d 100644
--- a/Frontend/src/assets/Css/crud.module.scss
+++ b/Frontend/src/assets/Css/crud.module.scss
@@ -1,271 +1,584 @@
+/* ======================================================
+   CONTENEDOR PRINCIPAL
+   ====================================================== */
+
 .Contenedor {
-    gap: 2rem;
-    color: var(--texto);
+    display: grid;
+    grid-template-columns: 1fr 3fr;
+    gap: var(--space-2xl);
+    padding: var(--space-lg);
+
+    h2 {
+        grid-column: span 2 / span 2;
+        font-size: var(--font-2xl);
+        font-weight: var(--weight-bold);
+        color: var(--texto);
+        text-align: center;
+        padding-bottom: var(--space-md);
+        border-bottom: var(--border-width) solid var(--card-border);
+    }
 }
 
-/* ================================
-    SECCIÓN TABLA PRODUCTOS
-   ================================ */
+/* ======================================================
+   SECCIÓN TABLA
+   ====================================================== */
 
 .TablaSection {
     display: flex;
     flex-direction: column;
-    gap: 1.5rem;
+    gap: var(--space-lg);
     background: var(--card-bg);
+    border-radius: var(--radius-lg);
+    border: var(--border-width) solid var(--card-border);
+    box-shadow: var(--shadow-md);
+    overflow: hidden;
+}
+
+/* ======================================================
+   HEADER TABLA - BOTÓN NUEVO
+   ====================================================== */
+
+.Header {
+    padding: var(--space-lg);
+    border-bottom: var(--border-width) solid var(--card-border);
+    background: linear-gradient(135deg, rgba(198, 40, 40, 0.05), rgba(43, 111, 163, 0.05));
+}
+
+.Header button {
+    display: inline-flex;
+    align-items: center;
+    gap: var(--space-sm);
+    padding: var(--space-md) var(--space-lg);
+    background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
+    color: #fff;
+    border: none;
     border-radius: var(--radius-md);
-    padding: 2rem;
-    box-shadow: var(--shadow-1);
-    border: 1px solid var(--card-border);
-    overflow-x: auto;
+    font-size: var(--font-md);
+    font-weight: var(--weight-semibold);
+    cursor: pointer;
+    transition: 
+        transform var(--transition-fast),
+        box-shadow var(--transition-fast);
+    box-shadow: var(--shadow-md);
 
-    h2 {
-        color: var(--texto);
-        font-size: 1.4rem;
-        font-weight: 600;
+    &:hover {
+        transform: translateY(-2px);
+        box-shadow: var(--shadow-lg);
     }
 
-    table {
-        width: 100%;
-        border-collapse: collapse;
-        color: var(--texto-sec);
+    &:active {
+        transform: translateY(0);
+    }
 
-        thead {
-            color: var(--texto);
-        }
+    i {
+        font-size: var(--font-lg);
+    }
+}
 
-        th, td {
-            padding: 0.75rem 1rem;
-            border-bottom: 1px solid var(--card-border);
-            text-align: left;
-            vertical-align: middle;
-        }
+/* ======================================================
+   TABLA RESPONSIVA
+   ====================================================== */
 
-        th {
-            font-weight: 600;
-            letter-spacing: 0.3px;
-        }
+.tablaWrapper {
+    overflow-x: auto;
+    -webkit-overflow-scrolling: touch;
+}
 
-        tbody tr {
-            transition: var(--transition-fast);
+table {
+    width: 100%;
+    border-collapse: collapse;
+    color: var(--texto-sec);
 
-            &:hover {
-                background: var(--glass);
-                cursor: pointer;
-            }
-        }
+    thead {
+        background: rgba(198, 40, 40, 0.08);
+        position: sticky;
+        top: 0;
+        z-index: 10;
+    }
 
-        button {
-            padding: 5px 10px;
-            border-radius: var(--radius-sm);
+    th {
+        padding: var(--space-md) var(--space-lg);
+        text-align: left;
+        font-weight: var(--weight-semibold);
+        font-size: var(--font-sm);
+        color: var(--texto);
+        text-transform: uppercase;
+        letter-spacing: var(--tracking-wide);
+        border-bottom: var(--border-width-md) solid var(--card-border);
+    }
+
+    td {
+        padding: var(--space-md) var(--space-lg);
+        border-bottom: var(--border-width) solid var(--card-border);
+        vertical-align: middle;
+        color: var(--texto-sec);
+    }
+
+    tbody tr {
+        transition: 
+            background var(--transition-fast),
+            transform var(--transition-fast);
+
+        &:hover {
+            background: rgba(198, 40, 40, 0.08);
         }
     }
 }
 
-/* Imagen dentro de la tabla */
+/* ======================================================
+   CELDA DE IMAGEN
+   ====================================================== */
+
 .ImagenProducto {
-    width: 70px;
-    height: 70px;
+    width: 60px;
+    height: 60px;
     object-fit: cover;
-    border-radius: var(--radius-sm);
+    border-radius: var(--radius-md);
     background: var(--fondo-elevado);
-    box-shadow: var(--shadow-2);
+    box-shadow: var(--shadow-sm);
+    cursor: pointer;
+    transition: 
+        transform var(--transition-fast),
+        box-shadow var(--transition-fast);
+
+    &:hover {
+        transform: scale(1.1);
+        box-shadow: var(--shadow-md);
+    }
 }
 
-/* ================================
-    HEADER TABLA
-   ================================ */
-.Header {
+/* ======================================================
+   ACCIONES EN TABLA
+   ====================================================== */
+
+.acciones {
     display: flex;
-    justify-content: space-between;
+    gap: var(--space-sm);
+    flex-wrap: wrap;
     align-items: center;
+}
 
-    button {
-        background-color: var(--btn-primary-bg);
-        border: none;
-        color: var(--btn-primary-text);
-        padding: 0.5rem 1rem;
-        border-radius: var(--radius-sm);
-        cursor: pointer;
-        font-weight: 500;
-        transition: var(--transition-fast);
-        box-shadow: var(--shadow-1);
+.tablaAccionesBtn {
+    padding: var(--space-sm) var(--space-md);
+    background: linear-gradient(135deg, var(--rojo-600), var(--rojo-700));
+    color: #fff;
+    border: none;
+    border-radius: var(--radius-sm);
+    font-size: var(--font-sm);
+    font-weight: var(--weight-medium);
+    cursor: pointer;
+    transition: 
+        transform var(--transition-fast),
+        box-shadow var(--transition-fast);
+    white-space: nowrap;
 
-        &:hover {
-            background-color: var(--btn-primary-bg-hover);
-        }
+    &:hover {
+        transform: translateY(-1px);
+        box-shadow: var(--shadow-md);
+    }
 
-        &:active {
-            background-color: var(--btn-primary-bg-active);
-        }
+    i {
+        margin-right: var(--space-xs);
     }
 }
 
-/* ================================
-    PAGINACIÓN
-   ================================ */
-.Paginacion {
-    width: 100%;
+/* ======================================================
+   TOGGLE/SWITCH DE VISIBILIDAD
+   ====================================================== */
+
+.visibilityToggle {
+    display: flex;
+    align-items: center;
+    gap: var(--space-sm);
+}
+
+.toggleSwitch {
+    position: relative;
+    width: 50px;
+    height: 28px;
+    background: var(--danger);
+    border: none;
+    border-radius: 20px;
+    cursor: pointer;
+    transition: 
+        background var(--transition-fast),
+        box-shadow var(--transition-fast);
+    padding: 2px;
     display: flex;
-    flex-direction: column;
     align-items: center;
+    padding-right: var(--space-xs);
+
+    &::after {
+        content: '';
+        width: 24px;
+        height: 24px;
+        background: #fff;
+        border-radius: 50%;
+        transition: transform var(--transition-fast);
+        box-shadow: var(--shadow-sm);
+    }
+
+    &.active {
+        background: var(--success);
+        padding-left: var(--space-xs);
+        padding-right: 2px;
+
+        &::after {
+            transform: translateX(22px);
+        }
+    }
+
+    &:focus {
+        outline: none;
+        box-shadow: 0 0 0 3px rgba(198, 40, 40, 0.2);
+    }
+}
+
+.toggleLabel {
+    font-size: var(--font-sm);
+    font-weight: var(--weight-medium);
     color: var(--texto-sec);
-    gap: 1rem;
-    flex-wrap: wrap;
-    border-top: 1px solid var(--texto-sec);
-    padding-top: 1rem;
-
-    button {
-        border-radius: var(--radius-sm);
-        padding: 0.4rem 1rem;
-        font-weight: 500;
-        cursor: pointer;
-        transition: var(--transition-fast);
-
-        &.btn-secondary {
-            background-color: var(--btn-secondary-bg);
-            border: 1px solid var(--btn-secondary-bg);
-            color: var(--btn-secondary-text);
-
-            &:hover {
-                background-color: var(--btn-secondary-bg-hover);
-            }
-
-            &:disabled {
-                background-color: var(--btn-disabled-bg);
-                color: var(--btn-disabled-text);
-                cursor: not-allowed;
-            }
+    white-space: nowrap;
+}
+
+/* ======================================================
+   TABLA RESPONSIVA - MÓVIL
+   ====================================================== */
+
+@media (max-width: 1024px) {
+    .TablaSection {
+        padding: 0;
+    }
+
+    .Header {
+        padding: var(--space-md);
+    }
+
+    .Header button {
+        width: 100%;
+        justify-content: center;
+    }
+
+    /* Convertir tabla a cards en tablet/móvil */
+    table,
+    thead,
+    tbody,
+    th,
+    td,
+    tr {
+        display: block;
+    }
+
+    thead {
+        display: none;
+    }
+
+    tr {
+        border: var(--border-width) solid var(--card-border);
+        border-radius: var(--radius-md);
+        margin-bottom: var(--space-md);
+        overflow: hidden;
+        box-shadow: var(--shadow-sm);
+        background: var(--card-bg);
+
+        &:hover {
+            box-shadow: var(--shadow-md);
+            border-color: var(--rojo-500);
         }
     }
 
-    select {
-        width: 150px;
-        background: var(--input-bg);
-        border: 1px solid var(--input-border);
-        color: var(--input-text);
-        border-radius: var(--radius-sm);
-        padding: 0.4rem 0.6rem;
+    td {
+        padding: var(--space-md);
+        padding-left: 0;
+        border: none;
+        border-bottom: var(--border-width) solid var(--card-border);
+        position: relative;
+        display: grid;
+        grid-template-columns: 1fr 1fr;
+        justify-items: center;
+        gap: var(--space-md);
 
-        &:focus {
-            border-color: var(--input-focus-border);
+        &::before {
+            content: attr(data-label);
+            font-weight: var(--weight-semibold);
+            color: var(--texto);
+            font-size: var(--font-sm);
+            text-transform: uppercase;
+            letter-spacing: var(--tracking-wide);
+            word-break: break-word;
+        }
+
+        &:last-child {
+            border-bottom: none;
         }
     }
-}
 
-.PaginacionBotones {
-    width: 100%;
-    display: flex;
-    gap: 2rem;
-    justify-content: center;
-    align-items: center;
+    /* Especial para columnas sin etiqueta */
+    .ImagenProducto {
+        grid-column: 1 / -1;
+        width: 100%;
+        height: auto;
+        max-height: 200px;
+        margin: 0 0 var(--space-md) 0;
+
+        &::before {
+            display: none;
+        }
+    }
 }
 
-/* ================================
-    MODAL (ACTUALIZADA)
-   ================================ */
+/* ======================================================
+   MODAL - OVERLAY
+   ====================================================== */
 
 .ModalOverlay {
     position: fixed;
-    top: 0; left: 0; right: 0; bottom: 0;
-    background: rgba(0, 0, 0, 0.65);
+    top: 0;
+    left: 0;
+    right: 0;
+    bottom: 0;
+    background: rgba(0, 0, 0, 0.7);
     display: flex;
     align-items: center;
     justify-content: center;
-    z-index: 1000;
-    backdrop-filter: blur(4px);
+    z-index: var(--z-modal);
+    backdrop-filter: blur(6px);
     overflow-y: auto;
-    padding: 2rem;
+    padding: var(--space-lg);
+    animation: fadeIn var(--transition-base) ease;
 }
 
+/* ======================================================
+   MODAL - CONTENEDOR
+   ====================================================== */
+
 .Modal {
     position: relative;
     background: var(--fondo-elevado);
-    border-radius: var(--radius-md);
-    box-shadow: var(--shadow-1);
+    border-radius: var(--radius-xl);
+    border: var(--border-width) solid var(--card-border);
+    box-shadow: var(--shadow-xl);
     color: var(--texto);
-    border: 1px solid var(--card-border);
-    animation: fadeIn 0.3s ease-in-out;
-    padding: 2rem;
+    padding: var(--space-2xl);
     width: 100%;
-    max-width: 700px;
+    max-width: 500px;
     max-height: 90vh;
     overflow-y: auto;
     display: flex;
     flex-direction: column;
-    gap: 1.2rem;
+    gap: var(--space-lg);
+}
 
-    h3 {
-        text-align: center;
-        font-weight: 600;
+.ProductoModal {
+    max-width: 600px;
+}
+
+.ScrollableContent {
+    overflow-y: auto;
+    padding-right: var(--space-sm);
+
+    &::-webkit-scrollbar {
+        width: 6px;
+    }
+
+    &::-webkit-scrollbar-track {
+        background: transparent;
+    }
+
+    &::-webkit-scrollbar-thumb {
+        background: var(--scrollbar-thumb);
+        border-radius: 3px;
+
+        &:hover {
+            background: var(--scrollbar-thumb-hover);
+        }
     }
 }
 
+.Modal h3 {
+    font-size: var(--font-xl);
+    font-weight: var(--weight-bold);
+    text-align: center;
+    color: var(--texto);
+    margin: 0;
+}
+
+/* ======================================================
+   BOTÓN CERRAR MODAL
+   ====================================================== */
+
 .CloseButton {
     position: absolute;
-    top: 10px;
-    right: 10px;
-    background: transparent;
+    top: var(--space-md);
+    right: var(--space-md);
+    width: 40px;
+    height: 40px;
+    background: rgba(198, 40, 40, 0.1);
     border: none;
+    border-radius: var(--radius-full);
     color: var(--texto);
-    font-size: 1.5rem;
+    font-size: var(--font-lg);
     cursor: pointer;
-    transition: 0.2s;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    transition: 
+        background var(--transition-fast),
+        transform var(--transition-fast);
 
     &:hover {
-        color: var(--rojo-400);
+        background: rgba(198, 40, 40, 0.2);
         transform: scale(1.1);
     }
+
+    &:active {
+        transform: scale(0.95);
+    }
 }
 
+/* ======================================================
+   CONTENIDO MODAL - FORMULARIO
+   ====================================================== */
+
 .ModalContent {
     display: flex;
     flex-direction: column;
-    gap: 1rem;
+    gap: var(--space-lg);
+}
 
-    select {
-        background: var(--input-bg);
-        border: 1px solid var(--input-border);
-        color: var(--input-text);
-        border-radius: var(--radius-sm);
-        padding: 0.4rem 0.6rem;
+.ModalContent label {
+    display: flex;
+    flex-direction: column;
+    gap: var(--space-sm);
+    font-weight: var(--weight-medium);
+    color: var(--texto);
+    font-size: var(--font-sm);
+}
 
-        &:focus {
-            border-color: var(--input-focus-border);
-        }
+.ModalContent input,
+.ModalContent textarea,
+.ModalContent select {
+    width: 100%;
+    padding: var(--space-md);
+    background: var(--input-bg);
+    color: var(--texto);
+    border: var(--border-width) solid var(--input-border);
+    border-radius: var(--radius-md);
+    font-size: var(--font-md);
+    font-family: inherit;
+    transition:
+        border-color var(--transition-fast),
+        box-shadow var(--transition-fast),
+        background var(--transition-fast);
+
+    &::placeholder {
+        color: var(--placeholder);
+    }
+
+    &:hover {
+        border-color: var(--rojo-700);
+    }
+
+    &:focus {
+        outline: none;
+        border-color: var(--rojo-500);
+        box-shadow: var(--focus-ring);
+        background: rgba(198, 40, 40, 0.05);
     }
+}
+
+.ModalContent textarea {
+    resize: vertical;
+    min-height: 80px;
+}
+
+/* ======================================================
+   IMAGEN EN MODAL
+   ====================================================== */
+
+.ImagenContainer {
+    display: flex;
+    flex-direction: column;
+    gap: var(--space-md);
+}
+
+.ImagenPreview {
+    display: flex;
+    justify-content: center;
+    align-items: center;
+    width: 100%;
+    min-height: 150px;
+    border: 2px dashed var(--card-border);
+    border-radius: var(--radius-md);
+    background: rgba(198, 40, 40, 0.05);
+    cursor: pointer;
+    transition:
+        border-color var(--transition-fast),
+        background var(--transition-fast);
+
+    &:hover {
+        border-color: var(--rojo-500);
+        background: rgba(198, 40, 40, 0.1);
+    }
+
+    img {
+        width: 100%;
+        height: 100%;
+        object-fit: contain;
+        max-width: 200px;
+        max-height: 200px;
+        border-radius: var(--radius-md);
+    }
+
+    i {
+        font-size: var(--font-3xl);
+        color: var(--azul-400);
+    }
+}
+
+/* ======================================================
+   FILA DE INPUTS (DOS COLUMNAS)
+   ====================================================== */
+
+.FilaInputs {
+    display: grid;
+    grid-template-columns: 1fr 1fr;
+    gap: var(--space-lg);
 
     label {
-        display: flex;
-        flex-direction: column;
-        font-weight: 500;
-        color: var(--texto-sec);
-        font-size: 0.9rem;
-
-        input, textarea {
-            background: var(--input-bg);
-            color: var(--input-text);
-            border: 1px solid var(--input-border);
-            border-radius: var(--radius-sm);
-            padding: 0.6rem;
-            margin-top: 0.4rem;
-            transition: 0.2s;
-
-            &:focus {
-                border-color: var(--rojo-600);
-                box-shadow: 0 0 5px var(--rojo-600);
-            }
-        }
+        margin: 0;
     }
 }
+
+@media (max-width: 576px) {
+    .FilaInputs {
+        grid-template-columns: 1fr;
+        gap: var(--space-md);
+    }
+}
+
+/* ======================================================
+   SWITCH PERSONALIZADO EN MODAL
+   ====================================================== */
+
 .SwitchLabel {
     display: flex;
     align-items: center;
-    gap: 0.8rem;
-    font-weight: 500;
-    font-size: 0.9rem;
+    gap: var(--space-md);
     cursor: pointer;
     user-select: none;
-    color: var(--texto-sec);
+    padding: var(--space-md);
+    background: rgba(198, 40, 40, 0.05);
+    border-radius: var(--radius-md);
+    border: var(--border-width) solid var(--card-border);
+    transition:
+        border-color var(--transition-fast),
+        background var(--transition-fast);
+
+    &:hover {
+        border-color: var(--rojo-500);
+        background: rgba(198, 40, 40, 0.08);
+    }
 
     input {
         display: none;
@@ -273,212 +586,336 @@
 
     .Switch {
         position: relative;
-        width: 42px;
-        height: 22px;
-        background: var(--azul-600);
-        border-radius: 22px;
+        width: 50px;
+        height: 28px;
+        background: var(--danger);
+        border-radius: 20px;
+        flex-shrink: 0;
         transition: background var(--transition-fast);
+        padding: 2px;
+        display: flex;
+        align-items: center;
 
-        &::before {
-        content: '';
-        position: absolute;
-        top: 2px;
-        left: 2px;
-        width: 18px;
-        height: 18px;
-        background: var(--texto);
-        border-radius: 50%;
-        transition: transform var(--transition-fast), background var(--transition-fast);
+        &::after {
+            content: '';
+            width: 24px;
+            height: 24px;
+            background: #fff;
+            border-radius: 50%;
+            transition: transform var(--transition-fast);
+            box-shadow: var(--shadow-sm);
         }
     }
 
-    .SwitchText {
-        transition: color var(--transition-fast);
-    }
-
-    /* --- Estado activo --- */
     input:checked + .Switch {
-        background: var(--rojo-600);
+        background: var(--success);
 
-        &::before {
-            transform: translateX(20px);
+        &::after {
+            transform: translateX(22px);
         }
     }
 
+    .SwitchText {
+        font-weight: var(--weight-medium);
+        font-size: var(--font-sm);
+        transition: color var(--transition-fast);
+    }
+
     input:checked ~ .SwitchText {
         color: var(--success);
     }
 
-    /* --- Estado inactivo --- */
     input:not(:checked) ~ .SwitchText {
         color: var(--danger);
     }
+}
+
+/* ======================================================
+   BOTONES MODAL
+   ====================================================== */
 
-    input:focus + .Switch {
-        box-shadow: 0 0 4px var(--rojo-600);
+.ModalActions {
+    display: flex;
+    gap: var(--space-md);
+    justify-content: flex-end;
+    padding-top: var(--space-lg);
+    border-top: var(--border-width) solid var(--card-border);
+}
+
+.ModalActions button {
+    padding: var(--space-md) var(--space-lg);
+    border: none;
+    border-radius: var(--radius-md);
+    font-weight: var(--weight-semibold);
+    cursor: pointer;
+    transition:
+        transform var(--transition-fast),
+        box-shadow var(--transition-fast);
+    font-size: var(--font-md);
+
+    &.btn_primary {
+        background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
+        color: #fff;
+
+        &:hover {
+            transform: translateY(-2px);
+            box-shadow: var(--shadow-lg);
+        }
+    }
+
+    &.btn_secondary {
+        background: var(--azul-600);
+        color: #fff;
+
+        &:hover {
+            background: var(--azul-700);
+            transform: translateY(-2px);
+        }
+    }
+
+    &:disabled {
+        opacity: var(--opacity-disabled);
+        cursor: not-allowed;
     }
 }
 
-.FilaInputs {
-    display: flex;
-    gap: 1rem;
-    flex-wrap: wrap;
+/* ======================================================
+   SELECT DE REACT
+   ====================================================== */
 
-    label {
-        flex: 1;
+.SelectCategoria,
+.SelectMarca {
+    width: 100%;
+    z-index: 10;
+}
+
+/* ======================================================
+   ANIMACIONES
+   ====================================================== */
+
+@keyframes fadeIn {
+    from {
+        opacity: 0;
+        transform: scale(0.96);
+    }
+    to {
+        opacity: 1;
+        transform: scale(1);
     }
 }
 
-.ModalActions {
-    display: flex;
-    justify-content: flex-end;
-    gap: 1rem;
-
-    button {
-        padding: 0.5rem 1.2rem;
-        font-weight: 500;
-        border-radius: var(--radius-sm);
-        cursor: pointer;
-        transition: var(--transition-fast);
-        box-shadow: var(--shadow-1);
-
-        &.btn-secondary {
-            background-color: var(--btn-secondary-bg);
-            color: var(--btn-secondary-text);
-            border: none;
-
-            &:hover {
-                background-color: var(--btn-secondary-bg-hover);
-            }
-        }
+@keyframes slideDown {
+    from {
+        opacity: 0;
+        transform: translateY(-10px);
+    }
+    to {
+        opacity: 1;
+        transform: translateY(0);
+    }
+}
 
-        &.btn-primary {
-            background-color: var(--btn-primary-bg);
-            color: var(--btn-primary-text);
-            border: none;
+/* ======================================================
+   RESPONSIVE - TABLET
+   ====================================================== */
 
-            &:hover {
-                background-color: var(--btn-primary-bg-hover);
-            }
+@media (max-width: 768px) {
+    .Contenedor {
+        width: 100%;
+        display: flex;
+        flex-direction: column;
+        padding: var(--space-md);
+        gap: var(--space-lg);
+    }
 
-            &:active {
-                background-color: var(--btn-primary-bg-active);
-            }
-        }
+    .Modal {
+        max-width: 90%;
+        padding: var(--space-lg);
+        max-height: 85vh;
+    }
+
+    .FilaInputs {
+        grid-template-columns: 1fr;
+    }
+
+    .ModalActions {
+        justify-content: center;
+    }
+
+    .ModalActions button {
+        width: 100%;
     }
 }
 
-.ImagenContainer {
-    display: flex;
-    flex-direction: column;
-    gap: 0.8rem;
-    margin-bottom: 1rem;
+/* ======================================================
+   RESPONSIVE - MÓVIL
+   ====================================================== */
 
-    label {
-        font-weight: 500;
-        color: var(--texto-sec);
+@media (max-width: 576px) {
+    .Contenedor {
+        padding: var(--space-sm);
     }
 
-    input[type="file"] {
-        background-color: var(--input-bg);
-        color: var(--input-text);
-        border: 1px solid var(--input-border);
-        border-radius: var(--radius-sm);
-        padding: 0.4rem;
-        cursor: pointer;
-        transition: var(--transition-fast);
+    .Modal {
+        max-width: 95%;
+        padding: var(--space-lg);
+        border-radius: var(--radius-lg);
+    }
 
-        &:hover {
-            border-color: var(--input-focus-border);
+    .CloseButton {
+        width: 36px;
+        height: 36px;
+        font-size: var(--font-md);
+    }
+
+    .Modal h3 {
+        font-size: var(--font-lg);
+    }
+
+    .ImagenPreview {
+        min-height: 120px;
+
+        i {
+            font-size: var(--font-2xl);
         }
     }
+
+    .SwitchLabel {
+        flex-direction: column;
+        padding: var(--space-md);
+    }
 }
 
-.ImagenPreview {
-    display: flex;
-    justify-content: center;
-    align-items: center;
-    margin-top: 0.5rem;
+/* ======================================================
+   BOTONES OUTLINE (AlmacenesView)
+   ====================================================== */
 
-    img {
-        width: 120px;
-        height: 120px;
-        object-fit: cover;
-        border-radius: var(--radius-md);
-        border: 1px solid var(--card-border);
-        background: var(--fondo-elevado);
-        box-shadow: var(--shadow-2);
+.btnOutlinePrimary,
+.btnOutlineSecondary {
+    padding: var(--space-sm) var(--space-md);
+    border-radius: var(--radius-sm);
+    background: transparent;
+    font-size: var(--font-sm);
+    font-weight: var(--weight-medium);
+    cursor: pointer;
+    transition:
+        background var(--transition-fast),
+        color var(--transition-fast),
+        transform var(--transition-fast);
+
+    &:hover {
+        transform: translateY(-1px);
     }
+}
 
-    i {
-        display: flex;
-        justify-content: center;
-        align-items: center;
-        font-size: 120px;
-        color: var(--azul-200);
-        border-radius: var(--radius-md);
-        border: 1px solid var(--card-border);
-        background: var(--fondo-elevado);
-        box-shadow: var(--shadow-2);
+.btnOutlinePrimary {
+    border: var(--border-width) solid var(--rojo-500);
+    color: var(--rojo-400);
+
+    &:hover {
+        background: var(--rojo-500);
+        color: #fff;
     }
 }
 
+.btnOutlineSecondary {
+    border: var(--border-width) solid var(--azul-500);
+    color: var(--azul-300);
 
-/* ================================
-    Modal ImgGrande
-   ================================ */
-.ImgGrande{
-    position: fixed;
-    top: 0;
-    left: 0;
-    width: 100%;
-    height: 100vh;
-    background-color: rgba(0, 0, 0, 0.8);
+    &:hover {
+        background: var(--azul-500);
+        color: #fff;
+    }
+}
+
+/* ======================================================
+   GRID DE INVENTARIO (AlmacenesView)
+   ====================================================== */
+
+.inventarioGrid {
     display: flex;
+    flex-wrap: wrap;
     justify-content: center;
-    align-items: center;
-    z-index: 9999;
+    gap: var(--space-md);
+}
 
-    img{
-        max-width: 90%;
-        max-height: 90%;
-        object-fit: contain;
+.inventarioCard {
+    position: relative;
+    width: 250px;
+    display: flex;
+    flex-direction: column;
+    justify-content: space-between;
+    gap: var(--space-xs);
+    padding: var(--space-md);
+    background: var(--card-bg);
+    border-radius: var(--radius-md);
+    box-shadow: var(--shadow-md);
+    transition: box-shadow var(--transition-fast);
+
+    h3 {
+        text-align: center;
+        font-size: var(--font-lg);
+        color: var(--texto);
     }
 
-    i{
-        font-size: 200px;
-        color: var(--rojo-200);
+    p {
+        margin: 0;
+        text-align: center;
+        color: var(--texto-sec);
+
+        b {
+            color: var(--azul-300);
+        }
+    }
+
+    &:hover {
+        box-shadow: var(--shadow-lg);
     }
 }
 
+.inventarioImagen {
+    width: 100%;
+    height: 200px;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    overflow: hidden;
+    background: var(--fondo-elevado);
+    border-radius: var(--radius-md);
 
-/* ================================
-    RESPONSIVE
-   ================================ */
-@media (max-width: 768px) {
-    .Modal {
-        width: 95%;
-        padding: 1.5rem;
-        max-height: 90vh;
-        overflow-y: auto;
+    img {
+        width: 100%;
+        height: 100%;
+        object-fit: cover;
     }
 
-    .Header {
-        flex-direction: column;
-        gap: 1rem;
+    i {
+        font-size: 150px;
+        color: var(--azul-300);
     }
 }
 
-/* ================================
-    ANIMACIÓN
-   ================================ */
-@keyframes fadeIn {
-    from {
-        opacity: 0;
-        transform: scale(0.96);
-    }
-    to {
-        opacity: 1;
-        transform: scale(1);
+.inventarioAlmacenRow {
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    gap: var(--space-sm);
+
+    p {
+        text-align: left;
     }
 }
+
+.inventarioAcciones {
+    display: flex;
+    flex-wrap: wrap;
+    justify-content: center;
+    gap: var(--space-sm);
+    margin-top: var(--space-sm);
+}
+
+@media (max-width: 576px) {
+    .inventarioCard {
+        width: 100%;
+    }
+}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/deuda.module.scss b/Frontend/src/assets/Css/deuda.module.scss
index 484fc9e..9bc6afd 100644
--- a/Frontend/src/assets/Css/deuda.module.scss
+++ b/Frontend/src/assets/Css/deuda.module.scss
@@ -1,58 +1,76 @@
+/* ======================================================
+   CONTENEDOR PRINCIPAL
+   ====================================================== */
+
 .Container {
     display: grid;
     grid-template-columns: 1fr 3fr;
     align-items: start;
-    gap: 1rem;
+    gap: var(--space-lg);
     color: var(--texto);
 
     h2 {
         text-align: center;
         grid-column: span 2 / span 2;
         text-transform: capitalize;
+        font-size: var(--font-2xl);
+        font-weight: var(--weight-bold);
+        padding-bottom: var(--space-md);
+        border-bottom: var(--border-width) solid var(--card-border);
     }
 }
 
-/* Opciones de navegación */
+/* ======================================================
+   OPCIONES DE NAVEGACIÓN
+   ====================================================== */
+
 .Item {
     display: flex;
     flex-direction: column;
-    gap: 1rem;
+    gap: var(--space-lg);
 }
 
 .Opciones {
     display: flex;
     flex-direction: column;
     background: var(--card-bg);
-    border: 1px solid var(--card-border);
+    border: var(--border-width) solid var(--card-border);
     border-radius: var(--radius-md);
-    box-shadow: var(--shadow-1);
+    box-shadow: var(--shadow-md);
     overflow: hidden;
     position: relative;
-    transition: transform var(--transition-fast), box-shadow var(--transition-fast);
+    transition: 
+        transform var(--transition-fast),
+        box-shadow var(--transition-fast);
 
     a {
         color: var(--texto);
         text-decoration: none;
         display: flex;
         align-items: center;
-        gap: 1rem;
-        padding: 1rem 1.5rem;
+        gap: var(--space-lg);
+        padding: var(--space-lg) var(--space-xl);
         border-radius: var(--radius-md);
         transition: color var(--transition-fast);
-        font-weight: 500;
+        font-weight: var(--weight-semibold);
     }
 
     i {
-        font-size: 2rem;
+        font-size: var(--font-3xl);
         color: var(--azul-400);
+        transition: color var(--transition-fast);
     }
 
     &:hover {
         transform: translateY(-4px);
-        box-shadow: var(--shadow-2);
+        box-shadow: var(--shadow-lg);
 
         a {
-            color: var( --rojo-900);
+            color: #fff;
+        }
+
+        i {
+            color: #fff;
         }
     }
 
@@ -64,7 +82,7 @@
         width: 0%;
         height: 100%;
         border-radius: var(--radius-md);
-        background: var(--gradient-primary);
+        background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
         transition: width var(--transition-fast);
         z-index: -1;
     }
@@ -74,219 +92,344 @@
     }
 }
 
-/* Listado de deudas */
+/* ======================================================
+   LISTADO DE DEUDAS
+   ====================================================== */
+
 .Deudas {
     display: flex;
     flex-direction: column;
-    gap: 1rem;
+    gap: var(--space-lg);
     width: 100%;
 }
 
-/* Card individual de deuda */
+/* ======================================================
+   CARD INDIVIDUAL DE DEUDA
+   ====================================================== */
+
 .Deuda {
     display: flex;
+    gap: var(--space-lg);
     background: var(--card-bg);
-    border: 1px solid var(--card-border);
+    border: var(--border-width) solid var(--card-border);
     border-radius: var(--radius-md);
-    padding: 1.2rem;
+    padding: var(--space-lg);
     color: var(--texto);
-    box-shadow: var(--shadow-1);
-    transition: transform var(--transition-fast), box-shadow var(--transition-fast);
+    box-shadow: var(--shadow-md);
+    transition: 
+        transform var(--transition-fast),
+        box-shadow var(--transition-fast),
+        border-color var(--transition-fast);
 
     p {
-        margin: 0.3rem 0;
-        font-size: 0.95rem;
+        margin: var(--space-sm) 0;
+        font-size: var(--font-sm);
         color: var(--texto-sec);
 
         b {
-            color: var(--rojo-300);
+            color: var(--rojo-400);
+            font-weight: var(--weight-semibold);
         }
     }
 
     &:hover {
         transform: translateY(-3px);
-        box-shadow: var(--shadow-2);
+        box-shadow: var(--shadow-lg);
         border-color: var(--azul-500);
     }
+}
 
-    .Imagen {
-        width: 150px;
-        height: 150px;
-        display: flex;
-        align-items: center;
-        justify-content: center;
-        border-radius: var(--radius-md);
-        overflow: hidden;
-
-        img {
-            max-width: 100%;
-            max-height: 100%;
-            object-fit: contain;
-            border-radius: var(--radius-sm);
-        }
+/* ======================================================
+   IMAGEN DE DEUDA
+   ====================================================== */
 
-        i {
-            font-size: 150px;
-            color: var(--texto-muted);
-        }
-    }
-}
-.Detalles{
-    flex: 1;
-}
-.Boton{
-    background: var(--card-bg);
-    box-shadow: var(--shadow-1);
-    color: var(--texto);
-    text-decoration: none;
+.Imagen {
+    width: 150px;
+    height: 150px;
+    flex-shrink: 0;
     display: flex;
-    flex-direction: column;
     align-items: center;
-    justify-content: space-around;
-    padding: 0.5rem;
-    gap: 0.5rem;
+    justify-content: center;
     border-radius: var(--radius-md);
-    transition: color var(--transition-fast);
+    overflow: hidden;
+    background: var(--fondo-elevado);
+
+    img {
+        max-width: 100%;
+        max-height: 100%;
+        object-fit: contain;
+        border-radius: var(--radius-sm);
+    }
 
-    i{
-        font-size: 2rem;
-        color: var(--warning)
+    i {
+        font-size: 80px;
+        color: var(--texto-sec);
     }
 }
 
+/* ======================================================
+   DETALLES DE DEUDA
+   ====================================================== */
+
+.Detalles {
+    flex: 1;
+    display: flex;
+    flex-direction: column;
+    justify-content: space-between;
+}
 
 .Cabecera {
     display: flex;
     flex-direction: column;
-    gap: 0.3rem;
+    gap: var(--space-sm);
 
     p {
         margin: 0;
-        font-size: 0.95rem;
+        font-size: var(--font-sm);
         color: var(--texto-sec);
 
         b {
-        color: var(--rojo-300);
+            color: var(--rojo-400);
+            font-weight: var(--weight-semibold);
         }
     }
 }
 
-.Filtros {
-    position: sticky;
-    top: 120px;
+/* ======================================================
+   BOTÓN DE ACCIÓN
+   ====================================================== */
+
+.Boton {
+    background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
+    box-shadow: var(--shadow-md);
+    color: #fff;
+    text-decoration: none;
     display: flex;
-    flex-wrap: wrap;
-    gap: 1rem;
-    margin-bottom: 1.5rem;
-    padding: 1rem;
-    background: var(--superficie);
-    border-radius: 12px;
-    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
+    flex-direction: column;
+    align-items: center;
+    justify-content: center;
+    padding: var(--space-md);
+    gap: var(--space-sm);
+    border-radius: var(--radius-md);
+    border: none;
+    cursor: pointer;
+    transition: 
+        transform var(--transition-fast),
+        box-shadow var(--transition-fast);
+    font-weight: var(--weight-semibold);
 
-    label {
-        display: flex;
-        flex-direction: column;
-        gap: 0.4rem;
-        flex: 1;
-        min-width: 200px;
-        font-size: 0.9rem;
-        font-weight: 500;
-        color: var(--texto-sec);
-        text-transform: capitalize;
+    i {
+        font-size: var(--font-2xl);
+    }
 
-        input,
-        select {
-            padding: 0.6rem 1rem;
-            border: 1px solid var(--glass);
-            border-radius: 8px;
-            background-color: #fff;
-            font-size: 0.95rem;
-            transition: all 0.2s ease-in-out;
-
-            &:focus {
-                outline: none;
-                border-color: var(--rojo-500);
-                box-shadow: 0 0 0 3px rgba(255, 0, 0, 0.2);
-            }
-        }
+    &:hover {
+        transform: translateY(-2px);
+        box-shadow: var(--shadow-lg);
+    }
+
+    &:active {
+        transform: translateY(0);
     }
 }
 
+/* ======================================================
+   FORMULARIO
+   ====================================================== */
+
 .Form {
     grid-column: span 2 / span 2;
     display: flex;
     flex-direction: column;
-    gap: 1.2rem;
+    gap: var(--space-lg);
     background: var(--card-bg);
-    padding: 2rem;
+    padding: var(--space-2xl);
     border-radius: var(--radius-md);
-    box-shadow: var(--shadow-1);
+    border: var(--border-width) solid var(--card-border);
+    box-shadow: var(--shadow-md);
 }
 
 .InputGroup {
     display: flex;
     flex-direction: column;
-    gap: 0.5rem;
+    gap: var(--space-sm);
 
     label {
-        font-weight: 600;
+        font-weight: var(--weight-semibold);
+        color: var(--texto);
+        font-size: var(--font-sm);
     }
 
     input,
     select,
     textarea {
-        padding: 0.7rem;
-        border: 1px solid var(--card-border);
-        border-radius: var(--radius-sm);
-        background: #fff;
-        font-size: 1rem;
+        width: 100%;
+        padding: var(--space-md);
+        border: var(--border-width) solid var(--input-border);
+        border-radius: var(--radius-md);
+        background: var(--input-bg);
+        color: var(--texto);
+        font-size: var(--font-md);
+        font-family: inherit;
+        transition:
+            border-color var(--transition-fast),
+            box-shadow var(--transition-fast);
+
+        &::placeholder {
+            color: var(--placeholder);
+        }
+
+        &:hover {
+            border-color: var(--rojo-700);
+        }
+
+        &:focus {
+            outline: none;
+            border-color: var(--rojo-500);
+            box-shadow: var(--focus-ring);
+        }
+    }
+
+    textarea {
+        resize: vertical;
+        min-height: 100px;
     }
 }
 
+/* ======================================================
+   BOTÓN SUBMIT
+   ====================================================== */
+
 .BotonSubmit {
-    padding: 0.8rem 1.5rem;
+    padding: var(--space-md) var(--space-xl);
     border: none;
     border-radius: var(--radius-md);
-    background: var(--azul-400);
+    background: linear-gradient(135deg, var(--rojo-600), var(--azul-600));
     color: #fff;
-    font-weight: bold;
+    font-weight: var(--weight-semibold);
+    font-size: var(--font-md);
     cursor: pointer;
-    transition: background var(--transition-fast);
+    transition: 
+        transform var(--transition-fast),
+        box-shadow var(--transition-fast);
+    box-shadow: var(--shadow-md);
 
     &:hover {
-        background: var(--azul-500);
+        transform: translateY(-2px);
+        box-shadow: var(--shadow-lg);
+    }
+
+    &:active {
+        transform: translateY(0);
+    }
+
+    &:disabled {
+        opacity: var(--opacity-disabled);
+        cursor: not-allowed;
     }
 }
 
+/* ======================================================
+   MENSAJES
+   ====================================================== */
+
 .ErrorMsg {
-    color: var(--rojo-500);
-    margin-top: 1rem;
+    padding: var(--space-md);
+    color: var(--danger);
+    background: rgba(198, 40, 40, 0.1);
+    border: var(--border-width) solid var(--danger);
+    border-radius: var(--radius-md);
+    font-weight: var(--weight-medium);
+    margin-top: var(--space-lg);
 }
 
 .SuccessMsg {
-    color: var(--verde-500);
-    margin-top: 1rem;
+    padding: var(--space-md);
+    color: var(--success);
+    background: rgba(39, 174, 96, 0.1);
+    border: var(--border-width) solid var(--success);
+    border-radius: var(--radius-md);
+    font-weight: var(--weight-medium);
+    margin-top: var(--space-lg);
 }
 
+/* ======================================================
+   RESPONSIVE - TABLET
+   ====================================================== */
 
-/* media queries */
-@media screen and (max-width: 850px) {
+@media (max-width: 850px) {
     .Container {
         grid-template-columns: 1fr;
-        gap: 1rem;
+        gap: var(--space-lg);
 
         h2 {
             grid-column: span 1 / span 1;
         }
     }
-    .Relleno{
-        display: none;
+
+    .Imagen {
+        width: 120px;
+        height: 120px;
+
+        i {
+            font-size: 60px;
+        }
     }
-    .Imagen{
-        width: 80px;
-        height: 80px;
+
+    .Form {
+        grid-column: span 1 / span 1;
     }
-    .Filtros{
-        position: static;
+}
+
+/* ======================================================
+   RESPONSIVE - MÓVIL
+   ====================================================== */
+
+@media (max-width: 576px) {
+    .Container {
+        padding: var(--space-md);
+        gap: var(--space-md);
+    }
+
+    .Container h2 {
+        font-size: var(--font-xl);
+    }
+
+    .Deuda {
+        flex-direction: column;
+        gap: var(--space-md);
+        padding: var(--space-md);
+    }
+
+    .Imagen {
+        width: 100%;
+        height: auto;
+        max-height: 150px;
+
+        i {
+            font-size: 50px;
+        }
+    }
+
+    .Cabecera {
+        order: -1;
+    }
+
+    .Boton {
+        width: 100%;
+        padding: var(--space-md);
+    }
+
+    .Form {
+        padding: var(--space-lg) var(--space-md);
+    }
+
+    .InputGroup input,
+    .InputGroup select,
+    .InputGroup textarea {
+        font-size: 16px; /* Previene zoom en iOS */
+    }
+
+    .BotonSubmit {
+        width: 100%;
     }
 }
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/index.css b/Frontend/src/assets/Css/index.css
deleted file mode 100644
index f9116c1..0000000
--- a/Frontend/src/assets/Css/index.css
+++ /dev/null
@@ -1,339 +0,0 @@
-:root {
-    /* -------------------- BASE / SUPERFICIES -------------------- */
-    --fondo: linear-gradient(var(--rojo-900) 0%, var(--azul-900) 100%);
-    --fondo-elevado: #121012;
-    --fondo-Pos: #75757568;
-    --superficie: #372525;
-    --glass: #ffffff08;
-
-
-    /* -------------------- PALETA ROJOS (principal) -------------------- */
-    --rojo-900: #2b0000;
-    --rojo-800: #420a0a;
-    --rojo-700: #6d1414;
-    --rojo-600: #9b1f1f;
-    --rojo-500: #c62828;
-    --rojo-400: #e53935;
-    --rojo-300: #f57373;
-    --rojo-200: #ffabab;
-    --rojo-100: #ffdede;
-
-    /* -------------------- PALETA azul (complementaria) -------------------- */
-    --azul-900: #0a1a26;
-    --azul-800: #0f2536;
-    --azul-700: #15354c;
-    --azul-600: #1e4a6a;
-    --azul-500: #2b6fa3;
-    --azul-400: #3d8bbd;
-    --azul-300: #5fa8d3;
-    --azul-200: #91c9e8;
-    --azul-100: #cfeaf7;
-
-    /* -------------------- SEMÁNTICOS / TIPOGRAFÍA -------------------- */
-    --texto: #f5f3f3;
-    --texto-negro: #333;
-    --texto-sec: #cfc9c9;
-    --texto-muted: #9b8f8f;
-    --placeholder: #7e7373;
-
-    /* -------------------- CABECERA / FOOTER -------------------- */
-    --header-bg: var(--rojo-800);
-    --footer-bg: var(--azul-800);
-
-    /* -------------------- BOTONES (rojo principal) -------------------- */
-    --btn-primary-bg: var(--rojo-600);
-    --btn-primary-bg-hover: var(--rojo-500);
-    --btn-primary-bg-active: var(--rojo-700);
-    --btn-primary-text: #ffffff;
-
-    --btn-secondary-bg: var(--azul-600);
-    --btn-secondary-bg-hover: var(--azul-500);
-    --btn-secondary-bg-active: var(--azul-700);
-    --btn-secondary-text: #ffffff;
-
-    --btn-disabled-bg: #221818;
-    --btn-disabled-text: #6b6161;
-
-    /* -------------------- BOTONES (azul complementario) -------------------- */
-    --btn-terciario-bg: var(--azul-600);
-    --btn-terciario-bg-hover: var(--azul-500);
-    --btn-terciario-bg-active: var(--azul-700);
-    --btn-terciario-text: #ffffff;
-
-    /* -------------------- INPUTS -------------------- */
-    --input-bg: #110e0e;
-    --input-text: var(--texto);
-    --input-placeholder: var(--placeholder);
-    --input-border: #2a1f1f;
-    --input-focus-border: var(--rojo-500);
-
-    /* -------------------- CARDS -------------------- */
-    --card-bg: var(--superficie);
-    --card-border: #201515;
-
-    /* -------------------- SOMBRAS -------------------- */
-    --shadow-1: 0 2px 6px #0000008c;
-    --shadow-2: 0 2px 6px #ffffff99;
-
-    /* -------------------- GRADIENTS -------------------- */
-    --gradient-primary: linear-gradient(135deg, var(--rojo-100), var(--rojo-800));
-    --gradient-alt: linear-gradient(135deg, var(--azul-100), var(--azul-700));
-
-    /* -------------------- UTILIDADES -------------------- */
-    --radius-sm: 6px;
-    --radius-md: 10px;
-    --radius-lg: 16px;
-    --border-width: 1px;
-    --transition-fast: 0.5s ease;
-
-    /* -------------------- STATUS COLORS -------------------- */
-    --success: #27ae60;
-    --warning: #f39c12;
-    --info: #3498db;
-    --danger: var(--rojo-500);
-
-    /* -------------------- BOOTSTRAP OVERRIDES -------------------- */
-    --bs-body-bg: var(--fondo);
-    --bs-body-color: var(--texto);
-    --bs-primary: var(--rojo-600);
-    --bs-primary-rgb: 155, 31, 31;
-    --bs-secondary: #3f2626;
-    --bs-secondary-rgb: 63, 38, 38;
-    --bs-danger: var(--rojo-500);
-    --bs-success: var(--success);
-    --bs-warning: var(--warning);
-    --bs-info: var(--info);
-    --bs-link-color: var(--rojo-400);
-    --bs-link-hover-color: var(--rojo-300);
-
-    /* Nuevo: asignación opcional para usar azul como "info-alt" */
-    --bs-purple: var(--azul-600);
-    --bs-purple-rgb: 106, 13, 173;
-
-    /* -------------------- POS -------------------- */
-    --pos-item-bg: var(--card-bg);
-    --pos-item-hover: var(--glass);
-    --pos-item-active: var(--rojo-700);
-
-    --pos-total-bg: var(--rojo-800);
-    --pos-total-text: var(--texto);
-
-    --pos-mesa-activa-bg: var(--rojo-600);
-    --pos-mesa-inactiva-bg: var(--azul-700);
-
-    --pos-badge-bg: var(--azul-600);
-    --pos-badge-text: #fff;
-
-    --pos-divider: var(--card-border);
-    
-    --modal-dark-bg: var(--fondo-elevado);
-    --modal-dark-text: var(--texto);
-    --modal-dark-divider: var(--pos-divider);
-
-    --pos-item-hover: color-mix(in srgb, var(--glass) 70%, transparent);
-    --pos-item-active: var(--rojo-700);
-
-    --input-focus-shadow: 0 0 0 2px color-mix(in srgb, var(--rojo-500) 40%, transparent);
-
-    --success-soft: color-mix(in srgb, var(--success) 20%, transparent);
-    --success-text: var(--texto);
-
-    --danger-soft: color-mix(in srgb, var(--danger) 20%, transparent);
-    --danger-text: var(--texto);
-
-    --pos-row-header: var(--card-bg);
-}
-
-/* ---- EXTRA ---- */
-/* Botón terciario */
-.btn-terciario {
-    background-color: var(--btn-terciario-bg) !important;
-    border-color: var(--btn-terciario-bg) !important;
-    color: var(--btn-terciario-text) !important;
-}
-.btn-terciario:hover {
-    background-color: var(--btn-terciario-bg-hover) !important;
-    border-color: var(--btn-terciario-bg-hover) !important;
-}
-.btn-terciario:active {
-    background-color: var(--btn-terciario-bg-active) !important;
-    border-color: var(--btn-terciario-bg-active) !important;
-}
-
-/* Badge azul */
-.badge-azul {
-    background-color: var(--azul-600);
-    color: #fff;
-}
-
-/* Card alternativa */
-.card-alt {
-    background: var(--gradient-alt);
-    color: #fff;
-}
-
-/* =========================
-   SPACING SYSTEM
-   ========================= */
-
-:root {
-    --space-xxs: 4px;
-    --space-xs: 8px;
-    --space-sm: 12px;
-    --space-md: 16px;
-    --space-lg: 24px;
-    --space-xl: 32px;
-    --space-2xl: 48px;
-    --space-3xl: 72px;
-    --space-4xl: 96px;
-}
-
-body,
-#root {
-    width: 100%;
-    min-height: 100vh;
-    height: auto;
-    background: var(--fondo) !important;
-    background-repeat: no-repeat !important;
-    background-attachment: fixed !important;
-    background-size: cover !important;
-    color: var(--texto);
-    font-family: sans-serif;
-    display: flex;
-    flex-direction: column;
-    align-items: center;
-    gap: 15px;
-    margin: 0;
-    scroll-behavior: smooth;
-    -webkit-font-smoothing: antialiased;
-    -moz-osx-font-smoothing: grayscale;
-    position: relative;
-}
-
-/* Ajustes para que Bootstrap use tus colores */
-.btn-primary {
-    background-color: var(--btn-primary-bg) !important;
-    border-color: var(--btn-primary-bg) !important;
-    color: var(--btn-primary-text) !important;
-}
-.btn-primary:hover {
-    background-color: var(--btn-primary-bg-hover) !important;
-    border-color: var(--btn-primary-bg-hover) !important;
-}
-.btn-secondary {
-    background-color: var(--btn-secondary-bg) !important;
-    border-color: var(--btn-secondary-bg) !important;
-    color: var(--btn-secondary-text) !important;
-}
-.btn-secondary:hover {
-    background-color: var(--btn-secondary-bg-hover) !important;
-    border-color: var(--btn-secondary-bg-hover) !important;
-}
-
-.btn-outline-primary{
-    color: var(--btn-primary-bg) !important;
-    border-color: var(--btn-primary-bg) !important;
-}
-.btn-outline-primary:hover{
-    background-color: var(--btn-primary-bg-hover) !important;
-    border-color: var(--btn-primary-bg-hover) !important;
-    color: var(--btn-primary-text) !important;
-}
-
-.btn-outline-secondary{
-    color: var(--btn-secondary-bg) !important;
-    border-color: var(--btn-secondary-bg) !important;
-}
-.btn-outline-secondary:hover{
-    background-color: var(--btn-secondary-bg-hover) !important;
-    border-color: var(--btn-secondary-bg-hover) !important;
-    color: var(--btn-secondary-text) !important;
-}
-
-.card {
-    background-color: var(--card-bg) !important;
-    border: 1px solid var(--card-border) !important;
-    color: var(--texto) !important;
-}
-
-.form-control {
-    background-color: var(--input-bg) !important;
-    color: var(--input-text) !important;
-    border: 1px solid var(--input-border) !important;
-}
-.form-control:focus {
-    border-color: var(--input-focus-border) !important;
-    box-shadow: 0 0 0 0.25rem rgba(198,40,40,0.25) !important;
-}
-
-a {
-    color: var(--bs-link-color);
-    text-decoration: none;
-}
-a:hover {
-    color: var(--bs-link-hover-color);
-}
-
-* {
-    box-sizing: border-box;
-    margin: 0;
-    padding: 0;
-}
-
-/*----- Cargando -----*/
-.cargando{
-    width: 100%;
-    height: 80vh;
-    display: flex;
-    flex-direction: column;
-    justify-content: center;
-    align-items: center;
-}
-.cargando i{
-    font-size: 150px;
-    color: var(--azul-500);
-}
-
-main{
-    width: 90% !important;
-}
-
-
-.volver{
-    width: 95%;
-    z-index: 100;
-
-    .butnvolver{
-        position: fixed;
-        top: 120px;
-    }
-    button{
-        border-radius: var(--radius-md);
-        padding: 10px;
-        background-color: var(--btn-terciario-bg);
-        border: none;
-        color: var(--btn-terciario-text);
-        cursor: pointer;
-        position: sticky;
-        top: 0 ;
-    }
-    button:hover{
-        background-color: var(--btn-terciario-bg-hover);
-    }
-}
-
-/* media queries */
-@media (max-width: 768px) {
-    main{
-        width: 95% !important;
-    }
-    main > h2{
-        margin-top: 2rem;
-    }
-    .volver{
-        .butnvolver{
-            position: fixed;
-            top: 80px;
-        }
-    }
-}
\ No newline at end of file
diff --git a/Frontend/src/assets/Css/index.module.scss b/Frontend/src/assets/Css/index.module.scss
deleted file mode 100644
index aa85087..0000000
--- a/Frontend/src/assets/Css/index.module.scss
+++ /dev/null
@@ -1,236 +0,0 @@
-/* ---------------- HEADER ---------------- */
-.Header {
-    width: 100%;
-    display: flex;
-    justify-content: center;
-    align-items: center;
-    padding: 10px;
-    position: sticky;
-    top: 0;
-    z-index: 1000;
-}
-
-.Header-container {
-    width: 95%;
-    background: var(--header-bg);
-    border-radius: var(--radius-md);
-    padding: 10px 20px;
-    display: flex;
-    justify-content: space-between;
-    align-items: center;
-    gap: 20px;
-    flex-wrap: wrap;
-    box-shadow: var(--shadow-1);
-}
-
-.Header-logo, 
-.Overlay-logo {
-    width: 80px;
-    height: 80px;
-}
-.Header-logo img, 
-.Overlay-logo img {
-    width: 100%;
-    height: 100%;
-    object-fit: contain;
-}
-
-.HeaderUser{
-    display: flex;
-    text-decoration: none;
-    width: 80px;
-    height: 80px;
-    border-radius: 50%;
-    overflow: hidden;
-    cursor: pointer;
-
-    img{
-        width: 100%;
-        height: 100%;
-        object-fit: cover;
-    }
-
-    i{
-        font-size: 80px;
-        color: var(--texto);
-        text-align: center;
-    }
-}
-
-.Header-title {
-    color: var(--texto);
-    font-size: 1.5rem;
-    font-weight: bold;
-}
-
-.Header-nav {
-    flex: 1;
-    display: flex;
-    justify-content: flex-end;
-}
-
-/* ===== LISTAS DE NAVEGACIÓN ===== */
-.Header-list {
-    list-style: none;
-    display: flex;
-    text-align: center;
-    gap: 20px;
-    margin: 0;
-    padding: 0;
-}
-
-/* ===== LINKS DE NAVEGACIÓN ===== */
-.Header-link {
-    position: relative;
-    padding: 10px;
-    border-radius: var(--radius-md);
-    display: flex;
-    flex-direction: column;
-    text-align: center;
-    text-decoration: none;
-    color: var(--texto);
-    font-weight: 500;
-    font-size: 1rem;
-    transition: color var(--transition-fast);
-    overflow: hidden;
-    z-index: 1;
-
-    i {
-        font-size: 1.5rem;
-    }
-}
-
-.Header-link::before {
-    content: "";
-    position: absolute;
-    top: 0;
-    left: 0;
-    width: 100%;
-    height: 0%;
-    background: var(--gradient-primary);
-    transition: height var(--transition-fast);
-    z-index: -1;
-}
-
-.Header-link:hover {
-    color: var(--rojo-900);
-}
-
-.Header-link:hover::before {
-    height: 100%;
-}
-
-/* Botón hamburguesa */
-.Header-burger {
-    display: none;
-    background: none;
-    border: none;
-    font-size: 2rem;
-    color: var(--texto);
-    cursor: pointer;
-}
-
-/* Overlay lateral */
-.Header-overlay {
-    position: fixed;
-    top: 0;
-    left: -100%;
-    width: 100%;
-    height: 100%;
-    background: var(--header-bg);
-    transition: left var(--transition-fast);
-    z-index: 999;
-    display: flex;
-    justify-content: center;
-    align-items: center;
-}
-.Header-overlay.is-open {
-    left: 0;
-}
-
-.Overlay-content {
-    display: flex;
-    flex-direction: column;
-    align-items: center;
-    gap: 40px;
-}
-
-@media (max-width: 850px) {
-    .only-desktop {
-        display: none;
-    }
-    .only-mobile {
-        display: block;
-    }
-
-    .Header-list {
-        flex-direction: column;
-        gap: 20px;
-    }
-    .Header-nav {
-        display: none; 
-    }
-    .Header-title {
-        font-size: 1.2rem;
-    }
-}
-
-/* ---------------- CONTAINER ---------------- */
-.Container {
-    position: relative;
-    width: 90%;
-    display: flex;
-    flex-direction: column;
-    align-items: center;
-    gap: 15px;
-}
-
-/* ---------------- FOOTER ---------------- */
-.Footer {
-    width: 100%;
-    display: flex;
-    justify-content: center;
-    align-items: center;
-    padding: 10px;
-}
-
-.Footer-container {
-    width: 95%;
-    background: var(--footer-bg);
-    border-radius: var(--radius-md);
-    padding: 15px 20px;
-    display: flex;
-    justify-content: space-between;
-    align-items: center;
-    flex-wrap: wrap;
-    color: var(--texto);
-    box-shadow: var(--shadow-1);
-
-    p{
-        margin: 0;
-    }
-}
-
-.FooterContador{
-    display: flex;
-    flex-direction: column;
-    justify-content: center;
-    align-items: center;
-    text-align: center;
-    gap: 0.5rem;
-}
-
-.FooterTiempo{
-    display: flex;
-    align-items: center;
-    gap: 0.5rem;
-}
-
-/* RESPONSIVE FOOTER */
-@media (max-width: 850px) {
-    .Footer-container {
-        flex-direction: column;
-        text-align: center;
-        gap: 5px;
-    }
-}
diff --git a/Frontend/src/main.jsx b/Frontend/src/main.jsx
index 44b8d86..7747b7e 100644
--- a/Frontend/src/main.jsx
+++ b/Frontend/src/main.jsx
@@ -3,25 +3,45 @@ import { createRoot } from 'react-dom/client';
 import { RouterProvider } from 'react-router-dom';
 
 // Estilos propios
-import './assets/Css/index.css';
+import './assets/Css/Principales/Global.Oscuro.scss';
+import './assets/Css/Principales/Global.Claro.scss';
+import './assets/Css/Principales/Global.css';
 
 // Estilos librerias 
-import 'bootstrap/dist/css/bootstrap.min.css';
 import 'boxicons/css/boxicons.min.css';
 import 'ldrs/react/Hourglass.css'
 
 import router from './router.jsx';
 import { AuthProvider } from './Pages/Context/AuthContext.jsx';
 import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
+import { ColorContexts } from './Pages/Context/ColorContexts.jsx';
 
 const queryClient = new QueryClient()
 
-createRoot(document.getElementById('root')).render(
-  <StrictMode>
+/**
+ * Orden de providers (de fuera hacia dentro):
+ *   QueryProvider   → cliente react-query global
+ *   ColorContexts   → Tema oscuro/claro/sistema
+ *   AuthProvider    → proveedor de autenticacion
+ *   RouterProvider → proveedor de rutas
+ */
+
+function ProvidersWrap({ children }) {
+  return (
     <QueryClientProvider client={queryClient}>
-      <AuthProvider>
-        <RouterProvider router={router} />
-      </AuthProvider>
+      <ColorContexts>
+        <AuthProvider>
+          {children}
+        </AuthProvider>
+      </ColorContexts>
     </QueryClientProvider>
+  )
+}
+
+createRoot(document.getElementById('root')).render(
+  <StrictMode>
+    <ProvidersWrap>
+      <RouterProvider router={router} />
+    </ProvidersWrap>
   </StrictMode>,
 )
diff --git a/Frontend/src/modules/Pos/components/productos/BuscadorProducto.jsx b/Frontend/src/modules/Pos/components/productos/BuscadorProducto.jsx
index a8b4511..108830f 100644
--- a/Frontend/src/modules/Pos/components/productos/BuscadorProducto.jsx
+++ b/Frontend/src/modules/Pos/components/productos/BuscadorProducto.jsx
@@ -1,6 +1,6 @@
 import Select from "react-select";
 import styles from "../../../../assets/Css/Pos/productoRow.module.scss";
-import {Loading, Error} from "../../../../Utils/Cargando";
+import {Loading, Error} from "../../../../Utils/Components/Cargando";
 
 export default function BuscadorProducto({ 
     agregarProducto, 
diff --git a/Frontend/src/router.jsx b/Frontend/src/router.jsx
index cea3df9..7d79f03 100644
--- a/Frontend/src/router.jsx
+++ b/Frontend/src/router.jsx
@@ -27,6 +27,7 @@ import AlmacenesView from "./Components/Vendedor/GestionInventario/CrudAlmacenes
 import POSLayout from "./modules/Pos/pages/PosLayout";
 import CierreDia from "./Components/Vendedor/CierreDia/CierreDia";
 import Bolsillos from "./Components/Vendedor/CierreDia/Bolsillos";
+import DashboardAdmin from "./Components/Admin/DashboardAdmin";
 
 const router = createBrowserRouter([
     {
@@ -73,7 +74,7 @@ const router = createBrowserRouter([
     {
         element: <LayoutPrivado requiredRole={1} />,
         children: [
-            { path: "/Administrador", element: <h1>Administrador</h1>, },
+            { path: "/Administrador", element: <DashboardAdmin />, },
         ],
     },
 ]);
diff --git a/Frontend/vercel.json b/Frontend/vercel.json
deleted file mode 100644
index e2a4bd7..0000000
--- a/Frontend/vercel.json
+++ /dev/null
@@ -1,5 +0,0 @@
-{
-    "rewrites": [
-        { "source": "/(.*)", "destination": "/" }
-    ]
-}
\ No newline at end of file
diff --git a/cambios.txt b/cambios.txt
new file mode 100644
index 0000000..e69de29
diff --git a/docker-compose.yml b/docker-compose.yml
index 9eccec8..90dfc97 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -36,6 +36,6 @@ services:
     build: ./Frontend
     container_name: cigarreria_frontend
     ports:
-      - "5173:80"
+      - "80:80"
     depends_on:
       - api
\ No newline at end of file
